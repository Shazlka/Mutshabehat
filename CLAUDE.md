@AGENTS.md

# Project: متشابهات V2 (Mutshabehat V2)

Arabic Quran "mutashabihat" (similar verses) library. Next.js 16 (App Router, **Turbopack**),
React 19, Supabase (SSR + RLS), Tailwind v4, D3 (network graph only).

- **📘 Start with `PROJECT_MASTER.md`**: locations, GitHub/Vercel/Tailscale wiring, backend, schema, design, rules.
- **Live:** https://mutshabehat-v2.vercel.app
- **Deploy:** `git push origin main` (Vercel git-connected, Production Branch `main`; fallback
  `vercel --prod --yes --scope shazlka-s-projects`, project `mutshabehat-v2`, user `amreshazly-4497`)
- **Backend:** self-hosted Supabase (Postgres + GoTrue + PostgREST + Caddy) on the Mac Mini under Colima,
  exposed at `https://youssefs-mac-mini.tailcd68dd.ts.net:8443` (Tailscale Funnel). The Supabase Cloud
  project `tthlhkdmwusxerfiimgc` is retired (paused, data migrated 2026-08-31).
- **Location:** `~/Projects/mutshabehat-v2` on the Mac Mini (GitHub `Shazlka/Mutshabehat`, branch
  `main`; V2 replaced the legacy V84 app on 2026-09-15, which is archived as `legacy-v84-main` / tag `legacy-v84-final`). This is the
  only checkout (`~/dev/mutshabehat-v2` was removed 2026-09-15). Ignore iCloud/OneDrive "Mutshabehat" folders.
- **Local build/dev env gotcha:** if a shell has `__NEXT_PROCESSED_ENV` set (leaks in from another
  Next.js process), `next build`/`next dev` will **skip loading `.env.local`** and fail to prerender
  with "Supabase URL and API key are required". Run with `env -u __NEXT_PROCESSED_ENV ...` or use a
  clean shell. Also unset any foreign `NEXT_PUBLIC_*` vars from other projects.

## Gotchas (read before coding)
- AGENTS.md: this Next.js has breaking changes — check `node_modules/next/dist/docs/` first.
- `ssr: false` dynamic imports are **NOT allowed in Server Components** — wrap in a client component.
- **Turbopack build output does NOT print Size / First Load JS columns.** Measure bundles via
  `.next/static/chunks` file sizes instead.
- `public/quran/ayahs.json` is **Uthmani script** (alef wasla `ٱ` U+0671, not `ا`). Any text
  matching must go through `normalizeArabic` (`src/lib/arabic.ts`), which folds `ٱ→ا` + strips tatweel.
- Applying DB DDL: the `service_role` key CANNOT run DDL via PostgREST. The backend is self-hosted, so the
  Management API no longer applies. Back up, then run `docker compose exec -T db psql -U postgres -d postgres
  -v ON_ERROR_STOP=1 --single-transaction < file.sql` from `/Volumes/External Mini/Projects/apps/mutshabehat-selfhost`,
  then `NOTIFY pgrst, 'reload schema'` (PROJECT_MASTER.md §8). The `supabase` CLI and host `psql` are not installed.
- **📋 CHANGELOG IS MANDATORY:** every bug fix, feature, or performance improvement **MUST** be
  recorded — dated, newest-first — in **both** the `# Changelog` section below **and** the root
  `CHANGELOG.md` (keep the two in sync), before the work is considered done. This is the running
  history/log of all changes for future reference.

# Changelog
- **Qiraat — deep audited import, Mushaf pages 303–306:** added **8** net source-backed wording variants (p303=4, p304=2, p305=1, p306=1). The additions use real token anchors and exact source rows; page 306 `مِن تَحْتِهَا` was reconciled into the existing locus so the remainder group is Q02/Q03/Q04/Q05-R01/Q09-R01/Q10, with no duplicate locus. Bare «خلف» remains Q06-R01; Q10 is included only where the source remainder or explicit name requires it. **Verification:** importer rerun idempotent; validator clean (2,811 variants, 9,888 rulings, 20 one-colour categories); 38/38 tests; TypeScript passed; `git diff --check` passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{303–306}.json`, `packages/qiraat-core/engine.test.mjs`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — attribution correction, Mushaf pages 583–584:** added **0** wording variants (p583=0, p584=0) and corrected **3** existing usul loci by removing erroneous Q10 assignments from bare-«خلف» rows; Q06 coverage remains intact. **Verification:** validator clean; 38/38 tests; source rows with non-token-backed multiword/ellipsis forms remain withheld. **Files:** `packages/qiraat-core/fixtures/rulings/page-{583–584}.json`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited source pass, Mushaf pages 225–234:** reviewed all source rows with the fixture importer; no new fixture records were added because the approved token-backed faces were already represented or safely withheld under the fail-closed rules (p225=0, p226=0, p227=0, p228=0, p229=0, p230=0, p231=0, p232=0, p233=0, p234=0). Added parser coverage for the standalone page-229 Thamud source paragraph and explicit page-225/226 rows, preserving exact source text and the Q06 bare-«خلف» attribution rule. **Verification:** importer rerun idempotent; validator clean (2,803 variants, 9,886 rulings, 20 one-colour categories); 38/38 tests; TypeScript and production build passed; `git diff --check` passed. **Files:** `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited import cycle, Mushaf pages 235–250:** added **1** source-backed farsh variant (p235=0, p236=0, p237=0, p238=0, p239=0, p240=0, p241=0, p242=0, p243=0, p244=0, p245=0, p246=0, p247=0, p248=0, p249=0, p250=1). Corrected the page-248 remainder assertion so «الباقين» includes Q10 consistently without dropping the audited face; unresolved spelling-only performance forms remain withheld. Bare «خلف» remains Q06; Q10 is never inferred from bare Khalaf. **Verification:** importer rerun idempotent; validator clean (2,803 variants, 9,886 rulings, 20 one-colour categories); 38/38 tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-250.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited import cycle, Mushaf pages 251–262:** added **5** source-backed farsh variants (p251=0, p252=0, p253=1, p254=1, p255=0, p256=0, p257=1, p258=0, p259=0, p260=1, p261=1, p262=0). Added a fail-closed page-260 parser fix so the packaged standalone Hisham source paragraph is accepted only when unique, preserving its exact source text and token anchor. Bare «خلف» remains Q06; Q10 requires explicit identification. **Verification:** importer rerun idempotent; validator clean (2,802 variants, 9,886 rulings, 20 one-colour categories); 38/38 tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{253,254,257,260,261}.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited import cycle, Mushaf pages 263–278:** added **8** source-backed farsh variants (p263=0, p264=0, p265=0, p266=1, p267=1, p268=0, p269=0, p270=1, p271=0, p272=2, p273=0, p274=0, p275=0, p276=1, p277=0, p278=2). Preserved exact source provenance and real token anchors, retained explicit reader-specific faces, and withheld performance-only or unresolved rows. Bare «خلف» remains Q06; Q10 requires explicit identification. **Verification:** importer rerun idempotent; validator clean (2,797 variants, 9,886 rulings, 20 one-colour categories); 38/38 tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{266–267,270,272,276,278}.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited import cycle, Mushaf pages 327–342:** added **13** source-backed farsh variants (p327=1, p328=1, p329=1, p330=0, p331=0, p332=1, p333=1, p334=1, p335=0, p336=2, p337=1, p338=2, p339=0, p340=1, p341=1, p342=0). Preserved exact source provenance and real token anchors, retained explicit reader-specific faces, and withheld performance-only or unresolved rows. Bare «خلف» remains Q06; Q10 requires explicit identification. **Verification:** importer rerun idempotent; validator clean (2,750 variants, 9,886 rulings, 20 one-colour categories); 38/38 tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{327–329,332–334,336–341}.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited import cycle, Mushaf pages 343–358:** added **10** source-backed farsh variants (p343=0, p344=1, p345=2, p346=2, p347=0, p348=0, p349=0, p350=2, p351=0, p352=0, p353=0, p354=1, p355=1, p356=0, p357=1, p358=0). Preserved exact source provenance and real token anchors, retained explicit reader-specific faces, and withheld performance-only or unresolved rows. Bare «خلف» remains Q06; Q10 requires explicit identification. **Verification:** importer rerun idempotent; validator clean (2,737 variants, 9,886 rulings, 20 one-colour categories); 38/38 tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{344–346,350,354–355,357}.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited import cycle, Mushaf pages 359–374:** added **11** source-backed farsh variants (p359=0, p360=0, p361=0, p362=1, p363=0, p364=0, p365=2, p366=2, p367=1, p368=2, p369=1, p370=0, p371=1, p372=0, p373=1, p374=0). Preserved exact source provenance and real token anchors, retained explicit reader-specific faces, and withheld unresolved multiword or diacritic-only rows. Bare «خلف» remains Q06; Q10 requires explicit identification. **Verification:** importer rerun idempotent; validator clean (2,727 variants, 9,886 rulings, 20 one-colour categories); 38/38 tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{362,365–368,371,373}.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited import cycle, Mushaf pages 375–390:** added **29** source-backed farsh variants (p375=0, p376=0, p377=2, p378=2, p379=2, p380=3, p381=5, p382=5, p383=1, p384=4, p385=2, p386=0, p387=0, p388=1, p389=1, p390=1). Preserved exact source provenance and real token anchors, retained explicit reader-specific faces, and withheld diacritic-only or complex unresolved rows. Bare «خلف» remains Q06; Q10 requires explicit identification. **Verification:** importer rerun idempotent; validator clean (2,716 variants, 9,886 rulings, 20 one-colour categories); 38/38 tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{377–385,388–390}.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited import cycle, Mushaf pages 391–406:** added **12** source-backed farsh variants (p391=0, p392=1, p393=1, p394=2, p395=0, p396=0, p397=0, p398=0, p399=1, p400=3, p401=1, p402=1, p403=0, p404=1, p405=1, p406=0). Preserved exact source provenance and real token anchors, retained explicit reader-specific faces, and withheld unresolved or normalization-colliding performance forms. Bare «خلف» remains Q06; Q10 requires explicit identification. **Verification:** importer rerun idempotent; validator clean (2,687 variants, 9,886 rulings, 20 one-colour categories); 38/38 tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{392–394,399–402,404–405}.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited import cycle, Mushaf pages 407–422:** added **29** source-backed farsh variants (p407=1, p408=2, p409=2, p410=2, p411=1, p412=1, p413=2, p414=1, p415=1, p416=1, p417=1, p418=2, p419=5, p420=2, p421=4, p422=1). Preserved exact source provenance and real token anchors, retained explicit reader-specific faces, and withheld unsafe harakah-only or complex multi-face rows. Bare «خلف» remains Q06; Q10 requires explicit identification. **Verification:** importer rerun idempotent; validator clean (2,675 variants, 9,886 rulings, 20 one-colour categories); 38/38 tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{407–422}.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited import cycle, Mushaf pages 423–438:** added **20** source-backed farsh variants (p423=1, p424=1, p425=2, p426=1, p427=3, p428=0, p429=1, p430=2, p431=1, p432=0, p433=7, p434=0, p435=1, p436=0, p437=0, p438=0). Preserved exact source provenance and real anchors, kept explicit reader-specific faces, and withheld the page-426 vowel-only candidate whose normalized spelling would collide with the base token. Bare «خلف» remains Q06; Q10 requires explicit identification. **Verification:** importer rerun idempotent; validator clean (2,646 variants, 9,886 rulings, 20 one-colour categories); 38/38 tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{423–425,427,429–431,433,435}.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited import cycle, Mushaf pages 439–454:** added **21** source-backed farsh variants (p439=1, p440=5, p441=0, p442=4, p443=0, p444=1, p445=0, p446=1, p447=0, p448=1, p449=0, p450=0, p451=5, p452=0, p453=2, p454=1). Preserved exact source provenance and real anchors, retained reader-specific faces, and left already-covered or unresolved rows unchanged. Bare «خلف» remains Q06; Q10 requires explicit identification. **Verification:** importer rerun idempotent; validator clean (2,626 variants, 9,886 rulings, 20 one-colour categories); 38/38 tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{439–442,444,446,448,451,453–454}.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited import cycle, Mushaf pages 455–470:** added **28** source-backed farsh variants (p455=2, p456=3, p457=1, p458=1, p459=3, p460=2, p461=3, p462=4, p463=0, p464=2, p465=0, p466=0, p467=2, p468=1, p469=4, p470=0) and merged two Abu Amr assignments into the existing page-458 usul locus. Preserved exact source provenance and real anchors; kept distinct reader faces; withheld page-470’s unsafe multiword partition pending a dedicated representation. Bare «خلف» is Q06; Q10 requires explicit identification. **Verification:** importer rerun idempotent; validator clean (2,605 variants, 9,886 rulings, 20 one-colour categories); 38/38 tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{455–464,467–469}.json`, `packages/qiraat-core/fixtures/rulings/page-458.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited import cycle, Mushaf pages 471–486:** added **41** source-backed farsh variants across the four-page slices (p471=4, p472=1, p473=4, p474=1, p475=3, p476=1, p477=1, p478=1, p479=1, p480=0, p481=1, p482=3, p483=7, p484=2, p485=4, p486=7). No new usul rulings were needed. Preserved exact source provenance and real token anchors, kept explicit reader-specific faces, and withheld ambiguous or performance-only candidates without a safe representation. Bare «خلف» remains Q06; Q10 requires explicit identification. **Verification:** importer rerun idempotent; validator clean (2,577 variants, 9,886 rulings, 20 one-colour categories); 38/38 Qiraat tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{471–479,481–486}.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited import cycle, Mushaf pages 487–502:** added **66** source-backed farsh variants across the audited slices (p487=4, p488=4, p489=12, p490=9, p491=0, p492=0, p493=0, p494=0, p495=9, p496=3, p497=3, p498=5, p499=6, p500=2, p501=3, p502=6), plus **3** net ruling records/reader-assignment changes. Preserved exact source IDs/raw text and real-token anchors, retained distinct approved faces for different readers, and withheld ambiguous, generic, negated, contradictory, or unresolved rows. Bare «خلف» is Q06 (خلف عن حمزة); Q10 is used only when explicitly identified. **Verification:** importer rerun idempotent; validator clean (2,536 variants, 9,886 rulings, 20 one-colour categories); 38/38 Qiraat tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{487–490,495–502}.json`, `packages/qiraat-core/fixtures/rulings/page-{487–490,496–497,499,502}.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.

> Newest first. One dated entry per change (bug fix / feature / perf). Include the files touched
> and any required DB migration. This log is the source of truth for "what changed and when".

## 2026-09-22
- **Qiraat — three-agent audited import cycle, Mushaf pages 503–514:** added **57** net source-backed farsh variants across twelve pages (p503=7, p504=11, p505=1, p506=0, p507=3, p508=1, p509=3, p510=7, p511=8, p512=4, p513=7, p514=5). Every addition has a real token anchor, source record/raw text, and independent authority evidence; duplicate faces already present were not re-added. Bare «خلف» remains Q06, with Q10 only when independently named. Ambiguous, contradictory, heading, and Hafs-baseline rows were withheld. **Verification:** importer rerun idempotent; validator clean (2,470 variants, 9,883 rulings, 20 one-colour categories); 38/38 Qiraat tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{503–514}.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — three-agent audited import cycle, Mushaf pages 515–526:** added **66** source-backed farsh variants across twelve pages (p515=10, p516=5, p517=6, p518=3, p519=2, p520=6, p521=5, p522=8, p523=2, p524=7, p525=6, p526=6). Added exact source-row provenance, independent verse authority links, and distinct reader/performance faces; corrected bare «خلف» to Q06 and retained Q10 only when independently named. The importer now handles the compact inline source rows for pages 515–518 and reruns idempotently. Ambiguous, conflicting, heading, or already represented rows were withheld. **Verification:** validator clean (2,412 variants, 9,883 rulings, 20 one-colour categories); 38/38 Qiraat tests; TypeScript and production build passed after a transient first build retry. **Files:** `packages/qiraat-core/fixtures/pages/page-{515–526}.json`, `scripts/qiraat/import_doc_tables.py`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — four-agent audited import cycle, Mushaf pages 531–546:** added 109 source-backed variant records across all 16 pages (p531–534: 18; p535–538: 33; p539–542: 24; p543–546: 34), plus exact source/authority provenance and targeted ruling corrections/merges. Preserved distinct reader-specific and performance faces, corrected reversed source groupings where independent verse apparatus proved them wrong, and enforced the project rule that bare «خلف» is Q06 while Q10 requires explicit apparatus identification. Withheld headings, negated rows, contradictory groups, Hafs-baseline-only rows, and unassigned references. **Verification:** importer rerun idempotent; validator clean (2,347 variants, 9,883 rulings, 20 one-colour categories); 38/38 Qiraat tests; TypeScript and production build passed. **Files:** `packages/qiraat-core/fixtures/pages/page-{531–546}.json`, `packages/qiraat-core/fixtures/rulings/page-{531–546}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited full-family import, Mushaf pages 563–584:** added 138 net source-backed variant records and 16 net ruling records across this 22-page tranche, with exact Mushaf token anchors and source-row provenance. Preserved separate approved faces for different readings, including the verified Ruways form at 72:28; bare «خلف» maps to Q06 (خلف عن حمزة), while Q10 is retained only where the verse apparatus explicitly identifies its narrators. Corrected conflicting existing reader assignments and kept disputed/unanchored source rows out. This is targeted locus coverage, not a claim that every page is complete. **Verification:** importer rerun idempotent; validator clean (2,238 variants, 9,876 rulings, 20 one-colour categories); 38/38 Qiraat tests; TypeScript and production build passed. Deployment/API verification pending. **Files:** `packages/qiraat-core/fixtures/pages/page-{563–584}.json`, `packages/qiraat-core/fixtures/rulings/page-{563–584}.json`, `scripts/qiraat/authorities.py`, `scripts/qiraat/import_surah_tables.py`, `scripts/validate-qiraat-data.mjs`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited full-face import, Mushaf pages 547–562:** added 66 token-anchored variant records and 13 net ruling records across the audited four-page slices; included performance-only variants for the explicit ishmaam/idghaam faces at 63:5 and 66:10. Applied the rule that bare «خلف» denotes Q06-R01, keeping Q10 only where the exact-verse apparatus independently names it or a clear «الباقون» group supports it. Verse-specific Quranpedia checks corrected the extracted reader splits for «مبينات», «تظاهرا», and Shuʿbah’s Jibril form. Removed three wrong legacy assignments (Q07 on Abu Amr idgham, and Q10 attributed only through bare «خلف»); conflicting and unsupported source rows remain withheld, and the touched pages are not claimed complete. **Verification:** additive diff except those explicit attribution corrections; importer preview/write idempotent; Qiraat validator passes (2,100 variants, 9,860 rulings, 20 one-colour categories). Verification passed: Qiraat validator (2,100 variants, 9,860 rulings, 20 one-colour categories), 38/38 Qiraat tests, TypeScript, and production build. Deployment and API smoke are pending. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{547,549–556,558–562}.json`, `packages/qiraat-core/fixtures/rulings/page-{551–558,560}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
## 2026-09-21
- **Qiraat — Khalaf attribution correction:** applied the project rule that bare «خلف» means Khalaf ʿan Hamza (`Q06-R01`), while Q10 requires explicit «خلف العاشر». Corrected the legacy word-face assignments linked to source rows `DOCX-P444-R02501` and `DOCX-P500-R03045`, replacing the two Q10 narrators with `Q06-R01` and retaining all other reader IDs. The processed source records explicitly use bare «خلف» in both cases. **Pages corrected in this piece:** 444 and 500 (two targeted loci; no new page coverage claimed). **Verification:** source-to-fixture attribution audit; Qiraat validator (2,034 variants, 9,847 rulings, 20 one-colour categories); 38/38 Qiraat tests; TypeScript and production build pass. Commit `be50760` is READY on Vercel (`dpl_78QLhcBf2hNkMJdx9CMB1dQQHQdr`); production API smoke on pages 444 and 500 returned Q06-R01 and no Q10 assignments. **Files:** `packages/qiraat-core/fixtures/pages/page-444.json`, `packages/qiraat-core/fixtures/pages/page-500.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — source-backed additions, Mushaf pages 499–530:** imported 25 fixture variant records across 11 pages and added 9 usul loci plus 2 merged loci with 57 reader assignments across 9 pages. Kept independent reader-specific faces at shared tokens; the source does not explicitly identify خلف العاشر at the وَأَمْلَىٰ locus, so that under-specified split remains excluded. These are targeted additions, not page-wide completion. **Verification:** additive fixture audit; validator clean (2,034 variants, 9,847 rulings, 20 one-colour categories); 38/38 Qiraat tests, TypeScript, and production build pass. Commit `876c972` is READY on Vercel (`dpl_55MKdeALDjrCHpqmbr5zCikiwiuw`); API smoke returned new source-backed data on pages 499, 500, 512, and 530. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{499,500,501,502,504,505,506,507,508,509,512}.json`, `packages/qiraat-core/fixtures/rulings/page-{509,511,514,515,519,521,526,528,530}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited source-backed faces and usul additions, Mushaf pages 467–482:** added 13 exact-token farsh variants across pages 477–479 and 481, including distinct reader faces at 41:10, and added three new usul loci plus one merged locus with 39 reader assignments on pages 468, 470, 471, and 481. Bare «خلف» resolves to خلف عن حمزة (Q06-R01); Q10 is assigned only when the source says «خلف العاشر». These are targeted additions; other farsh omissions and legacy attribution conflicts remain in the audited pages, so page-wide completion is not claimed. **Verification:** additive fixture diff; parser rerun writes no changes; validator clean (2,009 variants, 9,838 rulings, 20 one-colour categories); 38/38 Qiraat tests, TypeScript, and production build pass. Commit `fce6e5d` is READY on Vercel (`dpl_8GiJxcv79um3nw5okD1F8wxHj3dv`); production API returned all 13 new variants on pages 477–479 and 481. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{477,478,479,481}.json`, `packages/qiraat-core/fixtures/rulings/page-{468,470,471,481}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited multi-family additions, Mushaf pages 435–450:** added 18 source-linked, exact-token farsh faces on pages 435, 437–438, 443–444, and 447–450. Added 10 usul loci and merged 11 existing loci with 74 reader assignments on pages 435–436, 440, 444, 446, and 448–450. The importer retains independently attributed faces at the same token; no prior variant or ruling reader assignment was removed. Four read-only audits found further omissions and disputed existing assignments, so these are targeted additions and page-wide completion is not claimed. **Verification:** additive fixtures; parser preview/write rerun is idempotent; validator clean (1,996 variants, 9,835 rulings, 20 one-colour categories); 38/38 Qiraat tests, TypeScript, and production build pass. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{435,437,438,443,444,447,448,449,450}.json`, `packages/qiraat-core/fixtures/rulings/page-{435,436,440,444,446,448,449,450}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited targeted additions, Mushaf pages 387–397:** added eight source-backed word faces on pages 387–390 and 396–397, plus eight exact-token usul loci and one merged locus with 54 reader assignments on pages 389–390 and 394–396. The distinct-face importer retains separately sourced readings at the same locus for different readers. These are targeted additions; audits found other omissions and assignment conflicts, so page-wide completion is not claimed. **Verification:** additive fixture changes, parser preview/write idempotent, validator clean (1,978 variants, 9,825 rulings, 20 one-colour categories), 38/38 Qiraat tests, TypeScript, and production build pass. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{387,388,389,390,396,397}.json`, `packages/qiraat-core/fixtures/rulings/page-{389,390,394,395,396}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited usul family additions, Mushaf pages 371–386:** added 19 exact-token ruling loci with 114 reader assignments: eight YAAT_IDAFA loci (57 assignments), six YAAT_ZAWAID loci (12), one HAMZATAN_KALIMATAYN locus (9), two HAMZATAN_KALIMA loci (20), and two WAQF_RASM loci (16). The additions are on pages 371–375, 377, 379–380, 382, 385–386. Preview and rerun are idempotent; existing ruling records were byte-for-byte preserved and the diff contains additions only. These are targeted usul additions; external audits found other missing or disputed farsh/reader coverage in these pages, so page completion is not claimed. **Verification:** validator clean (1,970 variants, 9,817 rulings, 20 one-colour categories); 38/38 Qiraat tests, TypeScript, and production build pass. **Files:** `packages/qiraat-core/fixtures/rulings/page-{371,372,373,374,375,377,379,380,382,385,386}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — source-backed alternate word faces, Mushaf pages 311–326:** added 13 exact-token farsh variants with 35 reader assignments on pages 312, 316, 319–323, 325, and 326. This includes the distinct Hamza face at 19:97, Ibn Kathir’s hamza-transfer forms at 20:2 and 20:114, Yaqub’s hāʾ-dammah forms, and separately sourced forms at 21:47–48. The importer’s face identity preserves a second approved form at a token when its reader set is distinct. Other source rows in this range remain under review; this is targeted-locus coverage, not page-wide completion. **Verification:** 13 source links and exact token anchors; parser rerun is idempotent; fixture changes only add records (page 323 was an empty `[]` file); validator clean (1,970 variants, 9,798 rulings, 20 one-colour categories); 38/38 Qiraat tests, TypeScript, and production build pass. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{312,316,319,320,321,322,323,325,326}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — multi-face import cycle, Mushaf pages 295–310:** reviewed these 16 pages and added 25 token-backed farsh variants on pages 295–302 and 304, including distinct reader faces sharing one token (for example the two non-Hafs forms at al-Kahf 18:59). Changed variant deduplication to preserve vowel marks in face identity, so a second reading form can coexist when assigned to a different reader; same-reader conflicting forms remain withheld. Added five new usul loci and 9 additive ruling merges (57 reader assignments total) across pages 298–310. Several audited candidates remain for later review; this is not page-wide completion. **Verification:** fixture arrays/additions are additive and parser rerun is idempotent; validator clean (1,957 variants, 9,798 rulings, 20 one-colour categories); 38/38 Qiraat tests, TypeScript, and production build pass. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{295,296,297,298,299,300,301,302,304}.json`, `packages/qiraat-core/fixtures/rulings/page-{298,299,305,307,308,309}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited source/usul additions, Mushaf pages 279–294:** the four read-only audits reviewed 16 pages. This batch adds ten exact-token usul ruling loci across pages 279, 280, 285–288, and 291–292 (51 reader assignments including one merged assignment): small idgham at the quoted 16:113, 17:33, 17:41, 17:89, 17:94, 17:97, and 17:101 spans; and explicit hamza-change loci at 16:105, 17:54, and 17:93. No page-wide completeness is claimed; other audited candidates remain for the next import pass. Updated the shared reader resolver so bare “خلف” maps only to Q06-R01, while “خلف العاشر” remains Q10; updated farsh deduplication so a different reader can be added for a distinct form at the same token, while same-reader conflicts and duplicate assignments remain rejected. **Verification:** fixture diff is additive; rerun is idempotent; validator clean (1,932 variants, 9,793 rulings, 20 one-colour categories); 38/38 Qiraat tests, TypeScript, and production build pass. **Files:** `scripts/qiraat/{authorities.py,build_variants.py,data_rulings.py,data_variants.py,import_doc_tables.py,import_surah_tables.py}`, `packages/qiraat-core/fixtures/rulings/page-{279,280,285,286,287,288,291,292}.json`, `CHANGELOG.md`, `CLAUDE.md`, `docs/qiraat-import-progress.json`. **DB migration:** none — fixture-only.
- **Qiraat — additional verified faces, Mushaf pages 277–278:** switched the import parser to consume the supplied processed `qiraat_records.jsonl` as its primary page source, preserving raw source lines and record IDs. Added three token-backed variants: Hamza and Yaqub `عَلَيْهُم` at 16:89; Qalun, Abu Amr, al-Kisai, and Abu Jaafar `وَهْوَ` at 16:97; and Ibn Kathir `ٱلْقُرَانَ` at 16:98. Added four usul loci: two hamza-change rules at 16:89–90, the second `يَشَاءُ` waqf-Hamza occurrence at 16:93 with Hamza and Hisham, and Ibn Kathir’s rasm-waqf form `بَاقِي` at 16:96. Existing same-token forms and reader sets were preserved; ambiguous bare-«خلف» rows were withheld. **Verification:** additive fixture changes; importer idempotent; validator clean (1,932 variants, 9,783 rulings, 20 one-colour categories); 38/38 Qiraat tests, TypeScript, production build, and production API smoke passed. Commit `7950e3ad9f157c6f58554006c88f4fc9fbe2b144` is pushed to `origin/main`; Vercel deployment `dpl_9UkbaSgEhcM1vCWLaYdpwtncBkzS` is READY. Production API returned all three exact forms and the four new rule loci on pages 277–278. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{277,278}.json`, `packages/qiraat-core/fixtures/rulings/page-{277,278}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited source faces, Mushaf pages 275–276:** added eight page-275 variant records (36 reader assignments): three sukun-hāʾ forms at an-Nahl 16:75 and 16:76; Qanbal/Ruways sīn and Khalaf from Hamza’s ishmaam performance face at 16:76; Hamza’s `إِمِّهَاتِكُم` and al-Kisai’s `إِمَّهَاتِكُم` at 16:78; and the tāʾ form `تَرَوْا` for Ibn Amir, Hamza, Yaqub, and Khalaf al-Ashir at 16:79. The ishmaam face is represented as performance-only with the Mushaf base text preserved. Page 276 was audited: its explicit hamz, imalah, and rasm loci were already covered; rows with bare “خلف” or conflicting attribution were withheld. Distinct forms at the shared 16:76 and 16:78 tokens have disjoint reader sets. **Verification:** additive fixtures; importer idempotent; Qiraat validator (1,929 variants, 9,779 rulings, 20 one-colour categories), 38/38 Qiraat tests, TypeScript, production build, and production API smoke passed. Data commit `ef173ed872873245101ccda534b46f4c5594c357` is pushed to `origin/main`; Vercel deployment `dpl_2Rm9ebCatS6uC9LfGt2EyPQzBCP9` is READY. Production debug API returned all eight exact forms/spans/readers on page 275 and the existing reviewed loci on page 276. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-275.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — approved multiple faces and audited usul, Mushaf pages 273–274:** page 273 adds three sukun-hāʾ forms in an-Nahl 16:58, 16:60, and 16:63 (Qalun, both Abu Amr transmissions, both al-Kisai transmissions, and both Abu Jaafar transmissions), plus both independently supported forms at 16:62: `مُفْرِطُونَ` for Nafi and `مُفَرِّطُونَ` for Abu Jaafar. It adds Warsh tarqīq at 16:59 and five explicit TAGHYIR_HAMZ loci from the source rows. Page 274 adds both disjoint forms at 16:66 (`نَسْقِيكُمْ` and `تَسْقِيكُمْ`), Ibn Amir/Shuʿbah `يَعْرُشُونَ` at 16:68, and the Kisai waqf imalah ruling on `لَعِبْرَةً`. Total: eight farsh variant records and seven new usul loci, with 21 reader assignments in those rulings; distinct forms at shared tokens retain disjoint reader sets. Ambiguous and conflicting source rows were withheld. **Verification:** additive-only fixtures; importer rerun idempotent; Qiraat validator (1,921 variants, 9,779 rulings, 20 one-colour categories), 38/38 Qiraat tests, TypeScript, production build, and production API smoke passed. Data commit `b5497e6ea35f79a435d99354292ad7da89469c57` is pushed to `origin/main`; Vercel deployment `dpl_CpTZt5QonG2B4KkxphxEknqvSb6C` is READY. Production API returned the exact faces, token spans, and reader assignments for both pages. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{273,274}.json`, `packages/qiraat-core/fixtures/rulings/page-{273,274}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — source-backed farsh, Mushaf pages 271–272:** added Ibn Amir and al-Kisai’s naṣb form `فَيَكُونَ` at an-Nahl 16:40, plus four separately sourced forms on page 272: Hamza+Yaqub `يُوحَى إِلَيْهُم` at 16:43, Hamza+Yaqub `إِلَيْهُم` at 16:44, Abu Amr+Yaqub `بِهِمِ الْأَرْضَ` at 16:45, and the qasr form `لَرَؤُفٌ` at 16:47. Five targeted loci, 27 reading assignments. Ambiguous-reader rows were excluded. **Verification:** additive-only fixture diffs; importer rerun idempotent; Qiraat validator (1,913 variants, 9,772 rulings, 20 one-colour categories), 38/38 Qiraat tests, TypeScript, and production build passed. Commit `15bb6a0041f77f7e2d4726d66900d2126fa8dab4` is pushed to `origin/main`; Vercel deployment `dpl_A3DvyCbjrZfFM9hPuDdewdYsprvH` is READY. Production API returned the exact new forms and reader IDs on both pages. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{271,272}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — multiple approved farsh forms per word, Mushaf pages 264 and 270:** changed importer deduplication to distinguish a word’s form and reader assignments, allowing separate source-backed variants at the same token while rejecting reader overlaps. Restored Abu Jaafar’s `جُزٌّ` at al-Hijr 15:44 token 7 alongside Shuʿbah’s `جُزُءٌ`; added Yaqub’s `يُخْزِيهُمُ` and `فِيهُمُ` at an-Nahl 16:27, and the Hamza/Khalaf al-Ashir `يَتَوَفَّاهُمُ` at 16:28 and 16:32. Five targeted loci, 16 reader assignments. Ambiguous `خلف` in a neighboring source row remains excluded. **Verification:** additive fixture diffs, parser idempotent, Qiraat validator (1,908 variants, 9,772 rulings, 20 one-colour categories), 38/38 Qiraat tests, TypeScript, and production build passed. Commit `b6b724f4d8642725029001bf03bfb15ed3b5cf1c` is pushed to `origin/main`; Vercel deployment `dpl_41eUe3M43gsJh5R7xibvksUXAHxs` is READY. Production API returned both 15:44 forms with disjoint assignments at page 264 and all four new forms at page 270. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{264,270}.json`, `packages/qiraat-core/engine.test.mjs`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited farsh, Mushaf pages 267–268:** added `تُشْرِكُونَ` for Hamza, al-Kisai, and Khalaf al-Ashir at an-Nahl 16:1 token 9 and 16:3 token 7, plus Abu Jaafar `بِشَقِّ` at 16:7 token 9. This is three targeted word loci and 14 reader assignments. Processed package rows `DOCX-P267-R00639/R00640` and `DOCX-P268-R00649` match Quranpedia’s ten-reading displays and exact Mushaf tokens. The competing variants at 16:2 and rows with ambiguous readers were withheld. **Verification:** additive-only fixtures; parser idempotent and package-row checked; Qiraat validator (1,903 variants, 9,772 rulings, 20 one-colour categories), 37/37 Qiraat tests, TypeScript, and production build passed. Commit `ae09030` is pushed to `origin/main`; Vercel deployment `dpl_8go3tJzKYtRJEbiS3a7DxgN5hbB9` is READY. Production API returned the exact three loci and reader IDs. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{267,268}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited farsh, Mushaf page 264:** added Yaqub’s `عَلِيٌّ` at al-Hijr 15:41 token 4 for Ruways and Rawh, and the Hamza+Yaqub dammah-hāʾ form `عَلَيْهُمْ` at 15:42 token 5 (two word loci, six reader assignments). The processed rows `DOCX-P264-R00591–593` were matched to exact page tokens and Quranpedia’s ten-reading records; the 15:41 الصراط forms are already represented separately. Abu Jaafar’s `جُزٌّ` at 15:44 was withheld because this importer deduplicates to one variant per token and another form already occupies that token. **Verification:** additive-only fixture diff; parser rerun idempotent and package-row checked; Qiraat validator (1,900 variants, 9,772 rulings, 20 one-colour categories), 37/37 Qiraat tests, TypeScript, and production build passed. Commit `427ca7da3cb89f96c002a9ef3b5a0f44feda394c` is pushed to `origin/main`; Vercel deployment `dpl_C5oN3nicQCk182x48JAaVEUupktV` is READY. Production API returned both forms at their exact tokens with the expected readers. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-264.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited farsh, Mushaf pages 261–262:** added three source-backed harakah forms: the kasrah-on-sin `تَحْسِبَنَّ` at Ibrahim 14:47 token 2 for the twelve remaining explicitly identified readings (Hafs is independently verified in the fatha group), and dammah-on-hāʾ forms for Yaqub at al-Hijr 15:11 token 2 and Hamza+Yaqub at 15:14 token 3. These are three targeted word loci (18 reading assignments), not page-wide completeness claims. Processed package rows `DOCX-P261-R00551`, `DOCX-P262-R00571`, and `R00572` were checked against exact page tokens and independent ten-reading records. Conflicting and unresolved neighboring rows were withheld. **Verification:** additive-only fixture diff; parser rerun idempotent and package-row checked; Qiraat validator (1,898 variants, 9,772 rulings, 20 one-colour categories), 37/37 Qiraat tests, TypeScript, and production build passed. Commit `9cc52d7b78423c7df3b91723a60f494dec7b01b4` is pushed to `origin/main`; Vercel deployment `dpl_2atHmhN8dRojY95X4bMCzpopkVog` is READY. Production API returned the exact three forms, tokens, and reader IDs. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{261,262}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited farsh, Mushaf page 253:** added Ibn Kathir’s نقل حركة الهمزة إلى الراء وحذف الهمزة form `قُرَانًا` at ar-Ra'd 13:31, token 3 (`قُرْءَانًۭا` in the Hafs page fixture), for Q02-R01/R02. Source record `DOCX-P253-R00461` was independently checked against *القمر المنير في قراءة الإمام المكي عبد الله بن كثير*. This is one newly covered word, not a page-wide completeness claim. **Verification:** additive-only fixture diff (one variant, two readings); parser rerun idempotent; Qiraat validator (1,895 variants, 9,772 rulings, 20 one-colour categories), 37/37 Qiraat tests, TypeScript, and production build passed. Commit `787a0bd8c93de9956ec3d02d38011ab255718ee2` is pushed to `origin/main`; Vercel deployment `dpl_C7uBiMNiAB1i12Ye5osgm7DMChtQ` is READY. Production API returned the exact token, base/form, and both Ibn Kathir readings. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-253.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — al-Susi imalah attribution, Mushaf page 254:** added Q03-R02 for `ٱلْكَـٰفِرِينَ` at ar-Ra'd 13:35 to the existing imalah ruling. The processed package row `DOCX-P254-R00480` names Abu Amr; an independent ten-readings display confirms Abu Amr, Ruways, and al-Duri from al-Kisai have imalah. Added only the missing al-Susi assignment; left the conflicting Warsh status unchanged. **Verification:** additive-only fixture change (one reading and attribution); parser idempotent; Qiraat validator (1,894 variants, 9,772 rulings, 20 one-colour categories), 37/37 Qiraat tests, TypeScript, and production build passed. Commit `c1222c7ac422c522ecf616d5c9f0c549fd2601d0` is pushed to `origin/main`; Vercel deployment `dpl_9GtCtjLZkxcQBdNFiP166pccybhC` is READY. Production API returned the exact 13:35 token-18 imalah ruling with Q03-R02. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-254.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited farsh, Mushaf page 252:** added the explicit ḍammah form `عَلَيْهُم` for Hamza and Yaqub at ar-Ra'd 13:23, token 12. The processed import package's `DOCX-P252-R00451` row matches the raw wording; exact base token and attribution were independently checked. Neighboring page 253 suggestions with ambiguous/conflicting attribution were withheld. **Verification:** additive-only fixture diff; Qiraat validator (1,894 variants, 9,772 rulings, 20 one-colour categories), 37/37 Qiraat tests, TypeScript, and production build passed. Commit `50dd817af3a9ad4a9103b71f241df34db87c0239` is pushed to `origin/main`; Vercel deployment `dpl_5HGKcNtG6Kq9pCzNeVETGgUEZxv2` is READY. Production API returned the exact 13:23 token-12 form and all four expected reader IDs. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-252.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited farsh, Mushaf pages 250–251:** added the explicit Abu Amr and Yaqub kasrah forms at Ra'd 13:6 (`قَبْلِهِمِ`) and 13:18 (`لِرَبِّهِمِ`), and added al-Susi to the existing 13:24 al-Kāfirīn imalah ruling. This covers one newly represented word on each page and nine reader assignments across three words. Clauses with conflicting existing faces or bare خلف were withheld. **Verification:** attached raw records and source paragraphs checked against exact Mushaf tokens and an independent Qiraat reference; additive diff; parser idempotent; Qiraat validator, 37/37 Qiraat tests, all seven Mushaf validators, TypeScript, and production build passed. Commit `91d79543d766b2985205a680ffc4f25617e29754` is pushed to `origin/main`; Vercel deployment `dpl_Hb3XfwDiXJiWqubHrLijm5X1HjYc` is READY. Stable production API returned both variants at their exact tokens and all expected reader IDs, plus the al-Susi imalah assignment. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{250,251}.json`, `packages/qiraat-core/fixtures/rulings/page-251.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited farsh, Mushaf pages 248–249:** page 248 adds three token-backed forms at Yusuf 12:105 and 12:109 (including both explicitly attributed spellings at the single 12:105 token); page 249 adds Yaqub's two reader assignments to the existing tashdid form at Ra'd 13:3. This changes 16 reader assignments across four words. Ambiguous or conflicting page-249 usul excerpts and the route-sensitive 13:3 vowel form were withheld. **Verification:** raw source paragraphs cross-checked against al-Nashr/independent reading references and exact Mushaf tokens; additive diff; parser idempotent; Qiraat validator, 37/37 Qiraat tests, all seven Mushaf validators, TypeScript, and production build passed. Commit `6a80a2d6b316047bf10e1ef2279c0feca60af6a6` is pushed to `origin/main`; Vercel deployment `dpl_4rXFLtcj8GWuxLyX2ZwA3bT377GY` is READY. Stable production API smoke returned all four expected forms/assignments at the exact tokens. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{248,249}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Qiraat — audited farsh on Mushaf page 456:** added عَبْدَنَا for Ibn Kathir at Sad 38:45 and وَأُخَرُ for Abu Amr and Yaqub at 38:58. The attached import package's raw record text was used to locate both rows; its confidence flags were not trusted where the parser omitted a bare خلف on a different row. That 38:53 row remains excluded. **Verification:** both exact token anchors and reading groups checked against al-Nashr; additive fixture diff with two variants/eight assignments; parser idempotent; Qiraat validation, 37/37 tests, all seven Mushaf validators, TypeScript, and production build passed. Commit 0dee984a68748ccc7ad329cba3ec3da1b855ac8d is pushed to origin/main; Vercel deployment dpl_5Vy4nvd8VCpQQrV5P8a6noyc8gx7 is READY. The stable production debug API returned both exact variants and assignments. **Files:** scripts/qiraat/import_doc_tables.py, packages/qiraat-core/fixtures/pages/page-456.json, docs/qiraat-import-progress.json, CHANGELOG.md, CLAUDE.md. **DB migration:** none — fixture-only.
- **Qiraat — audited farsh on Mushaf page 247:** added the explicit opening-T reading at Yusuf 12:100 (Ibn ʿĀmir and Abū Jaʿfar) and the ḍammah reading at 12:102 (Ḥamza and Yaʿqūb), each anchored to its real page token and backed by an independent reading reference. **Verification:** additive fixture update; parser rerun idempotent; Qiraat data validation, 37/37 Qiraat tests, all seven Mushaf validators, TypeScript, and production build passed. Commit `196bb76e845d9e840215c9986fcca2a29669ba40` is pushed to `origin/main` and Vercel deployment `dpl_852vcdDyPovt62jdo4iYJ3z5eKT8` is READY. Production debug API returned both exact variants at token 9 with all eight reader assignments. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-247.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Fix — Qiraat Usul taxonomy hierarchy:** added additive, idempotent master taxonomy groups and child rules beneath the existing `USUL` root, exposed taxonomy metadata through `qiraat_editor_catalog()`, and made the editor render real parent/child breadcrumbs instead of `الأصول › الأصول`. Face presets now come from leaf metadata and remain hidden until a compatible rule is selected. No Reader applicability was invented; authority assignments remain manual. **Verification:** live self-hosted Postgres hierarchy query, editor API catalog check, real browser Usul/Madd/Idgham/Sakt flow, Node 22 typecheck/build, and Qiraat validation. **DB migrations:** `20260921170000_qiraat_taxonomy_hierarchy.sql`, `20260921170100_qiraat_taxonomy_catalog_metadata.sql` applied transactionally.
- **UX — Multi-select Reader authorities:** the compact Qiraat editor now supports selecting multiple Reader/Narrator authorities at once, shows removable colored authority chips, and expands the selected set into explicit entity-to-rule assignments through the existing write/OCC/cache pipeline. **Verification:** Node 22 typecheck, diff check, and browser smoke selecting two Readers with no page/console errors.
- **UX — Compact inline Qiraat editor workspace:** redesigned the Phase 5 editor as a desktop split pane with independently scrollable Mushaf/editor panes, selected-word focus outline, compact direct-click authority/taxonomy/Face controls, collapsed advanced metadata, editor-local sticky actions, and a responsive mobile sheet fallback. Existing authenticated write/OCC/revision/cache paths remain unchanged. **Verification:** Playwright desktop/mobile smoke passed with one visible editor, 200 editor load, successful draft create/soft-delete cleanup, no page/console errors, stable selected-word position, and Node 22 typecheck. **Files:** `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, `src/app/mushaf-1441/_components/qiraat/QiraatEditor.tsx`, `docs/qiraat/15-compact-editor-ux.md`.
- **Fix — Phase 5 mobile Qiraat editor acceptance:** corrected Mushaf word hit-testing and mobile touch routing. Current page-slot words now remain the interactive target above the printed text layer; Qiraat taps bypass the notes long-press handler, and the editor ignores the synthetic click that follows a touch-open so the bottom sheet stays visible. **Verification:** real Playwright touch smoke opened `002:001:001`, received the authenticated editor response (200), and rendered the editor with no browser errors.
- **Feature — Qiraat Phase 6 resolved indicators:** added the bulk `/api/mushaf-1441/qiraat-resolved?page=N` read path and page-level request deduplication. Manual annotations now receive compact face-count/authority-colour indicators from `resolved_qiraat_cache` without recursive hierarchy work or per-word requests; editor mutations invalidate and refresh the affected page projection.
- **Feature — Qiraat Phase 6 scope selection:** added semantic `RANGE` and `BOUNDARY` selection controls to the Qiraat rail. Two clicked Mushaf words are carried into the editor as canonical start/end anchors, including cross-ayah-capable keys, while the existing page-level Qiraat marker and review display remain unchanged and canonical Quran text stays immutable. **Verification:** desktop Playwright scope smoke passed with the responsive editor opening on the selected anchors and no page/console errors; Node 22 typecheck passed. **Documentation:** `docs/qiraat/14-scope-indicators-review.md`.
- **Feature — Qiraat annotation-engine Phase 5 visual editor:** added authenticated catalog/load/create/update/soft-delete RPCs and the responsive Mushaf word editor with explicit authority-to-rule assignments, structured Faces/Variants, source links, colors, status, advanced inheritance/context controls, OCC conflict handling, keyboard shortcuts, and canonical Save & Next/Previous navigation. The editor uses the existing normalized annotation and resolved-cache pipeline and never changes canonical Quran text. **Verification:** repeated editor GET requests remained 200 after the local PostgREST schema cache warm-up; browser smoke covered word click, canonical identity, create, edit, multi-assignment, soft delete, Save & Next, reload persistence, and zero page/console errors; Phase 2/3/4 Qiraat regressions and Node 22 typecheck/build passed. **DB migrations:** `20260921160000_qiraat_editor_api.sql`, `20260921160100_qiraat_cache_scope_rebuild_safety.sql` applied locally after the existing Phase 4 backup.
- **Feature — Qiraat annotation-engine Phase 4 reversible aggregate rollback:** added additive batch ledger/change tables and transactional `rollback_qiraat_batch(uuid)`. Rollback soft-deletes inserted annotations and restores UPDATE/DELETE aggregates (core fields, Faces, annotation-level/face-linked Variants, and source links) without deleting protected revision history. Versions remain monotonic and append revisions; stale post-batch edits raise `ROLLBACK_CONFLICT` atomically; a second rollback is a safe no-op. Cache projection now clears an annotation's former scope/authority rows before rebuilding its new affected projection. **Verification:** `npm run test:qiraat:batch` passed its isolated mixed INSERT/UPDATE/DELETE matrix with child restoration, cache/branch isolation, conflict atomicity, double rollback, soft-delete replacement and zero synthetic residue. **DB migrations:** `20260921150000_qiraat_batch_engine.sql`, `20260921150100_qiraat_cache_update_safety.sql` applied locally after the existing Phase 4 backup.
- **Feature — Qiraat annotation-engine Phase 3 resolver/cache:** added the page-indexed `resolved_qiraat_cache` and write-time closure-based resolver. Propagation is explicit for INHERIT/OVERRIDE/EXCLUDE; direct rules outrank inherited state and nearest eligible ancestors win. Added rollback-only resolver/cache matrix coverage for propagation, exclusions, Tariq precedence, branch/sibling isolation and deletion fallback. **Verification:** resolver suite passed with the intentional final rollback and confirmed zero synthetic authorities, annotations or cache rows remain. **DB migration:** `20260921140000_qiraat_annotation_resolution.sql` applied locally after backup.
- **Feature — Qiraat annotation-engine Phase 2 write model:** added additive normalized annotations with immutable dual canonical anchors, WORD/RANGE/BOUNDARY scopes, BOTH/WASL_ONLY/WAQF_ONLY context, status, entry origin, inheritance action, entity/taxonomy/framework links and local colour override. Added structured faces, overlay-only variants, multi-source citations, versioned revision history and server-side anchor/order validation. **Safety:** fresh backup `pre-qiraat-annotation-write-model-20260921.dump` was verified before apply; validation scenarios inserted then removed their test rows. **Verification:** migration applied; database scenarios passed for WORD, three faces, boundary, range, OCC stale-version rejection, and revision records. **DB migration:** applied locally after approval and backup.
- **Feature — Qiraat annotation-engine Phase 1 master schema:** added the additive `quran_words` semantic identity registry (`surah`, `ayah`, `word_position`, deterministic canonical key and current render-ID mapping); seeded all 77,429 real Mushaf fixture words without changing their text. Added corpus/framework catalogues, Reader/Narrator/Tariq closure support over the existing `qiraat_authorities` tree, framework-authority membership, database-driven taxonomy roots, empty verified-only groups, authority default colours, and extensible source-document metadata. Added the idempotent canonical-word validation/seeding script and master-data operations guide. **Safety:** backup `pre-qiraat-annotation-master-20260921.dump` was created and catalog-verified before DDL; migration applied and reapplied in one transaction; no Quran fixture, glyph, legacy entry or unverified scholarly group/framework membership changed. **Files:** `supabase/migrations/20260921120000_qiraat_annotation_master.sql`, `scripts/qiraat/seed-canonical-words.ts`, `package.json`, `docs/qiraat/11-annotation-master-data.md`, `CHANGELOG.md`, `CLAUDE.md`. **Verification:** migration reapply passed; canonical count/key/render-ID checks passed; closure/framework/colour/no-unverified-group queries passed; `npm run test:qiraat` (37/37), `npm run qiraat:canonical-words:validate`, `npm run mushaf:validate`, `npm run typecheck`, Node 22 production build with `NODE_OPTIONS=--max-old-space-size=3072`, and `git diff --check` passed. **DB migration:** applied locally after explicit approval and backup.
- **Docs — Qiraat annotation-engine Phase 0 audit:** documented the actual Mushaf rendering, fixture/QCF identity boundary, current API and deployment seams, and the observed self-hosted PostgreSQL Qiraat V2 state. Added the additive master/write/resolver/batch data model, phased implementation and migration plan, plus an explicit scholarly seed-data gap register. The audit corrects the stale claim that the V2 schema is unapplied: it is present locally, so future work must extend rather than reapply or replace it. **Files:** `AUDIT_AND_MIGRATION_MAP.md`, `QIRAAT_ENGINE_DATA_MODEL.md`, `PHASE_IMPLEMENTATION_PLAN.md`, `SEED_DATA_GAPS.md`, `CHANGELOG.md`, `CLAUDE.md`. **Verification:** read-only repository/database inspection; `git diff --check`; no functional code, fixture, schema or Quran-text mutation; no deployment. **DB migration:** none.
- **الهمزتان من كلمة — page 246:** added the explicitly named reader faces at Yusuf 12:90, anchored to tokens 2–3 (`أَءِنَّكَ لَأَنتَ`): Q01-R01 and both Q03 narrations with تسهيل مع الإدخال; Q01-R02 and Ruways without إدخال; Hisham with تحقيق; Ibn Kathir and Abu Jaafar with one hamza. The generic الباقون clause is not materialized. Quranpedia’s [ten-reading display for 12:90](https://quranpedia.net/ayahs/12/90/4) independently lists the insertion, non-insertion, Hisham, and one-hamza faces. Also restored the Mushaf page selection control's 44px minimum touch target after the integrated Phase 6 validator caught its smaller class. **Page covered in this piece:** 246 (one HAMZATAN_KALIMA ruling, 10 reader assignments). **Verification:** additive fixture change only; exact Hafs baseText and token span; parser rerun is idempotent; Qiraat validator clean (1,884 variants, 9,772 rulings, 20 one-colour categories); 37/37 Qiraat tests; all seven Mushaf validators; TypeScript; production build passed. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-246.json`, `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **Production:** data commit `1f764b9966255ab205bba70e493c7f29122a7038` is included in follow-up commit `25b66724dd9cf0e3fe2ec945e5b57baf39260b9d`, deployed READY as `dpl_Fw9jYQfa4cH3iDXXsPBwGiiXvJwq`. The public production API confirmed exact tokens 2–3, Hafs baseText, #DC2626, the 10 reader IDs/actions, and source notes. **DB migration:** none — fixture-only.
- **Source-reconciled فرش variants — pages 229 and 260:** page 229’s DOCX tanween group matches the existing 11:68 variant on token 7, so it was source-linked to that record instead of adding a duplicate two-token span. Page 260 adds one Hisham (Q04-R01) alternate at 14:37, token 17. The DOCX line is preserved verbatim in `sourceText`; it prints `أَفْيِدَةً` while describing a yā after the hamza. The independent `البدور الزاهرة` entry confirms `أَفْئِيدَةً` as Hisham’s second face, so that corroborated form is stored separately. **Pages newly covered in this piece:** 260; page 229’s existing coverage was confirmed and linked to the DOCX. **Verification:** one new exact-token variant / one reader assignment, one DOCX citation added to p229, two source citations on p260; no deletions; parser rerun idempotent; Qiraat validator clean (1,884 variants, 9,771 rulings, 20 one-colour categories); 37/37 Qiraat tests; all seven Mushaf validators; TypeScript; production build passed. Reference: [البدور الزاهرة، سورة إبراهيم 14:37](https://www.islamweb.net/ar/library/content/229/120/). **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-229.json`, `packages/qiraat-core/fixtures/pages/page-260.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **Production:** data commit `6e96747d5ac82eabc02327383f811e22792e1f8d` was pushed to `origin/main`; Vercel deployment `dpl_85QCsHCohjH1pZfZiKDqZ2YgJxKf` reached READY for that exact SHA at [the production deployment](https://mutshabehat-v2-5kzdkyea8-shazlka-s-projects.vercel.app). The production debug API confirmed p229’s token-7 source-linked variant and p260’s exact token 17, Hafs base, corrected alternate, and Hisham reading ID; the public Mushaf page 260 showed the Hisham marker. **DB migration:** none — fixture-only.
- **Externally audited inline فرش variants — pages 410, 411, 412, 422, 572, 573, 576, 580, 584:** added 11 exact-token variants (34 reader assignments) from explicit named clauses with source remainder/default clauses. The loci are 30:60 (Ruways), 31:7 (Nafi), 31:16 (Nafi and Abu Ja’far), 33:33 (Bazzi), 72:5 (Ya’qub), 72:19 (Hisham in his second face), 72:28 (Ruways), 74:30 (Abu Ja’far), 77:6 (Rawh and the six explicitly named readers for the second word), and 79:18 (Nafi, Ibn Kathir, Abu Ja’far, Ya’qub). The curated importer checks each original source fragment, reader group, and real Mushaf token anchor. Ten-reading views were checked for [30:60](https://quranpedia.net/embed?surah=30&ayah=60&type=qiraat), [31:7](https://quranpedia.net/embed?surah=31&ayah=7&type=qiraat), [31:16](https://quranpedia.net/embed?surah=31&ayah=16&type=qiraat), [33:33](https://quranpedia.net/embed?surah=33&ayah=33&type=qiraat), [72:5](https://quranpedia.net/embed?surah=72&ayah=5&type=qiraat), [72:19](https://quranpedia.net/embed?surah=72&ayah=19&type=qiraat), [72:28](https://quranpedia.net/embed?surah=72&ayah=28&type=qiraat), [74:30](https://quranpedia.net/embed?surah=74&ayah=30&type=qiraat), [77:6](https://quranpedia.net/embed?surah=77&ayah=6&type=qiraat), and [79:18](https://quranpedia.net/embed?surah=79&ayah=18&type=qiraat). **Pages covered in this piece:** 410, 411, 412, 422, 572, 573, 576, 580, 584 (targeted farsh loci only). **Verification:** 11 appended variants; no existing fixture records changed; reruns are idempotent; Qiraat validator clean (1,883 variants, 9,771 rulings, 20 one-colour categories); 37/37 Qiraat tests; all seven Mushaf validators; TypeScript; production build passed. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{410,411,412,422,572,573,576,580,584}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`.  **Production:** data commit `53741661c087e92fa6eb6dd8c565d82de20ac47c` was pushed to `origin/main`; Vercel deployment `dpl_8oUxjoZ8dRCa9rgb813f9AhmUokq` reached READY for that exact SHA at [https://mutshabehat-v2-di0qxtr1a-shazlka-s-projects.vercel.app](https://mutshabehat-v2-di0qxtr1a-shazlka-s-projects.vercel.app). The public production debug API returned all eleven exact token spans, forms, and reader IDs across the nine pages; the rendered public Mushaf page 410 showed the Ruways marker at 30:60. **DB migration:** none — fixture-only.
- **Inline فرش reader alternatives — pages 268, 274, 287, 296–298:** added six token-backed variants from one-line default/remainder plus explicitly named alternate rows: 16:11 (Shu’ba), 16:71 (Shu’ba and Ruways), 17:55 (both Nafi narrations), 18:26 and 18:28 (both Ibn Amir narrations at each locus), and 18:36 (Nafi, Ibn Kathir, Ibn Amir, and Abu Ja’far). The parser now accepts only this audited two-clause shape for the listed source fragments, checks the reader group and 20-reading remainder, and rejects negated/universal lines or non-token anchors. Quranpedia’s ten-reading views were checked for [16:11](https://quranpedia.net/tafsir/an-nahl/11), [16:71](https://quranpedia.net/tafsir/an-nahl/71), [17:55](https://quranpedia.net/tafsir/al-isra/55), [18:26](https://quranpedia.net/tafsir/al-kahf/26), [18:28](https://quranpedia.net/tafsir/al-kahf/28), and [18:36](https://quranpedia.net/tafsir/al-kahf/36). **Pages covered in this piece:** 268, 274, 287, 296, 297, 298 (targeted farsh loci only). **Verification:** six formerly empty fixture arrays now contain one appended variant each (17 reader assignments total); no existing fixture records changed; reruns are idempotent; Qiraat validator clean (1,872 variants, 9,771 rulings, 20 one-colour categories); 37/37 Qiraat tests; all seven Mushaf validators; TypeScript; production build passed. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/pages/page-{268,274,287,296,297,298}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`.  **Production:** data commit `1ba7e18d2a19bbe6357bcb046ea083a6435ff44d` was pushed to `origin/main`; Vercel deployment `dpl_AWyb6u8yWX7dWQdQeTL5usRh9WGZ` reached READY for that exact SHA. The six loci were also confirmed by the production debug API after the following release. **DB migration:** none — fixture-only.
- **ياءات الإضافة / ياءات الزوائد — pages 494, 563:** added the explicit YAAT_IDAFA ruling at page 494, 43:68 (10 named reader assignments) and the two YAAT_ZAWAID loci at page 563, 67:17–18 (six assignments). Fixed YAAT clause splitting so punctuation inside quoted forms does not split an anchor’s resulting form; scoped «مع الفتح وصلاً» to Shu’ba alone. The quoted page-563 forms and reader groups were checked against Quranpedia’s [67:17](https://quranpedia.net/tafsir/al-mulk/17), [67:18](https://quranpedia.net/tafsir/al-mulk/18), and [surah qiraat listing](https://quranpedia.net/surah/1/67/book/557); the page-494 group was checked against [43:68](https://quranpedia.net/tafsir/az-zukhruf/68). **Pages covered in this piece:** 494 and 563 (YAAT_IDAFA / YAAT_ZAWAID loci only). **Verification:** three token-backed ruling objects appended with exact baseText; both fixture diffs preserve all old records and add only; reruns are idempotent; Qiraat validator clean (1,866 variants, 9,771 rulings, 20 one-colour categories); 37/37 Qiraat tests; all seven Mushaf validators; TypeScript; production build passed. Category-wide conflicts elsewhere were withheld under the additive same-reader guard. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-{494,563}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **Production:** data commit `583d2221b4ac3ff4282653325a97c69c1c84e95b` was pushed to `origin/main`; Vercel deployment `dpl_5uu9hVF6EJ6FDB3ZTPnKXzByduhB` reached READY for that exact SHA at [https://mutshabehat-v2-awm6kellm-shazlka-s-projects.vercel.app](https://mutshabehat-v2-awm6kellm-shazlka-s-projects.vercel.app). Production API smoke could not be completed: deployment API fetches redirected to Vercel SSO in the available client. **DB migration:** none — fixture-only.
- **الهمزتان من كلمتين / من كلمة imported — pages 265, 329, 481:** extended the DOCX parser to inherit anchors from unbraced continuation rows, strip the explicit qualifier «لمن همز», and scope the p265 parenthetical face note only to its quoted anchor. Added four token-backed rulings (42 reader assignments): p265 at 15:61 and 15:67, p329 at 21:89, and p481 at 41:44. Quranpedia’s ten-reading views for [15:61](https://quranpedia.net/tafsir/al-hijr/61), [21:89](https://quranpedia.net/tafsir/al-anbiya/89), and [41:44](https://quranpedia.net/tafsir/fussilat/44) were checked alongside the versioned qiraat dump. The p481 Warsh actions conflict under the parser’s same-face guard and were dropped; its line naming an ambiguous bare «خلف» was also dropped. The generic “على القواعد المذكورة سابقاً” cases remain excluded. **Pages covered in this piece:** 265, 329, 481 (hamzatan categories only; not a claim that every family on each page is complete). **Verification:** all four rulings use exact page-token spans and verbatim baseText; fixture diff adds records only; rerunning both category imports is idempotent; Qiraat validator clean (1,866 variants, 9,768 rulings, 20 one-colour categories); 37/37 Qiraat tests; all seven Mushaf validators; TypeScript; production build passed. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-{265,329,481}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **Production:** data commit `2badb04bd6ae4714693f552290f30d7a37ae1717` was pushed to `origin/main`; Vercel deployment `dpl_7zyGge4TzBP6f6Zq7QntdFkeASg6` reached READY for that exact SHA at [https://mutshabehat-v2-9gugcsxdd-shazlka-s-projects.vercel.app](https://mutshabehat-v2-9gugcsxdd-shazlka-s-projects.vercel.app). Production API smoke returned all four exact token spans, categories, colours, notes, and reader counts (9, 9, 13, 11). **DB migration:** none — fixture-only.
- **External source correction — الإدغام الصغير, page 253:** the attached source explicitly assigns الإدغام to all readers except Ibn Kathir, Hafs, and Ruways at Ar-Ra'd 13:32 ﴿أَخَذتُّهُمْ﴾. Quranpedia’s [ten-reading display for the same ayah](https://quranpedia.net/tafsir/ar-rad/32) corroborates the remaining 16 narrations, including Abu Jaafar’s two narrations with صلة الميم; the [versioned Quranpedia qiraat dump](https://api.quranpedia.net/dumps?lang=en) was also checked (file version 2026-09-19, SHA256 `d4d4ef4fefa54f2fe9db0a377cf2d15744effe39c6436f0c9509dd21ea6f2a5f`). Added the 13 missing reader assignments to the existing token-backed ruling at word 10, base text `أَخَذْتُهُمْ ۖ`. The importer now handles the explicit action-prefixed all-except phrasing and anchored continuation rows. The inherited extra Hafs assignment at page 232 is preserved under the additive-only constraint; the universal page 581 line remains excluded. **Pages covered in this piece:** 253 (13 reader assignments at 13:32; no new ruling locus). **Verification:** one existing ruling grew by 13 attribution and 13 reading entries; no fixture deletions or other fixture changes; parser rerun is idempotent; Qiraat validator clean (1,866 variants, 9,764 rulings, 20 one-colour categories); 37/37 Qiraat tests; all seven Mushaf validators; TypeScript; production build passed. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-253.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **Production:** data commit `2a030a8750eee52f8534564cc6d64db5d45693a4` was pushed to `origin/main`; Vercel deployment `dpl_GBfGja1XD4Qs72g7wKGpc62L6U1R` reached READY for that exact SHA at [https://mutshabehat-v2-9uwebydbd-shazlka-s-projects.vercel.app](https://mutshabehat-v2-9uwebydbd-shazlka-s-projects.vercel.app). Production debug API smoke returned the exact page-253 token span, all 16 reader IDs, and `#15803D`. **DB migration:** none — fixture-only.
- **External rule audit complete — إخفاء أبي جعفر, pages 500–584:** the ghayn/kha rule in [*al-Nashr*](https://www.islamweb.net/amp/ar/library/content/70/157/%D8%AA%D9%86%D8%A8%D9%8A%D9%87%D8%A7%D8%AA) and [*Sharh Tayyibat al-Nashr*, p. 113](https://ablibrary.net/book_content/b/8368/113) adds the final 33 loci (66 Abu Jaafar narration assignments) on pages 505, 508, 510, 517–519, 522, 525, 530, 533–534, 542, 544, 546, 548, 551, 556, 560, 563–564, 566, 568, 573, 575, 579, 583, and 584. Across attached pages 225–584, the sweep covers all 134 eligible token contexts: 16 already present and 118 added in four slices. The three named exception verses are excluded. **Verification:** 33 appended ruling objects across 27 fixtures, no fixture deletions, old objects unchanged; importer idempotent; all 134 contexts matched; Qiraat validator clean (1,866 variants, 9,764 rulings, 20 one-colour categories); 37/37 Qiraat tests; all seven Mushaf validators; TypeScript; production build passed. **Files:** `packages/qiraat-core/fixtures/rulings/page-{505,508,510,517,518,519,522,525,530,533,534,542,544,546,548,551,556,560,563,564,566,568,573,575,579,583,584}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **Production:** data commit `8ddf9a2830c3ffb6a4ccd0457344e9935553e008` was pushed to `origin/main`; Vercel deployment `dpl_HxGyUmA9rT9QvAyBaCtkau3oweo5` reached READY for that exact SHA at [https://mutshabehat-v2-ou17bbeii-shazlka-s-projects.vercel.app](https://mutshabehat-v2-ou17bbeii-shazlka-s-projects.vercel.app). Production debug API smoke passed for all 33 final loci with exact spans, `#0891B2`, and both Abu Jaafar narrations. Across pages 225–584, all 134 rule contexts are covered (16 pre-existing, 118 additions), all 118 additions API-verified. **DB migration:** none — fixture-only. External completeness review of the other Qiraat families continues.
- **External rule audit — إخفاء أبي جعفر, pages 400–499:** the sourced ghayn/kha rule from [*al-Nashr*](https://www.islamweb.net/amp/ar/library/content/70/157/%D8%AA%D9%86%D8%A8%D9%8A%D9%87%D8%A7%D8%AA) and [*Sharh Tayyibat al-Nashr*, p. 113](https://ablibrary.net/book_content/b/8368/113) adds 37 loci (74 Abu Jaafar narration assignments) on pages 401, 403, 406, 409, 412–415, 419, 422, 425, 427, 430, 434, 437–440, 446, 461–462, 473, 477–478, 480–482, 488–489, 493, and 495. The full sweep now has 85 additions beyond the 16 pre-existing contexts; 33 eligible contexts remain in pages 500–584. **Verification:** 37 appended ruling objects across 31 fixtures, no fixture deletions, all earlier objects unchanged; importer idempotent; Qiraat validator clean (1,866 variants, 9,731 rulings, 20 one-colour categories); 37/37 Qiraat tests; all seven Mushaf validators; TypeScript; production build passed. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-{401,403,406,409,412,413,414,415,419,422,425,427,430,434,437,438,439,440,446,461,462,473,477,478,480,481,482,488,489,493,495}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **Production:** data commit `352327e48a008476c41538534e84da3f6ede44dd` was pushed to `origin/main`; Vercel deployment `dpl_6AXVin7ToE8Sf9idd92CEWL8H5vh` reached READY for that exact SHA at [https://mutshabehat-v2-4tebvlf6n-shazlka-s-projects.vercel.app](https://mutshabehat-v2-4tebvlf6n-shazlka-s-projects.vercel.app). Production debug API smoke passed for all 37 new loci with exact spans, `#0891B2`, and both Abu Jaafar narrations. **DB migration:** none — fixture-only.
- **External rule audit — إخفاء أبي جعفر, pages 300–399:** applying Ibn al-Jazari’s ghayn/kha rule from [*al-Nashr*](https://www.islamweb.net/amp/ar/library/content/70/157/%D8%AA%D9%86%D8%A8%D9%8A%D9%87%D8%A7%D8%AA) and [*Sharh Tayyibat al-Nashr*, p. 113](https://ablibrary.net/book_content/b/8368/113), the page-token sweep added 34 loci (68 explicit Abu Jaafar narration assignments) on pages 302, 304, 305, 312, 313, 314, 319, 323, 324, 326, 334, 339, 343, 344, 345, 348, 352, 353, 355, 362, 366, 368, 369, 377, 382, 383, 388, 389, 390, 394, and 397. Page 316 at 20:71 was already covered for both narrations and was skipped despite action-spelling variation; the importer now recognizes that coverage by reader ID. The full audit has 134 eligible contexts, 16 previously covered, 48 added in the first two slices, and 70 remaining on pages 400–584. **Verification:** 34 new ruling objects across 31 files, no fixture deletions, prior fixture objects unchanged; importer rerun idempotent; Qiraat validator clean (1,866 variants, 9,694 rulings, 20 one-colour categories); 37/37 Qiraat tests; all seven Mushaf validators; TypeScript; production build passed. **Files:** `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-{302,304,305,312,313,314,319,323,324,326,334,339,343,344,345,348,352,353,355,362,366,368,369,377,382,383,388,389,390,394,397}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **Production:** data commit `97b5a05089c78802610f3ec8cae0c6aa3496deeb` was pushed to `origin/main`; Vercel deployment `dpl_HGdX7JZerdSLFjozuwR5o5GvkJxX` reached READY for that exact SHA at [https://mutshabehat-v2-aj2vlh9ic-shazlka-s-projects.vercel.app](https://mutshabehat-v2-aj2vlh9ic-shazlka-s-projects.vercel.app). Production debug API smoke passed for all 34 new loci, including exact spans, `#0891B2`, and both Abu Jaafar narrations. **DB migration:** none — fixture-only.
- **External rule audit — إخفاء أبي جعفر, pages 225–299:** checked adjacent real page tokens against *al-Nashr* and *Sharh Tayyibat al-Nashr*; found 134 rule contexts across source pages 225–584, 16 already covered, and added 14 rulings (28 reader assignments) in this slice on pages 227, 233, 257, 259, 260, 264, 274, 286, 288, 289, and 297. Excluded the three named exception verses. Four inherited p227–228 records anchor before waw instead of ghayn/kha; the importer now rejects those source anchors, while their old fixture objects remain unchanged under the additive-only rule. **Pages covered in this piece:** 227, 233, 257, 259, 260, 264, 274, 286, 288, 289, 297. **Verification:** 14 fixture additions, no fixture deletions, existing ruling objects byte-for-byte unchanged; importer idempotent; Qiraat validator clean (1,866 variants, 9,660 rulings, 20 one-colour categories); 37/37 `test:qiraat`, seven Mushaf validators, TypeScript, and production build passed. Data commit `7ce48a0cafb31b9d11a79fcd69acb593ef38740e` is live as Vercel deployment `dpl_2AWDPRcUgjSr2ViHvKcKkzjCfJUB`; API smoke verified all 14 token loci with Q08-R01/Q08-R02 and `#0891B2`. **Files:** `scripts/qiraat/import_doc_tables.py`, 11 page ruling fixtures, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only. 104 valid contexts remain in pages 300–584.
- **WAQF_HAMZA source-detail reconciliation:** the parser now keeps already-recorded Hamza/Hisham assignments and adds unmatched source action details to a separate `sourceNotes` field, which the Mushaf ruling detail view displays. Added three source-note entries and no reader assignments or loci: page 228 (11:54 ﴿بِسُوءٍ﴾) and page 234 (11:109 ﴿هَـٰٓؤُلَآءِ﴾; 11:114 ﴿السَّيِّـَٔاتِ﴾). The project maps Hisham to Q04-R01. External references corroborate the وقف حمزة forms and the exact source details: [Ibn al-Jazari, *al-Nashr*, باب الوقف على الهمز](https://islamweb.net/ar/library/index.php?ID=123&bk_no=70&flag=1&page=bookcontents), [*معالم الإرشاد في معرفة الوقف والابتداء* on the 13 faces at هؤلاء](https://www.islamweb.net/ar/library/content/229/39/index.php?ID=34&bk_no=229&idfrom=32&idto=32&page=bookcontents_ver3), and [*Raf‘ al-darajat* on السيئات](https://d1.islamhouse.com/data/ar/ih_books/chain/ar_elqaraat/ar-qarat-hamza-elzayat-new.pdf). **Pages updated in this piece:** 228, 234 (three targeted source details; full-page review remains pending). **Verification:** fixture diffs contain only six added JSON lines across two files; existing ruling objects remain unchanged; importer rerun is idempotent; Qiraat validator clean (1,866 variants, 9,646 rulings, 20 one-colour categories); 37/37 `test:qiraat`, all seven Mushaf validators, TypeScript, and production build passed. Production commit `61a4c4fa635513a95713a66669a595caa45bc0c9` was pushed to `origin/main`; Vercel deployment `dpl_9cxBLvRDMPdyjJd2fpgaHsVZQ35r` reached READY for that exact SHA at [https://mutshabehat-v2-gtyvonnna-shazlka-s-projects.vercel.app](https://mutshabehat-v2-gtyvonnna-shazlka-s-projects.vercel.app). Production API smoke returned the expected reader IDs, `#EA580C` colour, and all three source notes on pages 228 and 234. The Mushaf detail panel displayed the 11:109 source note; browser console errors and warnings: 0. Files: `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-{228,234}.json`, `packages/qiraat-core/types.ts`, `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **WAQF_RASM source-note reconciliation:** corrected the importer to compare the core waqf action independently of legacy action-embedded imalah wording. All reader IDs at the four source loci were already present, so this pass added no duplicate assignments or rulings; it added the four explicit source qualifiers `(مع الإمالة)` / `(مع الإمالة بخلف)` to notes on pages 231, 236, 238, and 241. The relevant reader rule is corroborated by [*al-Taysir*](https://quranpedia.net/book/1148/1/53) and [*al-Tayyibat*](https://truthseekerway.com/dl/qs/tajwid/tajweed-qraat/nnxhLn6f.pdf). Repeated qualifiers already represented in notes on pages 491, 543, and 561 were deduplicated. **Pages updated in this piece:** 231, 236, 238, 241 (targeted loci; full-page review remains pending). **Verification:** fixture diff added four lines and removed none; importer rerun was idempotent; Qiraat validator clean (1,866 variants, 9,646 rulings, 20 one-colour categories); 36/36 `test:qiraat`, all seven Mushaf validators, TypeScript, and production build passed. Production commit `a9bbe2fcbbe790b207686eac91a88e13606843dc` was pushed to `origin/main`; Vercel deployment `dpl_f4VwuZ11d57YwWFfmAkTie8cjBvt` reached READY for that exact SHA at [https://mutshabehat-v2-mhd9k4tyr-shazlka-s-projects.vercel.app](https://mutshabehat-v2-mhd9k4tyr-shazlka-s-projects.vercel.app). Production API smoke passed at page 231 for 11:86 ﴿بَقِيَّتُ﴾: the WAQF_RASM ruling returned all eight expected reader IDs, the #EA580C family colour, and the new مع الإمالة note. Files: `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-{231,236,238,241}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Data import — الإدغام الصغير from the attached page extraction:** fixed per-anchor reader scoping in the source parser, normalized inflected أبو جعفر/أبي عمرو names, and suppressed generic `إدغام صغير` duplicates when a reader already has a more specific idgham action. Added six new token-anchored loci and expanded two existing loci with 49 explicit reader assignments across pages 349, 362, 437, 449, 462, and 478. The downloaded official Quranpedia corpus corroborated reader groups at 23:110, 23:112, 25:27, and 35:26; because its wider dump contains many wrong-verse and unmatched anchors, it was used only for targeted corroboration. **Pages covered in this piece:** 349, 362, 437, 449, 462, 478 (targeted loci; full-page review remains pending). **Verification:** fixture diff added 573 lines and removed none; importer rerun was idempotent; Qiraat validator clean (1,866 variants, 9,646 rulings, 20 one-colour categories); 36/36 `test:qiraat`, all seven Mushaf validators, TypeScript, and production build passed. Production commit `16623cdfebfa52193d6cfe883f2bc6cd2ed60f74` was pushed to `origin/main`; Vercel deployment `dpl_6FtCoqFBjvhkaRNaQD21RBMtgH3Z` reached READY for that exact SHA at [https://mutshabehat-v2-98yif70of-shazlka-s-projects.vercel.app](https://mutshabehat-v2-98yif70of-shazlka-s-projects.vercel.app). Production API smoke passed at page 349 for 23:110 ﴿فَٱتَّخَذْتُمُوهُمْ﴾ and 23:112 ﴿لَبِثْتُمْ﴾; both IDGHAM_SAGHIR records returned the expected reader IDs, including the newly added Q01-R01/R02, Q05-R01, Q09-R02, and Q04-R02. Files: `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-{349,362,437,449,462,478}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Data import — الهمزتان من كلمة وكلمتين from the attached page extraction:** the hamzatan parser now scopes parenthetical `بخلف` and `في وجهه الثاني` to the named reader, preserves explicit action alternatives, and withholds multi-anchor lines whose special mode is scoped only to the second anchor. Added 52 explicitly named assignments to 10 existing `HAMZATAN_KALIMA` loci on pages 288, 327, 328, 361, 440, 441, 453, 529, 544, and 563; no new loci. The `HAMZATAN_KALIMATAYN` pass added no assignments; 29 non-compatible source assignments were withheld. Two same-reader alternate conflicts on page 327 (`Q04-R01` and `Q01-R02`) were also withheld. **Pages updated in this piece:** 288, 327, 328, 361, 440, 441, 453, 529, 544, and 563 (targeted loci; full-page review remains pending). **Verification:** fixture diff added 468 lines and removed none; Qiraat validator clean (1,866 variants, 9,640 rulings, 20 one-colour categories); 36/36 `test:qiraat`, TypeScript, and production build passed. Production data commit `fb68ed5b47cb2b04ca9e99a23ddecdcbe5643c39` was pushed to `origin/main`; Vercel deployment `dpl_A2yHVvksjT6BNkzgtTqA9DJHax5W` reached READY for that exact SHA. Production Mushaf UI smoke passed on page 288, 17:61 (`HAMZATAN_KALIMA`), with the named reader/action groups visible under the review-data toggle. Files: `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-{288,327,328,361,440,441,453,529,544,563}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Data import — ياءات الزوائد from the attached page extraction:** added 25 new `YAAT_ZAWAID` loci and completed 11 existing loci with 173 explicitly named reader assignments across Mushaf pages 230, 246, 258, 260, 272, 288, 292, 295, 296, 298, 301, 429, 433, 437, 441, 448, 449, 460, 468, 470, 471, 487, 494, 497, 518, 520, 528, 529, and 530. Five same-reader disagreements were withheld: four narrations at 20:93 page 318 conflict with the existing actions, and Warsh’s `Q01-R02` at page 337 has a source/default-status mismatch. The anchors ﴿نَرْتَعِ﴾ on page 236, ﴿مَتَابِ﴾ on page 254, and ﴿فَلَا تَسْتَعْجِلُونِ﴾ on page 523 did not match a real word-fixture token and were dropped. Parenthetical possible Qalun alternates on pages 468 and 470 were not promoted without a separately explicit reader clause. **Pages covered in this piece:** 230, 246, 258, 260, 272, 288, 292, 295, 296, 298, 301, 429, 433, 437, 441, 448, 449, 460, 468, 470, 471, 487, 494, 497, 518, 520, 528, 529, 530 (targeted loci; full-page review remains pending). **Verification:** fixture diff added 2,110 lines and removed none; Qiraat validator clean (1,866 variants, 9,640 rulings, 20 one-colour categories); 36/36 `test:qiraat`, TypeScript, and production build passed. Production commit `6228f81fb2757c815fbac495f098f7025b147368` was pushed to `origin/main`; Vercel deployment `dpl_5VYhwJQAgVqHN2jV39CLdDDPeGu1` reached READY for that SHA. Production Mushaf UI confirmed the new ruling at page 230, 11:78. Files: `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-{230,246,258,260,272,288,292,295,296,298,301,429,433,437,441,448,449,460,468,470,471,487,494,497,518,520,528,529,530}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Data import — ياءات الإضافة from the attached page extraction:** added seven new token-anchored `YAAT_IDAFA` rulings across Mushaf pages 259, 307, 329, 429, 455, 462, and 564, with 19 explicitly named reader assignments. Existing source-covered assignments were deduplicated; the two conflicting Hamza assignments at 23:105 on page 331 were withheld because the fixture says فتح and this extraction says إسكان. The parser now merges only missing compatible assignments, normalizes equivalent wording, and drops same-reader action conflicts. **Pages covered in this piece:** 259, 307, 329, 429, 455, 462, 564 (targeted loci; full-page review remains pending). **Verification:** fixture diff added 325 lines and removed none; Qiraat validator clean (1,866 variants, 9,615 rulings, 20 one-colour categories); 36/36 `test:qiraat`, TypeScript, and production build passed. Production commit `f6eb36977d84f12a70ea9bcf882aca7d556289bd` was pushed to `origin/main`; Vercel deployment `dpl_DU4t6WNHaBB7RZCXCU4QZdgb9S1j` reached READY for that SHA. Production Mushaf UI confirmed the new ruling at page 259, 14:31. Files: `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-{259,307,329,429,455,462,564}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Data correction — imalah and taqlil reader reconciliation on pages 247, 279, and 583:** added both Abu Amr narrations as taqlil at Yusuf 12:100 ﴿رُءْيَـٰيَ﴾, preserving the Susi hamza substitution note in the source audit; added Ruways (`Q09-R01`) with imalah at Nahl 16:107 ﴿الْكَـٰفِرِينَ﴾, supported by the source parenthetical and [*غاية المطلوب*](https://aljam3.com/ar/16352/36528/136); and added both al-Kisai narrations at eight unambiguous waqf imalah loci in Naba 79:6–14. The separate ﴿خَاشِعَةٌ﴾ occurrence was withheld because the source marks a khilaf there. Lines on pages 259, 289, 293, 562, and 578 containing bare «خلف» were dropped; the parser no longer guesses that name as Khalaf al-Ashir and now supports category-scoped additive writes. [nQuran's ten-readings display](https://www.nquran.com/ar/index.php?ayano=100&group=tb1&sorano=12&tpath=2) confirms both Abu Amr narrations' taqlil at 12:100. **Pages corrected in this piece:** 247, 279, 583 (targeted rulings; this does not certify each full page). **Verification:** `import_doc_tables.py 247 279 583 --category IMALAH_TAQLIL --write` added 0 new loci after dedupe; 19 reader assignments and 8 rulings were appended directly to real-token fixtures. Fixture diff added 347 lines and removed none; Qiraat validator clean (1,866 variants, 9,608 rulings, 20 one-colour categories), 36/36 `test:qiraat`, all seven Mushaf validators, TypeScript, and production build passed. Commit `16e3e65b9f3453c6f75e9103428451167cf881ca` was pushed to `origin/main`; Vercel deployment `dpl_3QWdA8cGCRAiH31aeFLVmjDnAJR6` reached READY for that SHA. Production Mushaf UI confirmed the added forms at page 247 (12:100), page 279 (16:107), and page 583 (79:6). Files: `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-{247,279,583}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Data correction — complete explicit imalah/taqlil assignments on pages 226 and 230:** added both Abu Amr narrations and Warsh at Hud 11:41 ﴿مَجْرَاهَا﴾, and al-Duri from Abu Amr with taqlil at Hud 11:72 ﴿يَاوَيْلَتَى﴾. The latter action follows the ten-readings reference’s specific classification as taqlil. References: [11:41](https://www.nquran.com/ar/index.php?ayano=41&group=tb1&sorano=11&tpath=2), [11:72](https://www.nquran.com/ar/index.php?ayano=72&group=tb1&sorano=11&tpath=2). Parser `--write` over 226 and 230 added no new loci; the four reader assignments were appended to existing real-token rulings. **Pages corrected in this piece:** 226, 230. **Verification:** Parser --write over pages 226 and 230 added 0 new loci; fixture diff added 32 lines and removed none. Qiraat validator clean (1,866 variants, 9,600 rulings, 20 one-colour categories); 36/36 test:qiraat; all seven Mushaf validators; TypeScript; production build. Commit `8441a3b` was pushed to `origin/main`; Vercel deployment `dpl_HhUwibqs2H3SmhREGsFDsAXB3JDW` reached READY for that SHA. Production debug API checks passed at both loci; records remain REVIEWED and debug-scoped. Files: `packages/qiraat-core/fixtures/rulings/page-226.json`, `page-230.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Data correction — complete three explicit reader groups on pages 326, 330, and 335:** expanded the existing `HAMZATAN_KALIMATAYN` rulings at Al-Anbiya 21:45 and 21:99 with Abu Ja'far's two narrations (`Q08-R01/R02`) and Ruways (`Q09-R01`), matching the source's named group and the ten-readings detail at [21:45](https://www.nquran.com/ar/index.php?ayano=45&group=tb1&sorano=21&tpath=2) and [21:99](https://www.nquran.com/ar/index.php?ayano=99&group=tb1&sorano=21&tpath=2). Added both Abu Ja'far narrations to `YAAT_IDAFA` at Al-Hajj 22:26 ﴿بَيْتِيَ لِلطَّائِفِينَ﴾; the [*al-Nashr* passage](https://jamharah.net/showthread.php?mode=hybrid&s=5a6971cef6ffadc9883063dcb7da604d&t=27573) names the two Madinan readers with Hisham and Hafs, and [Quranpedia's verse reading](https://quranpedia.net/surah/1/22/book/557) spells out Abu Ja'far. An all-pages reader-ID audit also found an apparent page 231 11:84 Ibn Kathir gap, but the independent [reading reference](https://quranpedia.net/book-attachment/18717/77135) specifies al-Bazzi only (already present), while the attached extraction generalizes this to Ibn Kathir; no Qanbul assignment was added. **Pages corrected in this piece:** 326, 330, 335 (three targeted loci; full-page audits remain pending). **Verification:** parser `--write` over 326, 330, 335 added 0 new loci; fixture changes added 72 lines and removed none. Qiraat validator clean (1,866 variants, 9,600 rulings, 20 one-colour categories), 36/36 `test:qiraat`, all seven Mushaf validators, `tsc --noEmit -p .`, and production build passed. Commit `d3bcdf9` was pushed to `origin/main`; production deployment `dpl_HJ5UEDhFN7YZ5uHxvdL2CdEoYf55` reached READY for that exact SHA. Live debug API checks returned the expected added reader IDs at 21:45 (page 326), 21:99 (page 330), and 22:26 (page 335). Files: `packages/qiraat-core/fixtures/rulings/page-{326,330,335}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Data correction — missing ياءات الإضافة reader assignments added on pages 236, 308, and 313:** expanded the existing token-anchored rulings at Yusuf 12:13 ﴿لَيَحْزُنُنِي أَنْ﴾ with Q01-R01, both Ibn Kathir narrations, and both Abu Ja'far narrations; at Maryam 19:47 ﴿رَبِّيٓ إِنَّهُۥ﴾ with both Abu Amr narrations; and at Ta-Ha 20:14 ﴿إِنَّنِيٓ أَنَا﴾ with both Ibn Kathir narrations. Ibn al-Jazari's [*al-Nashr*, chapter on yā’s followed by open hamza](https://lib.rafed.net/view.php?b_id=1120&page=163&type=c_fbook) specifically lists Ibn Kathir, Nafi, and Abu Ja'far for «ليحزنني»; *al-Nashr* and [*al-Mabsut*](https://ketabonline.com/ar/books/4114/read?page=283&part=1) confirm Abu Amr at Maryam 19:47; [Quranpedia's ten-readings display](https://quranpedia.net/tafsir/ta-ha/14) confirms both Ibn Kathir narrations on «إنني أنا». The page 236 extraction places Ibn Kathir and Abu Ja'far on «وأخاف أن», which has no yā of addition; that pre-existing record and the old explanatory text on «ليحزنني» remain unchanged under the additive-only rule and need a separate cleanup. **Pages covered in this piece:** 236, 308, 313 (targeted loci; full-page audits remain pending and page 236 retains the noted source anomaly). **Verification:** importer --write over 236, 308, 313 deduped all existing loci (0 new variants, 0 new rulings); fixture diff added 66 lines with zero removals; Qiraat validator clean (1,866 variants, 9,600 rulings, 20 one-colour categories), 36/36 test:qiraat, all seven Mushaf validators, tsc --noEmit -p ., and production build passed. Commit 0a6889a was pushed to origin/main; production deployment dpl_4ArVXzpAjepMpTcoxh4CfCCRVfMb reached READY. Live debug API checks returned the expected added reader IDs for 12:13 on page 236, 19:47 on page 308, and 20:14 on page 313. Files: packages/qiraat-core/fixtures/rulings/page-{236,308,313}.json, docs/qiraat-import-progress.json, CHANGELOG.md, CLAUDE.md. **DB migration:** none — fixture-only.

## 2026-09-20
- **Data correction — Ibn Amir added to page 348 ياء الإضافة:** expanded the existing YAAT_IDAFA ruling at 23:100 ﴿لَعَلِّيٓ أَعْمَلُ﴾ with `Q04-R01` and `Q04-R02`, preserving its real token anchor, colour, and all existing assignments. The page extraction explicitly names ابن عامر. Independent references: Ibn al-Jazari’s *al-Nashr* lists Ibn Amir among those opening «لعلي» in its six occurrences, including al-Mu’minun; the [Ibn Amir min al-Tayyiba reading reference](https://quranpedia.net/book-attachment/18176/76699) specifically lists «لعلي أعمل» for both narrations. **Page corrected in this piece:** 348 (one targeted locus; full-page review remains pending). **Verification:** fixture diff additive (14 lines added, none removed); source parser emitted both Q04 narrations and importer preview deduped the already-existing locus; Qiraat validator clean (1,866 variants, 9,600 rulings, 20 one-colour categories), 36/36 `test:qiraat`, all seven Mushaf validators, `tsc --noEmit -p .`, and production build passed. Commit `6c9dc9d` was pushed to `origin/main`; production deployment `dpl_Gp335qxPVoKZc7gUu1kEYEKgzwTG` reached READY. The live page 348 qiraat API debug response returned both Q04 narrator assignments at 23:100. Files: `packages/qiraat-core/fixtures/rulings/page-348.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Data correction — Hisham pause attribution restored at six loci:** added `Q04-R01` to the existing `WAQF_HAMZA` rulings at 11:43 ﴿الْمَاءِ﴾, 11:54 ﴿بِسُوءٍ﴾, 12:56 ﴿يَشَاءُ﴾ and ﴿نَشَاءُ﴾, 12:76 ﴿نَّشَاءُ﴾, and 22:18 ﴿يَشَاءُ﴾. Existing Hamza assignments and explanatory notes remain. The fixture registry maps هشام to `Q04-R01` (`Q07` is الكسائي). Independent check: Ibn al-Jazari’s [*al-Nashr*, باب الوقف على الهمز](https://islamweb.net/ar/library/content/70/129/%D8%AE%D8%A7%D8%AA%D9%85%D8%A9-%D9%81%D9%8A-%D8%B0%D9%83%D8%B1-%D9%85%D8%B3%D8%A7%D8%A6%D9%84-%D9%85%D9%86-%D8%A7%D9%84%D9%87%D9%85%D8%B2) restricts Hisham’s agreement with Hamza to terminal hamza, and the [*Mushaf Asim and Ibn Amir min al-Tayyiba*](https://quranpedia.net/book/19506) includes Hisham’s pause forms for final-hamza examples. **Pages corrected in this piece:** 226, 228, 242, 244, 334 (six targeted loci; full-page audits remain pending). **Verification:** additive-only fixture diff, all six Hisham assignments checked against source-emitted loci, Qiraat validator clean (1,866 variants, 9,600 rulings, 20 one-colour categories), 36/36 `test:qiraat`, all seven Mushaf validators, `tsc --noEmit -p .`, and production build passed. Commit `f54344b` was pushed to `origin/main`; production deployment `dpl_3RYjyVk482LTqrPrE1V3EQuQZivM` reached READY. The live qiraat API debug response for page 226 returned the corrected 11:43 ruling with `Q04-R01` (the imported records remain REVIEWED). Files: `packages/qiraat-core/fixtures/rulings/page-{226,228,242,244,334}.json`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Data correction — Abu Ja'far's مستهزون now renders on page 3:** added an explicit Q08 overlay at 2:14 ﴿مُسْتَهْزِءُونَ﴾ → ﴿مُسْتَهْزُونَ﴾ for ابن وردان and ابن جماز. The existing `TAGHYIR_HAMZ` ruling was already present and remains unchanged. The page 3 token anchor and reader form were checked against *اتباع الأثر في قراءة أبي جعفر*. **Page corrected in this piece:** 3 (one targeted locus; the full page audit remains pending). **Verification:** the fixture diff is additive; Qiraat validator clean (1,866 variants, 9,600 rulings across pages 1–584, 20 categories with one colour each), 36/36 `test:qiraat`, all seven Mushaf validators, `tsc --noEmit -p .`, and production build passed. Commit `33354fc` pushed to `origin/main`; Vercel deployment `dpl_AkWxcXwSLdwoE1nSadRpuhb66FCf` reached READY for that exact SHA. Production smoke passed for both Q08 narrations (displayed `مُسْتَهْزُونَ`), Abu Ja'far reader filter, existing hamz ruling details, and the 390×844 mobile sheet; Vercel reported no runtime errors. Files: `packages/qiraat-core/fixtures/pages/page-003.json`, `packages/qiraat-core/engine.test.mjs`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Data correction — Yaqub’s added-yā readings now render on page 7:** added explicit word overlays at 2:40 ﴿فَٱرْهَبُونِ﴾ → ﴿فَٱرْهَبُونِي﴾ and 2:41 ﴿فَٱتَّقُونِ﴾ → ﴿فَٱتَّقُونِي﴾ for Q09-R01 and Q09-R02. The existing `YAAT_ZAWAID` rulings were already present and were preserved. Attribution and both verse-head loci were checked against Ibn al-Jazari’s *al-Nashr*, chapter on extra yā’s, vol. 2, pp. 181–182. **Page corrected in this piece:** 7 (two targeted loci; the full page audit remains pending). **Verification:** the fixture diff is additive; Qiraat validator clean (1,865 variants, 9,600 rulings across pages 1–584, 20 categories with one colour each), 35/35 `test:qiraat`, all seven Mushaf validators, `tsc --noEmit -p .`, and production build passed. Files: `packages/qiraat-core/fixtures/pages/page-007.json`, `packages/qiraat-core/engine.test.mjs`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only. The commit deployed READY to production; page 7 was smoke-tested with رويس عن يعقوب and both added-yā forms rendered with their `YAAT_ZAWAID` details; no Vercel runtime errors were found.
- **Data correction — independently verified Hisham attribution for إبراهام in Baqarah:** expanded 12 existing Ibn Dhakwan-alternate records with Hisham (Q04-R01) across Mushaf pages 19, 20, 43, and 44; pages 21's three matching loci already had both Q04-R01 and Q04-R02. Corrected five final case vowels so each variant preserves the case ending of its canonical Mushaf token. The scanned مصحف القراءات العشر tables, Quranpedia Qiraat data at 2:125/2:133, and the commentary on Shatibiyyah verse 480 agree. A regression test checks all 15 Ibrahim loci. **Pages corrected in this piece:** 19, 20, 43, 44 (targeted Ibrahim loci; the full page audits continue). **Verification:** no variant/ruling records removed; 34/34 `test:qiraat`, qiraat validator clean (1,863 variants, 9,600 rulings over pages 1–584), all seven Mushaf validators, `tsc --noEmit -p .`, and production build passed. Files: `packages/qiraat-core/fixtures/pages/page-{019,020,043,044}.json`, `packages/qiraat-core/engine.test.mjs`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only. Production commit 7688bc3 is READY; the live page 19 Hisham display was smoke-tested at 2:124.
- **Data correction — independently verified page 303 reading assignment:** added خلف عن حمزة and خلاد عن حمزة to the existing 18:96 hamzat-wasl variant alongside شعبة, based on Quranpedia's versioned Qiraat dump (file version 2026-09-19; checksum recorded in the fixture). Preserved the original page extraction and added the independent source reference. **Pages checked in this piece:** 303. **Verification:** additive fixture update; Qiraat validator clean (1,863 variants, 9,600 rulings across pages 1–584), 33/33 `test:qiraat`, all seven Mushaf validators, `tsc --noEmit -p .`, and production build pass. The live deployment smoke check follows the push. Files: `packages/qiraat-core/fixtures/pages/page-303.json`, `packages/qiraat-core/engine.test.mjs`, `docs/qiraat-import-progress.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Feature — page 303 فرش gap recovered from the page-by-page source:** added five explicit, token-aligned variants at 18:86, 18:88, 18:95, 18:96, and 18:97 (حَامِيَةٍ, يُسُرًا, مَكَّنَنِي, ائْتُونِي, and اسْطَّاعُوا). The source puts multiple reading clauses on single lines and quotes an alternate as the header at 18:95, so the importer now handles these five reviewed cases locally while anchoring each to the Hafs word fixture and retaining the original source row. Rows containing ambiguous bare «خلف», the unsupported group «جميع القراء الآخرين», or no exact alternate token remain dropped. **Pages done in this piece:** 303. **Verification:** additive-only; full source preview is idempotent (0 additional variants/rulings); Qiraat validator clean (1,863 variants, 9,600 rulings across pages 1–584), 33/33 `test:qiraat`, all seven Mushaf validators, `tsc --noEmit`, and production build pass; Vercel READY; page 303 production smoke passed with four reader-specific forms and no console errors or warnings. Files: `docs/qiraat-import-progress.json`, `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/pages/page-303.json`, `packages/qiraat-core/fixtures/rulings/page-303.json`, `packages/qiraat-core/engine.test.mjs`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Feature — إخفاء أبي جعفر imported from the page-by-page source:** added seven token-anchored IKHFA rulings (colour `#0891B2`) across Mushaf pages 227, 228, 250, and 269, attributed to Q08 with both narrators. One source anchor, `بَيِّنَةٍ وَمَا`, had no matching real token and was dropped; the unrelated existing IKHFA record on page 227 was preserved. **Pages done in this piece:** 227, 228, 250, 269. **Verification:** all existing fixture records remain unchanged prefixes, with zero fixture-line deletions; Qiraat validator clean (1,858 variants, 9,600 rulings across 583 pages, 20 categories with one colour each), 32/32 `test:qiraat`, `tsc --noEmit`, and production build clean. Files: `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-{227,228,250,269}.json`, `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Feature — وقف حمزة imported from the page-by-page source:** added 12 token-anchored WAQF_HAMZA rulings across nine Mushaf pages, with Hamza as the fixed reader and explicit Hisham agreements on five new loci. The repository maps Hisham to Q04-R01 (Q07 is al-Kisai). Fifteen already-covered token/category loci were deduped and preserved unchanged, including six source loci with an explicit Hisham marker; the import remains additive. **Pages done in this piece:** 231, 241, 245, 277, 304, 310, 331, 334, 437. **Verification:** zero fixture-line deletions and no existing records changed; Qiraat validator clean (1,858 variants, 9,593 rulings across 583 pages, 20 categories with one colour each), 32/32 test:qiraat, tsc --noEmit, and production build clean. Files: scripts/qiraat/import_doc_tables.py, packages/qiraat-core/fixtures/rulings/page-NNN.json (9 pages), CHANGELOG.md, CLAUDE.md. **DB migration:** none — fixture-only.
- **Feature — وقف على مرسوم الخط imported from the page-by-page source:** added 24 token-anchored WAQF_RASM rulings (colour #EA580C) across 20 Mushaf pages, attributing only the explicitly named بالهاء readers. Remainder clauses and other waqf forms were omitted; six already-covered loci were deduped, parenthetical notes such as مع الإمالة were retained, and the negation-marked line on page 407 was dropped. **Pages done in this piece:** 247, 259, 260, 274, 276, 280, 305, 350, 409, 414, 434, 439, 453, 476, 491, 498, 524, 537, 543, 561. **Verification:** zero fixture-line deletions and no existing records changed; Qiraat validator clean (1,858 variants, 9,581 rulings across 583 pages, 20 categories with one colour each), 32/32 test:qiraat, tsc --noEmit, and production build clean. Files: scripts/qiraat/import_doc_tables.py, packages/qiraat-core/fixtures/rulings/page-NNN.json (20 pages), CHANGELOG.md, CLAUDE.md. **DB migration:** none — fixture-only.
- **Feature — both hamzatan families imported from the page-by-page source:** added 55 explicit-reader, token-anchored rulings (39 `HAMZATAN_KALIMATAYN`, 16 `HAMZATAN_KALIMA`, colour `#DC2626`) across 44 Mushaf pages. Clauses attributed only to the remainder and references to rules stated earlier were omitted; unresolved groups and non-token anchors were dropped. Fourteen already-covered loci were deduped, and the fixture audit confirmed all existing records remain prefixes. **Pages done in this piece:** 247, 259, 273, 288, 292, 304, 305, 332, 340, 356, 361, 363, 415, 417, 422, 424, 425, 433, 434, 439, 440, 441, 447, 448, 449, 453, 476, 477, 479, 488, 493, 495, 506, 508, 516, 518, 529, 530, 539, 544, 549, 551, 555, 563. **Verification:** zero fixture-line deletions and no existing records changed; Qiraat validator clean (1,858 variants, 9,557 rulings across 583 pages, 20 categories with one colour each), 32/32 `test:qiraat`, `tsc --noEmit`, and production build clean. Files: `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/fixtures/rulings/page-NNN.json` (44 pages), `CHANGELOG.md`, `CLAUDE.md`. **DB migration:** none — fixture-only.
- **Feature — ياءات الإضافة وياءات الزوائد imported from the page-by-page source:** added 90 explicit-reader, token-anchored أصول rulings (62 `YAAT_IDAFA`, 28 `YAAT_ZAWAID`, colour `#CA8A04`) across 56 Mushaf pages. Negated/universal lines, remainder clauses, unresolved names, and non-token anchors were omitted; existing fixture records remain byte-for-byte prefixes. Newly populated pages 301–302 are wired in both repository loader maps. **Pages done in this piece:** 227, 239, 242, 245, 247, 248, 253, 258, 260, 265, 267, 292, 295, 296, 298, 301, 302, 304, 305, 318, 320, 349, 362, 433, 434, 437, 441, 449, 453, 454, 455, 457, 460, 465, 467, 470, 471, 472, 474, 482, 491, 493, 497, 504, 505, 520, 523, 530, 544, 547, 552, 564, 570, 571, 573, 581. **Verification:** zero fixture-line deletions and no existing records changed; Qiraat validator clean (1,858 variants, 9,502 rulings across 583 pages, 20 categories with one colour each), 32/32 `test:qiraat`, `tsc --noEmit`, and production build clean. Files: `scripts/qiraat/import_doc_tables.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/pages/page-{301,302}.json`, `packages/qiraat-core/fixtures/rulings/page-NNN.json` (56 pages). **DB migration:** none — fixture-only.
- **Feature — صلة ميم الجمع is now its own أصول family (MEEM_JAM, colour `#DB2777`), and the two records that were mislabeled under صلة هاء الكناية are reclassified onto it (Mushaf pages 129, 164):** first piece of the "all usul + farsh families" expansion. ميم الجمع (ابن كثير reading the plural mīm with a صلة واو: عَلَيْهِمُو، ٱسْتَرْهَبُوهُمُو) was filed under `SILAT_HA` (a different rule — the singular هاء الكناية), so 6:9 ﴿عَلَيْهِم﴾ (page 129) and 7:116 ﴿وَٱسْتَرْهَبُوهُمْ﴾ (page 164) painted teal and read wrong; they now carry the dedicated pink family and read «صلة ميم الجمع». No UI change and no validator count change (colours are read off each ruling; the linter only enforces one-colour-per-category — now 20 families). Edited surgically in the two fixtures (verified byte-identical to a canonical `build_rulings.py` regeneration of those pages in isolation) rather than re-running the full generator, which would clobber the doc-import additions written directly into the 225–244 fixtures. Source of truth updated too: `build_rulings.py` `CATEGORIES`, `data_rulings.py` (rows + comment), and the `RCOLORS` in both parsers. The docx (225–584) has no ميم الجمع lines, so no page in that range changed. **Pages done: 129, 164.** Verification: `qiraat:validate` clean (1,858 variants, 9,412 rulings, 581 pages, 20 categories, 4 flagged), 32/32 tests, `tsc` clean, `next build` clean. Files: `scripts/qiraat/{build_rulings,data_rulings,import_doc_tables,import_surah_tables}.py`, `packages/qiraat-core/fixtures/rulings/page-{129,164}.json`. **DB migration:** none — fixture-only.
- **Feature — Doc extraction reconciled against the 255 already-imported pages (225–584): +1 variant and +802 أصول rulings added, strictly additively, no existing record touched:** the second piece of the docx import. Where the doc overlaps pages that other batches already filled, the importer ran with dedup ON so it adds a locus only for a token that has **no existing record** ((surah,ayah,startToken[,category]) not already present) — existing hand-encoded records are preserved byte-for-byte and only appended to. This fills the coverage the earlier partial bulk import (esp. pages 359–537) left uncoloured, without any duplication. **Independently verified strictly additive:** across all 211 modified fixture files the git diff has **zero deleted lines and zero files that shrank** (checked every page's HEAD-vs-worktree record count), so no existing ruling/variant was dropped or altered; e.g. page 305 rulings 32→33, page 440 rulings 7→10, both prefix-identical. Same guards as Piece 1 (deterministic/explicit-reader families only, per-anchor الممال clause splitting, negation/universal refusal, real-token anchoring, drop-on-doubt). Delegated to a Sonnet subagent under the lead's review; the lead owns git/deploy and re-ran every check. **Verification:** `qiraat:validate` clean across all 581 active pages (1,858 variants, 9,412 rulings, 4 flagged — unchanged), 32/32 engine tests, `tsc --noEmit` clean, `next build` clean, all 7 Mushaf validators, and Playwright/Chromium at 1440×950 over overlap pages 225, 400, 520 — **3/3 colour-code checks, zero real page errors** (40–189 أصول-coloured words per spread). Ships at `REVIEWED`, pending the paper original. With this, the doc's pages 225–584 are reconciled (gaps filled in Piece 1, overlaps topped up here). Files: `packages/qiraat-core/fixtures/{pages,rulings}/page-NNN.json` (211 modified, additive only). **DB migration:** none — fixture-only.
- **Feature — Qiraat gap pages 245–584 filled from the page-by-page prose extraction (docx): +7 variants and +566 أصول rulings across 102 previously-empty Mushaf pages, via a third guarded parser, delegated to a Sonnet subagent under review:** the user supplied a docx that extracts القراءات العشر صفحةً صفحة for Mushaf pages **225–584**. Of those, 255 pages were already imported (cross-check deferred) and **105 were gaps** with no fixtures at all (245–248, 268–304, 467–476, a few singletons, 538–584); this piece imports those gaps, where the doc is the sole source and there is **zero duplication risk by construction**. **(1) A third ingestion path.** New `scripts/qiraat/import_doc_tables.py` parses the doc's page-keyed prose (verified: doc page N == Mushaf page N, no offset) reusing `tokens.py`/`authorities.py`/`import_surah_tables.resolve_readers`, and enforces the same hard invariants as every batch: a variant ships only if its أوجه partition the 20 Riwayat once with the حفص وجه matching the rasm at a real token; a ruling anchors on a real token with verbatim baseText; anything unprovable is **dropped, never guessed**. It also dedups against any existing fixtures per page (add a locus only if no record shares its surah/ayah/token[/category]). **(2) The أصول families this source labels per line are parsed with per-anchor precision.** Two bugs were found and fixed before anything shipped: the الممال والمقلل lines carry several anchors with *different* reader groups on one line (﴿تَتَوَفَّىٰهُمُ﴾ الكسائي+ورش vs a neighbour حمزة/خلف), which a line-level parse flattened — now each clause (split on ؛/./وأمال) resolves its own anchors and groups, and ورش's «بخلف» تقليل is stored as a non-default alternate (`hasAlternate`); and تغيير الهمز was mis-applying its default إبدال trio to per-anchor exceptions (﴿يَسْتَهْزِءُونَ﴾ is أبو جعفر حذف *only*) — now it emits only explicitly-attributed clauses and drops the rest. Deterministic families (مد البدل/ترقيق/تغليظ/النقل→ورش, الإدغام الكبير→السوسي, ترك الغنة→خلف عن حمزة, صلة هاء→ابن كثير only) keep their fixed attribution; negation («غير وارد»، «لا يدغم»، «لا ترقق») and universal («للجميع»، «لجميع القراء») markers are refused. **(3) What dropped, honestly.** 3 of the 105 pages produced nothing (301/302/303: only out-of-scope ياءات lines, a negation-guarded ترقيق, or farsh-only), and a genuine farsh-shaped locus like page 270 ﴿يَتَوَفَّاهُمُ﴾ was dropped because that token does not occur on the page (the doc describes a تاء/ياء *reading* there, not a same-rasm أصول note) — correctly refused rather than mis-anchored. Farsh (variant) yield is intentionally low: the doc's single-line farsh forms carry bare «خلف»/multi-anchor clauses that are unsafe to auto-resolve, so only the clean multi-line 2-way blocks import. **(4) Division of labour** (per the user's request to conserve the lead model's quota): the lead designed the parser scaffold, found the two correctness bugs, and owns all git/deploy and Qiraat-correctness review; a Sonnet subagent did the parser refinement, the 105-page import, and the loader wiring, and reported samples for review — it self-caught a third regression (a missing `.`-split that re-flattened multi-clause lines) and reran clean. **Verification (re-run independently by the lead):** `qiraat:validate` clean across all 581 active pages (1,857 variants, 8,610 rulings, 4 flagged — unchanged), 32/32 engine tests, `tsc --noEmit` clean, `next build` clean, all 7 Mushaf validators, and Playwright/Chromium at 1440×950 over new pages 270, 300, 540, 560, 584 — **5/5 colour-code checks, zero real page errors** (16–33 أصول-coloured words per spread). Nothing outside the allowed file set changed. Ships at `REVIEWED`, visible only through the include-reviewed path, pending the paper original. **Still to do (later pieces):** the 255 overlap pages (verify the doc against existing data, add only tokens with no record — strictly additive), and the doc's richer فرش variants where a safe parse is possible. Files: `scripts/qiraat/import_doc_tables.py` (new), `packages/qiraat-core/fixtures/pages/page-NNN.json` (102 new), `packages/qiraat-core/fixtures/rulings/page-NNN.json` (102 new), `packages/qiraat-core/repository.ts`. **DB migration:** none — fixture-only.
- **Feature — Surahs 25–56 (الفرقان–الواقعة) bulk-imported from ayah-by-ayah tables: +235 variants and +772 أصول rulings across Mushaf pages 359–537, via a parser that ships only what a hard invariant can prove and drops the rest:** the user supplied 5 workbooks / 32 sheets / **2,220 ayat** in the same فرش+أصول+شواهد table shape as Maryam — far too many to hand-encode, so this batch is imported by a new parser, `scripts/qiraat/import_surah_tables.py`, run once against the extracted JSON. **(1) The safety model is the point.** A variant is emitted **only** when its أوجه partition all 20 Riwayat exactly once (no gap, no overlap) *and* the وجه containing حفص (Q05-R02) matches the Mushaf-1441 rasm verbatim at a real anchored token — the same two invariants `build_variants.py` enforces. Anything that misses is dropped, never guessed: of 1,568 فرش blocks, 215 loci (235 records) passed; the ~1,013 single-statement waqf/naql notes, 29 anchor misses and 58 baseline-text mismatches (where the source quoted a *reading* like ﴿وَثَمُودَ﴾ where the rasm is ﴿وَثَمُودَا۟﴾, so no safe anchor exists) were dropped and logged to `docs/qiraat/surahs-25-56-dropped.md` for the paper original. **(2) أصول rulings are parsed with hard guards, because they have no partition checkpoint** — a misparsed ruling would ship silently. The prose mixes real rulings with **negative** statements («ترقيق راء (المرسلين) غير وارد … فتفخم لجميع القراء»، «الإدغام الكبير … لا يدغم … بل إظهار للجميع») and **universal** ones («صلة صغرى … للجميع»، «ساكنة لجميع القراء») under the *same* leading phrase. The parser imports only families whose attribution is deterministic or explicitly listed — مد البدل/مد اللين/ترقيق/تغليظ/النقل (ورش), الإدغام الكبير (السوسي), صلة هاء الكناية (ابن كثير only, never the universal case), الممال والمقلل (reader groups parsed per action), and the 2-way إدغام الصغير — and refuses any bullet containing a negation or universal marker, any unresolved reader, or the universal families (ميم الجمع، المد المنفصل/المتصل/العارض/اللازم، الإخفاء العام), exactly the Maryam classification. 5,279 أصول bullets → 772 rulings imported, 4,449 skipped, 58 anchor-dropped. Verified by spot test that every negative/universal sample is rejected and every real one attributed correctly. **(3) A real resolver bug found and fixed.** The reader-name resolver stripped a leading و as a connector *before* trying a whole-name match, so every و-initial name in a list — ورش above all — was silently turned into «رش» and dropped, quietly corrupting reader sets (and, for variants, failing their partition so they vanished rather than shipping wrong). It now matches a whole name as-is first and only treats و as a connector when a real name follows it; ﴿الْمَوْتَىٰ﴾ now correctly yields «إمالة → حمزة، الكسائي، خلف العاشر» *and* «تقليل → ورش، أبو عمرو». Bare «خلف» / «الدوري» still refuse to resolve (drop), never guessed between the reader and the narrator. **(4) The page table stays deliberately non-contiguous** — 225–304 and 313–358 are unimported, so 312 → 359 → … → 537 with gaps is legal; the loader table, `build_rulings.py` and `validate-qiraat-data.mjs` are all data-driven and none assumes a dense range. Every touched page gets both a `fixtures/pages/page-NNN.json` and a `fixtures/rulings/page-NNN.json` (empty array where a page has only one kind) so the linter reads both. **Verification:** `qiraat:validate` clean across all 394 pages (1,467 variants, 6,316 rulings, 4 flagged — unchanged), 30/30 engine tests, `tsc --noEmit` clean, `next build` clean, all 7 Mushaf validators, and Playwright/Chromium at 1440×950 over pages 359, 367, 440, 453, 458, 467, 489, 518, 531 — **10/10 checks, zero real page errors** (the 401s are the annotations API with no signed-in account) — 14–41 أصول-coloured words and 0–17 variant-marked words per spread, and clicking ﴿ٱلْكَٰفِرِينَ﴾ (35:39) explaining it as «إمالة → الراوي الدوري عن أبي عمرو، الراوي السوسي…». As with every batch, nothing claims `VERIFIED`; it ships at `REVIEWED`, visible only through the include-reviewed path, pending the paper original — and coverage here is intentionally partial (the clean-partition farsh + deterministic rulings only), with the dropped remainder catalogued rather than guessed. Files: `scripts/qiraat/import_surah_tables.py` (new), `packages/qiraat-core/fixtures/pages/page-{359..537}.json` (162 new, non-contiguous), `packages/qiraat-core/fixtures/rulings/page-{359..537}.json` (162 new), `packages/qiraat-core/repository.ts`, `docs/qiraat/surahs-25-56-dropped.md` (new), `PROJECT_MASTER.md`, `HANDOFF.md`. **DB migration:** none — fixture-only.
- **Fix — completed the second Qiraat colour-coverage audit for pages 249–267:** imported the
  outstanding unambiguous reader-specific loci from the supplied Ar-Ra'd, Ibrahim, and Al-Hijr
  tables: +2 comparison variants (15:82 ﴿بُيُوتًا﴾ and 15:87 ﴿وَٱلْقُرْءَانَ﴾) and +55
  token-anchored أصول markers, raising the corpus to 1,615 variants and 7,272 rulings. Added a
  red/green page-266 regression covering both variant words and the adjacent Warsh marker, and
  documented that a detail-table row must become a source → fixture → word-marker record before
  it is visible. Universal tajwid and self-contradictory source statements remain deliberately
  uncoloured. Files: `scripts/qiraat/{data_variants,data_rulings}.py`, generated page/ruling
  fixtures 249–267, `packages/qiraat-core/engine.test.mjs`, `docs/qiraat/05-ui-ux.md`,
  `CHANGELOG.md`, `CLAUDE.md`, `HANDOFF.md`. No database migration.
- **Fix — multi-reader Qiraat gradients in the Mushaf:** centralised marker paint with
  `markerPaintForWord()` so segmented comparison markers use a text-clipped CSS gradient instead
  of falling back to normal ink; their underline shares the same gradient. Reader-specific أصول
  colours, annotations, and all existing solid markers retain their established precedence.
  Added a red/green engine regression test and documented the rendering invariant in
  `docs/qiraat/05-ui-ux.md` and `PROJECT_MASTER.md`. Verified with Qiraat engine tests,
  TypeScript, Qiraat validation, and Chrome page-249 inspection with zero console errors. No DB
  migration.
- **Fix — Qiraat colour coverage, pages 249–267:** filled the missing reader-specific أصول records on pages that had imported variants but no word-colour fixtures; every imported variant page now has loadable أصول coverage. Added the mandatory data-to-word-colour contract to `docs/qiraat/05-ui-ux.md` and `PROJECT_MASTER.md`. Verified generation, data linting, and all direct repository loads for pages 249–267; no database write.
- **Feature — Qiraat pages 249–267 (الرعد، إبراهيم، الحجر):** added 57 fully partitioned variant loci and 52 token-anchored reader-specific أصول rulings from the supplied tables; generated and loader-wired all fixture pages, including empty-but-addressable transition pages. Current fixture corpus: 1,613 variants and 7,190 rulings across 318 active pages. Universal tajwid and internally contradictory rows were not imported. Verification: both Python generators and `npm run qiraat:validate` clean; no DB migration or database population.
- **Feature — سورة النور (24) imported in full from the supplied ayah-by-ayah table: pages 350–359, +48 variants and +178 أصول rulings:** processed An-Nur 1–64 (and Al-Furqan 1–2 on page 359) into the established fixture model, bringing the combined dataset to **1,556 variants and 7,138 rulings across 299 active pages** (1–244 and 305–359; 305 pages remaining of 604). Every variant locus partitions the 20 Riwayat exactly once; every record is anchored to a real Mushaf-1441 token, all generated `baseText` remains verbatim, and no new `NEEDS_MANUAL_REVIEW` record was added. Handled source slips, disambiguations, and rasm normalizations: 24:6 & 24:8 ﴿أَرْبَعُ شَهَـٰدَٰتٍ﴾ (Hafs baseline with raf' per Shatibiyyah 912 `«وَأَرْبَعُ أَوَّلاَ صِحَابٌ»` for Hafs, Hamza, Kisai, Khalaf 10; nasb for REST; consensus on nasb in 24:8); 24:7 & 24:9 ﴿وَٱلْخَـٰمِسَةُ / وَٱلْخَـٰمِسَةَ﴾ (Hafs alone reads nasb in 24:9 `وَٱلْخَـٰمِسَةَ` per Shatibiyyah 913 `«وَغَيْرُ الْحَفْصِ خَامِسَةُ الأَخِيرُ»`; REST read raf' `وَٱلْخَـٰمِسَةُ`; Nafi and Ya'qub read `أَنْ غَضِبَ`); 24:20 ﴿رَءُوفٌۢ﴾ (qasr `رَؤُفٌ` for Suhbah, Abu Amr, Ya'qub, Khalaf 10 per Shatibiyyah 512; madd baseline for Hafs + REST); 24:21 ﴿خُطُوَٰتِ﴾ (sukun `خُطْوَٰتِ` for Hamza, Khalaf 10); 24:24 ﴿تَشْهَدُ عَلَيْهِمْ﴾ (yaa `يَشْهَدُ` for Hamza, Kisai, Khalaf 10 per Shatibiyyah 914); 24:31 ﴿جُيُوبِهِنَّ﴾ (kasr for Ibn Kathir, Ibn Dhakwan, Hamza, Kisai, Khalaf 10), ﴿غَيْرِ أُو۟لِى﴾ (nasb `غَيْرَ` for Ibn Amir, Shu'ba, Abu Ja'far), and ﴿أَيُّهَ ٱلْمُؤْمِنُونَ﴾ (3-way partition: damm on haa وصلاً for Ibn Amir `أَيُّهُ`, alif وقفاً for Abu Amr, Kisai, Ya'qub `أَيُّهَا`, fat-h/haa for Hafs + REST); 24:40 ﴿سَحَابٌ ظُلُمَـٰتٌ﴾ (3-way partition per Shatibiyyah 917: Al-Bazzi idafa without tanween `سَحَابُ ظُلُمَـٰتٍۢ`, Qunbul tanween and jar `سَحَابٌۭ ۚ ظُلُمَـٰتٍۢ`, Hafs + REST tanween and raf' `سَحَابٌۭ ۚ ظُلُمَـٰتٍۢ`); dropped hallucinated prompt rows for 24:41 (consensus ghayb `يفعلون`) and 24:53 (consensus khitab `تعملون`); 24:51 ﴿قَوْلَ﴾ (consensus nasb among the 10 Minor Readings); 24:55 ﴿ٱسْتَخْلَفَ﴾ (Shu'ba alone `ٱسْتُخْلِفَ`, 19 vs 1 partition) and ﴿وَلَيُبَدِّلَنَّهُم﴾ (takhfeef for Ibn Kathir, Shu'ba, Ya'qub); 24:57 ﴿تَحْسَبَنَّ﴾ (3-way partition: Ibn Amir & Hamza `يَحْسَبَنَّ`, Asim & Abu Ja'far `تَحْسَبَنَّ`, REST `تَحْسِبَنَّ`); 24:58 ﴿ثَلَـٰثُ عَوْرَٰتٍۢ﴾ (nasb for Shu'ba, Hamza, Kisai, Khalaf 10; raf' for Hafs + REST); 24:61 ﴿بُيُوتِكُمْ﴾ and ﴿بُيُوتًا﴾ modeled with `all_occurrences=True`; 24:64 ﴿يُرْجَعُونَ﴾ (fat-h `يَرْجِعُونَ` for Ibn Kathir, Abu Amr, Shu'ba, Ya'qub). Added page/ruling fixtures and repository loaders for 350–359. Verification: generators clean; `validate-qiraat-data.mjs` clean (1,556/7,138/299, 19 categories, 4 pre-existing flags); 30/30 Qiraat tests; seven Mushaf validators; TypeScript clean; Next.js production build clean (31/31 routes). Files: `scripts/qiraat/{data_variants,data_rulings}.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/{pages,rulings}/page-{350..359}.json`, `PROJECT_MASTER.md`, `HANDOFF.md`, `CLAUDE.md`, `CHANGELOG.md`. **DB migration/population:** none; Maryam through An-Nur remain fixture-only.

## 2026-09-19
- **Feature — سورة المؤمنون (23) imported in full from the supplied ayah-by-ayah table: pages 342–349, +44 variants and +171 أصول rulings:** processed Al-Mu'minun 1–118 into the established fixture model, bringing the combined dataset to **1,508 variants and 6,960 rulings across 289 active pages** (1–244 and 305–349). Every variant locus partitions the 20 Riwayat exactly once; every record is anchored to a real Mushaf-1441 token, all generated `baseText` remains verbatim, and no new `NEEDS_MANUAL_REVIEW` record was added. Handled source slips and rasm normalizations: corrected 23:52 ﴿وَإِنَّ هَـٰذِهِۦٓ﴾ where the source stated Hafs read with fat-h (in Hafs rasm it is with kasrah `وَإِنَّ`, baseline with kasrah/tashdeed for Hafs, Hamza, Kisai, Khalaf 10; variant 1 fat-h/tashdeed for Nafi, Abu Amr; variant 2 kasrah/takhfeef for REST); 23:85 ﴿تَذَكَّرُونَ﴾ where source claimed Hafs read with takhfeef (Hafs reads tashdeed `تَذَكَّرُونَ`, takhfeef for Hamza, Kisai, Khalaf 10); 23:92 ﴿عَـٰلِمِ﴾ where source claimed Asim read by raf' (Hafs reads by khafd `عَـٰلِمِ` for Ibn Kathir, Abu Amr, Ibn Amir, Hafs, Ya'qub; raf' `عَـٰلِمُ` for REST); 23:106 ﴿شِقْوَتُنَا﴾ where source included Hafs in `شَقَاوَتُنَا` (Hafs reads `شِقْوَتُنَا` with kasr sheen and sukun qaf; `شَقَـٰوَتُنَا` for Hamza, Kisai, Khalaf 10); normalized rasm anchors for 23:36 ﴿۞ هَيْهَاتَ هَيْهَاتَ﴾, 23:50 ﴿وَءَاوَيْنَـٰهُمَآ﴾, 23:50 ﴿مَّعِينٍۢ﴾, 23:56 ﴿نُسَارِعُ﴾, 23:66 ﴿ءَايَـٰتِى﴾, 23:72 ﴿أَمْ تَسْـَٔلُهُمْ﴾, 23:78 ﴿وَٱلْأَفْـِٔدَةَ﴾, 23:83 ﴿وَءَابَآؤُنَا﴾, 23:91 ﴿فَتَعَـٰلَى﴾, 23:112 ﴿قَـٰلَ كَمْ﴾, 23:114 ﴿قَـٰلَ إِن لَّبِثْتُمْ﴾. Added page/ruling fixtures and repository loaders for 342–349. Verification: generators clean; `validate-qiraat-data.mjs` clean (1,508/6,960/289, 19 categories, 4 pre-existing flags); 30/30 Qiraat tests; seven Mushaf validators; TypeScript clean; Next.js production build clean (31/31 routes). Files: `scripts/qiraat/{data_variants,data_rulings}.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/{pages,rulings}/page-{342..349}.json`, `PROJECT_MASTER.md`, `HANDOFF.md`, `CLAUDE.md`, `CHANGELOG.md`. **DB migration/population:** none; Maryam through Al-Mu'minun remain fixture-only.
- **Feature — سورة الحج (22) imported in full from the supplied ayah-by-ayah table: pages 332–341, +37 variants and +173 أصول rulings:** processed Al-Hajj 1–78 into the established fixture model, bringing the combined dataset to **1,464 variants and 6,789 rulings across 281 active pages** (1–244 and 305–341). Every variant locus partitions the 20 Riwayat exactly once; every record is anchored to a real Mushaf-1441 token, all generated `baseText` remains verbatim, and no new `NEEDS_MANUAL_REVIEW` record was added. Handled source swaps, Duri imalah conditions, and rasm normalizations: resolved swapped rows for 22:62 ﴿يَدْعُونَ﴾ (Hafs baseline الغيب, variant الخطاب) and 22:73 ﴿تَدْعُونَ﴾ (Hafs baseline الخطاب, variant الغيب); enforced strict majroor condition for Duri an Abi Amr imalah in ﴿ٱلنَّاسِ﴾ (22:3, 8, 11, 18, 25, 27, 65, 75, 78); normalized rasm anchors for 22:4 ﴿فَأَنَّهُۥ﴾, 22:26 ﴿بَيْتِىَ لِلطَّآئِفِينَ﴾, 22:30 ﴿ٱلْأَوْثَـٰنِ﴾, 22:52 ﴿ءَايَـٰتِهِۦ﴾, 22:72 ﴿ءَايَـٰتُنَا﴾. Added page/ruling fixtures and repository loaders for 332–341. Verification: generators clean; `validate-qiraat-data.mjs` clean (1,464/6,789/281, 19 categories, 4 pre-existing flags); 30/30 Qiraat tests; seven Mushaf validators; TypeScript clean; Next.js production build clean (31/31 routes). Files: `scripts/qiraat/{data_variants,data_rulings}.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/{pages,rulings}/page-{332..341}.json`, `PROJECT_MASTER.md`, `HANDOFF.md`, `CLAUDE.md`, `CHANGELOG.md`. **DB migration/population:** none; Maryam, Taha, Al-Anbiya, and Al-Hajj remain fixture-only.
- **Feature — سورة الأنبياء (21) imported in full from the supplied ayah-by-ayah table: pages 322–331, +35 variants (31 loci) and +157 أصول rulings:** processed Al-Anbiya 1–112 into the established fixture model, bringing the combined dataset to **1,427 variants and 6,616 rulings across 271 active pages** (1–244 and 305–331). Every variant locus partitions the 20 Riwayat exactly once; every record is anchored to a real Mushaf-1441 token, all generated `baseText` remains verbatim, and no new `NEEDS_MANUAL_REVIEW` record was added. Handled source slips and rasm normalizations: 21:7 ﴿نُّوحِىٓ إِلَيْهِمْ ۖ﴾ corrected from source conflation with 21:25 (Hafs alone reads بالنون وكسر الحاء, 19 Riwayat read يُوحَىٰٓ إِلَيْهِمْ per Shatibiyyah 887); 21:25 ﴿نُوحِىٓ إِلَيْهِ﴾ (Hafs, Hamza, Kisai, Khalaf 10 read نُوحِىٓ إِلَيْهِ, 13 Riwayat read يُوحَىٰٓ إِلَيْهِ); 21:4 ﴿قَالَ رَبِّى﴾ and 21:112 ﴿قَـٰلَ رَبِّ ٱحْكُم﴾ (Hafs alone in 21:112 reads قَالَ ماضي, REST read قُل أمر); 21:67 ﴿أُفٍّۢ لَّكُمْ﴾ partitioned cleanly 5-6-9 into أُفٍّۢ / أُفَّ / أُفِّ. Added page/ruling fixtures and repository loaders for 322–331. Verification: generators clean; `validate-qiraat-data.mjs` clean (1,427/6,616/271, 19 categories, 4 pre-existing flags); 30/30 Qiraat tests; seven Mushaf validators; TypeScript clean; Next.js production build clean (31/31 routes). Files: `scripts/qiraat/{data_variants,data_rulings}.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/{pages,rulings}/page-{322..331}.json`, `PROJECT_MASTER.md`, `HANDOFF.md`, `CLAUDE.md`, `CHANGELOG.md`. **DB migration/population:** none; Maryam, Taha, and Al-Anbiya remain fixture-only.
- **Feature — سورة طه (20) imported in full from the supplied ayah-by-ayah table: pages 312–321, +62 variants and +322 أصول rulings:** processed Taha 1–135 into the established fixture model, bringing the combined dataset to **1,392 variants and 6,459 rulings across 261 active pages** (1–244 and 305–321). Page 312 now safely carries the Maryam/Taha transition without replacing its seven existing Maryam rulings. Every variant locus partitions the 20 Riwayat exactly once; every record is anchored to a real Mushaf-1441 token, all generated `baseText` remains verbatim, and no new `NEEDS_MANUAL_REVIEW` record was added. Universal tajwid, ordinary madd amounts, ميم الجمع, and rows explicitly agreed by all readers were omitted under the documented Maryam classification rule; contradictory/self-correcting clauses were omitted rather than guessed. Added page/ruling fixtures and repository loaders for 313–321; updated page 312's mixed-source metadata. Verification: generators clean; `qiraat:validate` clean (1,392/6,459/261, 19 categories, 4 pre-existing flags); 30/30 Qiraat tests; seven Mushaf validators; TypeScript clean; Next.js production build clean (31/31 routes). Files: `scripts/qiraat/{data_variants,data_rulings}.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/{pages,rulings}/page-{312..321}.json`, `PROJECT_MASTER.md`, `HANDOFF.md`, `CLAUDE.md`, `CHANGELOG.md`. **DB migration/population:** none; Maryam and Taha remain fixture-only.
- **Integration state after reconciling the parallel page batches:** the committed fixture set now contains **1,330 variants and 6,137 أصول rulings across 252 pages**: contiguous pages 1–244 plus Maryam pages 305–312. `SAKT` from the Maryam source brings the combined fixture set to 19 active ruling families; pages 245–304 remain the only gap before Maryam. The self-hosted PostgreSQL population remains intentionally scoped to contiguous pages 1–244 (1,278 variants, 5,969 rulings, 7,247 total entries); Maryam remains fixture-only. This line records the post-rebase current state; the two detailed entries below retain their point-in-time verification counts.
- **Feature — سورة مريم (19) imported in full: Mushaf pages 305–312, +52 variants and +168 أصول rulings, from a source that is not the PDF — and the page table stops pretending to be contiguous:** the first batch supplied as an **ayah-by-ayah table** (فرش الكلمات + الأصول المطردة + الشواهد for 19:1–98) rather than as page images of «مصحف القراءات العشر». **(1) Data.** All 98 ayat processed into **52 new `QiraatVariant` records** (1,232 total after merging the parallel 42–224 batches) and **168 new `QiraatRuling` records** (5,544 total after that merge) across pages 305–312. Every locus is anchored by `scripts/qiraat/tokens.py` to a real Mushaf-1441 token and every `baseText` is verbatim from the page-word fixtures; **every one of the variant loci partitions the 20 Riwayat exactly once**, and the batch adds **zero** new `NEEDS_MANUAL_REVIEW` records. The فرش: ﴿زَكَرِيَّا﴾ at both 19:2 and 19:7 (القصر vs المد والهمز, stored as two separate checked records because the إعراب differs), ﴿يَرِثُنِى وَيَرِثُ﴾ (الرفع vs الجزم), ﴿عِتِيًّا﴾/﴿جِثِيًّا﴾/﴿صِلِيًّا﴾ (كسر vs ضم), ﴿خَلَقْتُكَ﴾/﴿خَلَقْنَاكَ﴾, ﴿لِأَهَبَ﴾/﴿لِيَهَبَ﴾, ﴿مِتُّ﴾/﴿مُتُّ﴾ at 19:23 and 19:66, ﴿نَسْيًا﴾, ﴿مِن تَحْتِهَا﴾/﴿مَن تَحْتَهَا﴾, ﴿تُسَاقِطْ﴾ (three أوجه: حفص، حمزة، الباقون), ﴿قَوْلَ ٱلْحَقِّ﴾, ﴿فَيَكُونَ﴾, ﴿وَإِنَّ﴾/﴿وَأَنَّ﴾, ﴿يَا أَبَتِ﴾ ×4, ﴿مُخْلَصًا﴾, ﴿ٱلنَّبِيِّـۧنَ﴾, ﴿وَبُكِيًّا﴾, ﴿يَدْخُلُونَ﴾, ﴿نُورِثُ﴾/﴿نُوَرِّثُ﴾ (رويس وحده), ﴿أَءِذَا﴾ (ابن ذكوان ذو وجهين), ﴿يَذَّكَّرُ﴾ (أبو جعفر وحده), ﴿نُنَجِّى﴾, ﴿مَقَامًا﴾, ﴿وَرِءْيًا﴾, ﴿أَفَرَءَيْتَ﴾, ﴿وَلَدًا﴾/﴿وُلْدًا﴾ at all four positions, ﴿تَكَادُ﴾/﴿يَكَادُ﴾, ﴿يَتَفَطَّرْنَ﴾/﴿يَنفَطِرْنَ﴾, plus the عليهم/إليهم and the يعقوب-only هاء الكناية loci — the first reusing the exact shape pages 1–20 already use, so one locus can never disagree with itself across the Mushaf. **(2) The source names itself now.** A page spec may carry `src=` and its records then say where they came from instead of borrowing a PDF page nobody read; `build_variants.source_of()` keeps the old PDF shape for every page that came from the PDF. **(3) The page table is no longer contiguous, deliberately.** 225–304 are still unimported, so 224 → 305 is a legal jump — the loader table, the linter and `build_rulings.py` are all data-driven and none of them assumes a dense range (the 22–41 batch is exactly what made that true). Merged on top of the pages 42–224 batches that landed on `main` in parallel; regenerating every fixture afterwards was a clean no-op on those pages, and the one duplicate شاهد constant the two sides both introduced (`D_SIRAT`) was collapsed onto the existing definition. **(4) Classification rules, now written up in `PROJECT_MASTER.md` §12.5 for the next ayah-by-ayah batch.** A row that holds for **all ten** readers (مقادير المد، القلقلة، الإدغام الشمسي، الإقلاب، كسر الساكن للساكنين، إبدال التنوين ألفًا وقفًا، صلة الهاء بين متحركين) is **not imported** — the أصول layer marks who *differs*, and a universal rule would colour half the page while telling the reader nothing. ميم الجمع is left out entirely because this dataset has no such family and one surah does not justify a 19th colour. A سكت وصلًا goes to `SAKT` — a category that existed since the first import but had never been used, so the dataset now has 19 families in use, not 18 — while وقف حمزة stays in `WAQF_HAMZA`; ورش's النقل goes to `TAGHYIR_HAMZ` as a تغيير همز rather than inventing a family for it. And a row is classified **by its nature, not by the column the source printed it in**: the source puts ياءات الإضافة and الإمالة in its فرش column, and they are still rulings here. **(5) Five source rows dropped rather than guessed, and two imported against the source's own aside** — all seven listed in §12.5 as the first things to check on the paper original. Dropped: 19:2 إمالة ﴿زَكَرِيَّا﴾ (the row is headed «لمن قرأ بالقصر» then lists ورش, who reads it بالهمز والمد); 19:4 ترقيق الراء في ﴿ٱلرَّأْسُ﴾ (the source asks and answers itself, «؟ بل الراء مفتوحة فلا تُرقَّق»); 19:25 الإدغام الكبير (the row ends «لا إدغام هنا لعدم توفر الشروط»); 19:46 غنة النون عند اللام (النون عند اللام بغير غنة للعشرة جميعًا); and 19:38 ﴿وَأَبْصِرْ﴾ + 19:62 ﴿رِزْقُهُمْ﴾ (the source itself says «لجميع القراء»). Imported against the aside: **19:1 ﴿كهيعص﴾**, whose إمالة row names الدوري عن الكسائي inside a group that already holds الكسائي whole and then retracts it («والمشهور عن الكسائي إمالتهما معًا كحمزة») — taken على المشهور, which is also the **only** reading under which the five إمالة groups partition the twenty exactly once (6+1+2+5+6), with the aside kept in the row's note; and 19:21 ﴿لِّلنَّاسِ﴾, `؟`-marked but followed by «وَخُلْفُهُ فِي النَّاسِ فِي الْجَرِّ حُصِّلَا», so it ships with the خلف. **(6) One anchor rejection in the whole batch**, and it was the fourth shape §12.4b already predicts: the source quotes ﴿إِسْرَائِيلَ﴾ where the token is ﴿وَإِسْرَٰٓءِيلَ﴾. **Verification:** `qiraat:validate` clean across all 232 pages (1,232 variants, 5,544 rulings, 4 flagged — unchanged), 30/30 engine tests, `tsc --noEmit` clean, `next build` clean, all 7 Mushaf validators, and the API returning every page's records (305: 6+32, 306: 8+14, 307: 5+25, 308: 7+24, 309: 6+23, 310: 11+21, 311: 9+22, 312: 0+7 — 19:96–98 carry «لا خلاف فرشي» so page 312 has variants but no فرش, and its أصول rows all land in surah 19, none in the طه that shares the page). Playwright/Chromium at 1440×950 walking 305 → 312: **9/9 checks, zero real page errors** (the 401s are the annotations API with no signed-in account in this sandbox), 35–53 أصول-coloured words per spread in 9–13 distinct family colours and 9–17 variant-marked words, and clicking ﴿أَفَرَءَيْتَ﴾ (19:77) explaining it correctly as «خلاف في الرسم → أَفَرَيْتَ، الإمام الكسائي» plus «تغيير الهمز → الراوي قالون، الإمام أبو جعفر، تسهيل الهمزة الثانية» and ورش's second وجه. As with every batch so far, nothing here claims `VERIFIED`; it ships at `REVIEWED`, visible only through the include-reviewed path, pending the paper original. Files: `scripts/qiraat/{data_variants,data_rulings,build_variants}.py`, `packages/qiraat-core/fixtures/pages/page-3{05..12}.json` (new), `packages/qiraat-core/fixtures/rulings/page-3{05..12}.json` (new), `packages/qiraat-core/repository.ts`, `PROJECT_MASTER.md`, `HANDOFF.md`. **DB migration:** none — fixture-only, same as every Qiraat batch.
- **Feature & Database Population — Qiraat pages 225–244 imported (Juz' 12, هود ٢٩ – يوسف ٧٨): +98 variants (1,278 total) and +593 أصول rulings (5,969 total), with contiguous pages 1–244 and PostgreSQL 17 ingestion (7,247 entries, 22,224 reading assertions):** completed Antigravity's interrupted twelfth batch from the supplied page-by-page extraction. All 98 فرش records partition the 20 Riwayat without gaps or overlaps; all 593 rulings resolve to genuine Mushaf-1441 page-word tokens across the established 18 categories; no new `NEEDS_MANUAL_REVIEW` record was introduced (4 total unchanged). Corrected the supplied page-242 heading to Yusuf 53–63 because ﴿أَبِيهِمْ﴾ and ﴿نَكْتَلْ﴾ are both 12:63; kept page 240 as an أصول-only page (0 variants, 35 rulings); aligned page 244 ﴿نَرْفَعُ دَرَجَـٰتٍ مَّن نَّشَاءُ﴾ to the Hafs tanween baseline and modeled the idafa/Ya'qub forms separately. Omitted question-marked artifacts instead of guessing (page 225 ﴿قَوْمًا تَجْهَلُونَ؟﴾; page 239 ﴿رَءَايَةً؟﴾ / ﴿نَبِّئْتُكُمَا؟﴾), while retaining the unambiguous neighboring rulings. Added and wired 20 variant fixtures plus 20 ruling fixtures in `packages/qiraat-core/repository.ts`; updated the canonical Python datasets and generalized the PostgreSQL importer's stale page-range docstring. Populated the existing V2 schema to 244 `qiraat_pages`, 7,038 `qiraat_loci`, 7,247 `qiraat_entries`, 22,224 `qiraat_entry_readings`, 752 evidence texts, and 1,696 evidence links; verified `qiraat_export_page(244::smallint, true)` and issued `NOTIFY pgrst, 'reload schema'`. Verification: `node scripts/validate-qiraat-data.mjs` and `npm run test:qiraat` clean (30/30 tests; 1,278 variants, 5,969 rulings, 18 categories); `npm run mushaf:validate` passed all 7 validators; Python compile + all 40 JSON arrays passed; `npx tsc --noEmit --incremental false -p .` clean; `env -u __NEXT_PROCESSED_ENV npm run build` clean (31/31 routes); local production Playwright on pages 225 and 244 rendered 40 and 41 Qiraat markers respectively with 0 page errors, 0 console errors, and 0 `/api/mushaf-1441/qiraat` requests. Files: `scripts/qiraat/data_variants.py`, `scripts/qiraat/data_rulings.py`, `scripts/qiraat/import_to_postgres.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/pages/page-{225..244}.json`, `packages/qiraat-core/fixtures/rulings/page-{225..244}.json`, `PROJECT_MASTER.md`. **DB migration:** none — data population into the existing V2 schema.
- **Feature & Database Population — Qiraat pages 205–224 imported (Juz' 11 & early Juz' 12, التوبة ١١٢ – هود ٢٨) completing Surah Yunus: +107 variants (1,180 total across 224 pages) and +498 أصول rulings (5,376 total), with 100% contiguous coverage of Juz' 1–11 and full PostgreSQL 17 ingestion (6,556 entries, 19,899 readings):** the eleventh major batch of the Ten Minor Readings (القراءات العشر الصغرى), completing the contiguous sequence of pages 1–224 (Juz' 1–11 and early Juz' 12). **(1) Data Ingestion.** 107 new variant records (1,180 total across 224 pages) and 498 new `QiraatRuling` records (5,376 total) across all 18 established أصول categories. Every locus is resolved and anchored by `scripts/qiraat/tokens.py` to genuine Mushaf-1441 page-word tokens, partitions the 20 Riwayat without gap or overlap, and introduces zero new `NEEDS_MANUAL_REVIEW` records (4 total unchanged). **(2) Token Anchor Normalizations & Disambiguations Resolved.** Dagger alefs, small high letters, and rasm normalizations: page 205 ﴿لِلنَّبِىِّ – ٱلنَّبِىِّ﴾ (9:113, 117) Nafi with hamza & madd muttasil, ﴿إِبْرَٰهِيمَ﴾ (9:114) Hisham with alef, ﴿ٱلْعُسْرَةِ﴾ (9:117) Abu Ja'far damma on seen, ﴿كَادَ يَزِيغُ﴾ (9:117) Hamzah, Hafs, Ibn Kathir with taa `تَزِيغُ`; page 207 ﴿يَلُونَكُم﴾ (9:123) damma on laam for Ibn Kathir, ﴿يَفْقَهُونَ﴾ (9:127) Abu Ja'far with taa `تَفْقَهُونَ`; page 208 Yunus 1–6: ﴿الٓر ۚ﴾ (10:1) Abu Ja'far sakt on huruf tahajji, ﴿لَسَـٰحِرٌ﴾ (10:2) Sahir vs Sihr, ﴿فَصَّلَ ٱلْـَٔايَـٰتِ﴾ (10:5) Nafi, Abu Amr, Hafs, Abu Ja'far with nun `نُفَصِّلُ`; page 212 omitted Hud table artifacts ﴿ٱلْأَخْسَرُونَ﴾ and ﴿يَسْتَغْشُونَ﴾ accidentally included in Yunus extraction; page 213 ﴿لَّا يَهِدِّىٓ﴾ (10:35) Hafs baseline aligned with rasm `لَّا يَهِدِّىٓ`, ﴿يَعْزُبُ﴾ (10:61) Hamzah, Kisai, Khalaf 10 kasr on zaay; page 215 ﴿قِطَعًا﴾ (10:27) Ibn Kathir, Kisai sukun on taa `قِطْعًا`, ﴿أَصْغَرَ﴾ and ﴿أَكْبَرَ﴾ (10:61) Hamzah, Khalaf 10 rafa'; page 216 ﴿مَتَـٰعُ﴾ (10:23) Hafs nasb `مَتَـٰعَ`; page 219 ﴿وَلَا تَتَّبِعَآنِّ﴾ (10:89) corrected prompt inversion: Hafs and majority read `وَلَا تَتَّبِعَآنِّ` (tashdeed nun maksoorah + madd mushabba') matching rasm baseline, Ibn Dhakwan alone reads `وَلَا تَتَّبِعَانِ` (takhfeef); page 220 ﴿نُنَجِّى﴾ (10:103) Kisa'i, Ya'qub with single nun and sukun `نُنْجِ`; page 221 transition page spanning Surah 10 (Yunus 107–109) and Surah 11 (Hud 1–5): `PAGES[221]` defined with `surah=10, af=107, at=5`, modeled ﴿وَهُوَ﴾ with `all_occurrences=True` matching all 3 positions (10:107, 10:109, 11:4); page 222 ﴿سِحْرٌۭ﴾ (11:7) Hamzah, Kisai, Khalaf 10 read `سَـٰحِرٌۭ`; page 224 ﴿يُضَـٰعَفُ﴾ (11:20) Ibn Kathir, Ibn Amir, Abu Ja'far, Ya'qub read `يُضَعَّفُ` without alef and with tashdeed on 'ayn. **(3) Repositories & Fixtures.** Generated 20 new variant fixture files (`packages/qiraat-core/fixtures/pages/page-{205..224}.json`) and 20 new ruling fixture files (`packages/qiraat-core/fixtures/rulings/page-{205..224}.json`). Connected loaders in `packages/qiraat-core/repository.ts` under both `PAGE_VARIANT_LOADERS` and `PAGE_RULING_LOADERS`. **(4) PostgreSQL Database Ingestion.** Populated self-hosted PostgreSQL 17 container (`mutshabehat-db`) with all 224 pages: 224 `qiraat_pages`, 6,363 `qiraat_loci`, 6,556 `qiraat_entries` (1,180 variants, 5,376 rulings), 19,899 `qiraat_entry_readings`, 699 `qiraat_evidence_texts`, and 1,574 `qiraat_evidence_links`. Executed `NOTIFY pgrst, 'reload schema'`. **(5) Verification.** `node scripts/validate-qiraat-data.mjs` clean across all 224 pages (1,180 variants, 5,376 rulings, 18 usul categories, 4 flagged — unchanged); 30/30 Qiraat engine tests passed (`npm run test:qiraat`); `npm run mushaf:validate` passed all 7 test suites; `npx tsc --noEmit --incremental false -p .` clean (0 errors); `npm run build` clean (31/31 routes compiled). Files: `scripts/qiraat/data_variants.py`, `scripts/qiraat/data_rulings.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/pages/page-{205..224}.json` (20 files), `packages/qiraat-core/fixtures/rulings/page-{205..224}.json` (20 files). **DB migration:** none — data population into existing V2 schema.
- **Feature & Database Population — Qiraat pages 185–204 imported (Juz' 10 & early Juz' 11, الأنفال ٦٢ – التوبة ١١١) completing Juz' 10: +72 variants (1,073 total across 204 pages) and +441 أصول rulings (4,878 total), with 100% contiguous coverage of Juz' 1–10 and full PostgreSQL 17 ingestion (5,951 entries, 17,988 readings):** the tenth major batch of the Ten Minor Readings (القراءات العشر الصغرى), completing the contiguous sequence of pages 1–204 (Juz' 1–10 and early Juz' 11). **(1) Data Ingestion.** 72 new variant records (1,073 total across 204 pages) and 441 new `QiraatRuling` records (4,878 total) across all 18 established أصول categories. Every locus is resolved and anchored by `scripts/qiraat/tokens.py` to genuine Mushaf-1441 page-word tokens, partitions the 20 Riwayat without gap or overlap, and introduces zero new `NEEDS_MANUAL_REVIEW` records (4 total unchanged). **(2) Token Anchor Normalizations & Disambiguations Resolved.** Dagger alefs, small high letters (`ۥ` / `ۦ`), and rasm normalizations: page 185 ﴿بِنَصْرِهِۦ وَبِٱلْمُؤْمِنِينَ﴾ (8:62) `ۦ` expanded to `ي` (`'بنصرهي'`); page 186 ﴿ٱلۡأَسۡرَىٰ﴾ (8:70) combined Abu Amr's variant `الأسارى` with base imalah on `الاسرى`, ﴿ٱلْمُؤْمِنُونَ﴾ (8:74); page 188 ﴿ذِمَّةًۭ ۚ وَأُو۟لَـٰٓئِكَ﴾ (9:10); page 189 At-Tawbah 14–20 omitted duplicate ﴿تُطَهِّرُهُمْ﴾ (which belongs to 9:103 on page 203); page 190 ﴿مِّنْهُ﴾ (9:21), ﴿عَلَىٰ رَسُولِهِۦ وَعَلَى ٱلْمُؤْمِنِينَ﴾ (9:26) anchored to `'المؤمنين'`, ﴿بِأَمْرِهِۦ ۗ﴾ (9:24) `'بامرهي'`; page 192 ﴿بِٱلْهُدَىٰ﴾ (9:33); page 193 ﴿وَكَلِمَةُ ٱللَّهِ﴾ (9:40); page 195 ﴿ٱئْذَن لِّى وَلَا﴾ (9:49) assigned all ten readers for agreed ya'at idafa; page 196 ﴿هُوَ أُذُنٌۭ﴾ and ﴿قُلْ أُذُنُ خَيْرٍۢ﴾ (9:61) modeled as two distinct loci; page 197 ﴿وَءَايَـٰتِهِۦ﴾ (9:65) `'وءايتهۦ'`, ﴿إِن نَّعْفُ﴾ (9:66) `'ان نعف'` with note for Hamzah on reading `إن يُعف`; page 199 ﴿وَٱغْلُظْ عَلَيْهِمْ﴾ (9:73); page 200 pure usul (0 variants) with 17 rulings, ﴿ٱسْتَـْٔذَنَكَ﴾ (9:86); page 202 ﴿نُّؤْمِنَ لَكُمْ﴾ (9:94); page 203 ﴿صَلَوٰتَكَ﴾ (9:103) baseline text aligned with Mushaf-1441 rasm; page 204 ﴿أَسَّسَ بُنْيَـٰنَهُۥ﴾ (9:109 occurrences 1 and 2 under locus group `L204-اسس_بنيانه`). **(3) Repositories & Fixtures.** Generated 20 new variant fixture files (`packages/qiraat-core/fixtures/pages/page-{185..204}.json`) and 20 new ruling fixture files (`packages/qiraat-core/fixtures/rulings/page-{185..204}.json`). Connected loaders in `packages/qiraat-core/repository.ts` under both `PAGE_VARIANT_LOADERS` and `PAGE_RULING_LOADERS`. **(4) PostgreSQL Database Ingestion.** Populated self-hosted PostgreSQL 17 container (`mutshabehat-db`) with all 204 pages: 204 `qiraat_pages`, 5,774 `qiraat_loci`, 5,951 `qiraat_entries` (1,073 variants, 4,878 rulings), 17,986 `qiraat_entry_readings`, 647 `qiraat_evidence_texts`, and 1,431 `qiraat_evidence_links`. Executed `NOTIFY pgrst, 'reload schema'`. **(5) Verification.** `node scripts/validate-qiraat-data.mjs` clean across all 204 pages (1,073 variants, 4,878 rulings, 18 usul categories, 4 flagged — unchanged); 30/30 Qiraat engine tests passed (`npm run test:qiraat`); `npm run mushaf:validate` passed all 7 test suites; `npx tsc --noEmit --incremental false -p .` clean (0 errors); `npm run build` clean (31/31 routes compiled). Files: `scripts/qiraat/data_variants.py`, `scripts/qiraat/data_rulings.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/pages/page-{185..204}.json` (20 files), `packages/qiraat-core/fixtures/rulings/page-{185..204}.json` (20 files). **DB migration:** none — data population into existing V2 schema.
- **Feature & Database Migration — Qiraat pages 166–184 imported (Juz' 9 & early Juz' 10, الأعراف ١٣١ – الأنفال ٦١) completing Juz' 9: +101 variants (1,001 total across 184 pages) and +469 أصول rulings (4,437 total), plus Qiraat V2 PostgreSQL Migration Applied & 100% Populated (5,438 entries, 16,538 readings):** the ninth major batch of the Ten Minor Readings (القراءات العشر الصغرى), completing the contiguous sequence of pages 1–184 (Juz' 1–9 and early Juz' 10). **(1) Data Ingestion.** 101 new variant records (1,001 total across 184 pages) and 469 new `QiraatRuling` records (4,437 total) across all 18 established أصول categories. Every locus is resolved and anchored by `scripts/qiraat/tokens.py` to genuine Mushaf-1441 page-word tokens, partitions the 20 Riwayat without gap or overlap, and introduces zero new `NEEDS_MANUAL_REVIEW` records (4 total unchanged). **(2) Token Anchor Normalizations & Disambiguations Resolved.** Dagger alefs, small high letters (`ۥ` / `ۦ`), and rasm normalizations: page 166 ﴿أَنجَيْنَـٰكُم﴾ (7:141); page 168 ﴿حُلِيِّهِمْ﴾ (7:148) and Yaqub ضم الهاء on ﴿يَهْدِيهِمْ﴾ and ﴿أَيْدِيهِمْ﴾; page 170 disambiguated multiple occurrences of `عليهم` in 7:157 (occurrence 1 ﴿عَلَيْهِمُ ٱلْخَبَـٰٓئِثَ﴾ before sakin vs occurrence 2 ﴿عَلَيْهِمْ ۚ﴾ before mutaharrik); page 172 ﴿ذُرِّيَّتَهُمْ﴾ (7:172) Hafs singular baseline vs plural; page 173 ﴿تَقُولُوا۟﴾ (7:172) and ﴿تَقُولُوٓا۟﴾ (7:173); page 175 ﴿طَـٰٓئِفٌۭ﴾ (7:201) Hafs baseline vs ﴿طَيْفٌ﴾; page 178 ﴿وَرِئَآءَ﴾ (8:47). Resolved cross-ayah multi-word spans (page 86 4:49–50 ﴿فَتِيلًا ٱنظُرْ﴾ and page 160 7:74–75 ﴿مُفْسِدِينَ قَالَ﴾). **(3) Repositories & Fixtures.** Generated 19 new variant fixture files (`packages/qiraat-core/fixtures/pages/page-{166..184}.json`) and 19 new ruling fixture files (`packages/qiraat-core/fixtures/rulings/page-{166..184}.json`). Connected loaders in `packages/qiraat-core/repository.ts` under both `PAGE_VARIANT_LOADERS` and `PAGE_RULING_LOADERS`. **(4) Database Backup & V2 Schema Migration.** Completed full `pg_dump -Fc` binary backup to `/Volumes/External Mini/Projects/apps/mutshabehat-selfhost/backups/pre-qiraat-v2-20260919091434.dump` (4.1 MB). Cleanly superseded the 6 obsolete prototype tables (`qiraat_attributions, qiraat_targets, qiraat_variants, qiraat_loci, qiraat_sources, qiraat_persons`) and applied `supabase/migrations/20260917120000_qiraat_v2_schema.sql` (19 tables, enums, triggers, and export functions) into self-hosted `mutshabehat-db`. Notified PostgREST schema reload (`NOTIFY pgrst, 'reload schema'`). **(5) PostgreSQL Bulk Ingestion.** Implemented `scripts/qiraat/import_to_postgres.py` populating all 184 pages: 184 `qiraat_pages`, 5,266 `qiraat_loci`, 5,438 `qiraat_entries` (1,001 variants, 4,437 rulings), 16,538 `qiraat_entry_readings`, 597 `qiraat_evidence_texts`, and 1,331 `qiraat_evidence_links`. Verified `qiraat_export_page()` on PostgreSQL 17. **(6) Verification.** `node scripts/validate-qiraat-data.mjs` clean across all 184 pages (1,001 variants, 4,437 rulings, 18 usul categories, 4 flagged — unchanged); 30/30 Qiraat engine tests passed (`npm run test:qiraat`); `npm run mushaf:validate` passed all 7 test suites; `npx tsc --noEmit --incremental false -p .` clean (0 errors). Files: `scripts/qiraat/data_variants.py`, `scripts/qiraat/data_rulings.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/pages/page-{166..184}.json` (19 files), `packages/qiraat-core/fixtures/rulings/page-{166..184}.json` (19 files), `scripts/qiraat/import_to_postgres.py`, `supabase/migrations/20260917120000_qiraat_v2_schema.sql`. **DB migration:** applied `20260917120000_qiraat_v2_schema.sql` with prior `pg_dump -Fc` backup.

## 2026-09-18
- **Feature — Qiraat pages 146–165 imported (Juz' 8, الأنعام ١٣٨ – الأعراف ١٣٠): +117 variants (900 total across 165 pages) and +513 أصول rulings (3,968 total), with zero anchor errors and 100% contiguous coverage of Juz' 1–8 (pages 1–165):** the eighth major batch of the Ten Minor Readings (القراءات العشر الصغرى), continuing the contiguous sequence of pages 1–165. **(1) Data.** 117 new variant records (900 total across 165 active pages) and 513 new `QiraatRuling` records (3,968 total) across the established 18 أصول categories. Every single locus is resolved and anchored by `scripts/qiraat/tokens.py` to genuine Mushaf-1441 page-word tokens, partitions the 20 Riwayat without gap or overlap, and introduces zero new `NEEDS_MANUAL_REVIEW` records (4 total unchanged). **(2) Token Anchor Normalizations & Alignments Resolved.** Handled multi-word, small-letter, and boundary queries: page 146 ﴿شُرَكَٰٓؤُاْ﴾ aligned to Hafs rasm ﴿شُرَكَآءُ ۚ﴾ (6:139); page 147 ﴿ٱلْحَوَايَآ﴾ (6:146); page 148 pure usul (0 variants) with 26 rulings, including ﴿كَذَٰلِكَ كَذَّبَ﴾ (6:148); page 149 ﴿بِـَٔايَـٰتِ﴾ and ﴿ءَايَـٰتِنَا﴾ (6:157), ﴿ٱلْعَذَابِ بِمَا﴾ (6:157); page 150 ﴿إِلَىٰ صِرَٰطٍۢ مُّسْتَقِيمٍۢ﴾ (6:161), ﴿أَن تَأْتِيَهُمُ﴾ (6:158); page 151 ﴿الٓمٓصٓ﴾, disambiguated Abu Ja'far ikhfa ﴿وَمَنْ خَفَّتْ﴾ (7:9) and Khalaf tark al-ghunna ﴿بِعِلْمٍۢ ۖ وَمَا﴾ (7:7); page 152 ﴿وَيَـٰٓـَٔادَمُ﴾ (7:19), ﴿نَّارٍۢ وَخَلَقْتَهُۥ﴾ (7:12); page 153 ﴿بِٱلْفَحْشَآءِ ۖ أَتَقُولُونَ﴾ (7:28), ﴿مُسْتَقَرٌّۭ وَمَتَـٰعٌ﴾ (7:24); page 154 ﴿كَذَّبَ بِـَٔايَـٰتِهِۦٓ﴾ (7:37); page 155 ﴿رُسُلُ رَبِّنَا﴾ (7:43); page 156 ﴿عِوَجًۭا وَهُم﴾ (7:45), ﴿نَنسَىٰهُمْ﴾ (7:51); page 159 ﴿فَٱنتَظِرُوٓا۟﴾ (7:71), ﴿وَءَابَآؤُكُم﴾ (7:71), ﴿بَصْۜطَةًۭ ۖ﴾ (7:69); page 160 ﴿إِنَّكُمْ﴾ (7:81); page 163 ﴿أَوَأَمِنَ﴾ (7:98); page 164 ﴿أَرْجِهْ﴾ (7:111), ﴿نَّكُونَ نَحْنُ﴾ (7:115), ﴿يَدَهُۥ﴾ (7:108); page 165 ﴿مَكَرْتُمُوهُ﴾ (7:123). **(3) Repositories & Fixtures.** Generated 20 new variant fixture files (`packages/qiraat-core/fixtures/pages/page-{146..165}.json`) and 20 new ruling fixture files (`packages/qiraat-core/fixtures/rulings/page-{146..165}.json`). Connected loaders in `packages/qiraat-core/repository.ts` under both `PAGE_VARIANT_LOADERS` and `PAGE_RULING_LOADERS`. **(4) Verification.** `node scripts/validate-qiraat-data.mjs` clean across all 165 pages (900 variants, 3,968 rulings, 18 usul categories, 4 flagged — unchanged); 30/30 Qiraat engine tests passed (`npm run test:qiraat`); `npm run qiraat:validate` clean; `npx tsc --noEmit --incremental false -p .` clean (0 errors); all 7 Mushaf-1441 validators passed (`npm run mushaf:validate`); `npm run build` clean (31/31 routes). Files: `scripts/qiraat/data_variants.py`, `scripts/qiraat/data_rulings.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/pages/page-{146..165}.json` (20 files), `packages/qiraat-core/fixtures/rulings/page-{146..165}.json` (20 files). **DB migration:** none — fixture-only.
- **Feature — Qiraat pages 126–145 imported (Juz' 7 & early Juz' 8, المائدة ١٠٩ – الأنعام ١٣٧): +155 variants (783 total across 145 pages) and +527 أصول rulings (3,455 total), with zero anchor errors and 100% contiguous coverage of Juz' 1–7 and early Juz' 8 (pages 1–145):** the seventh major batch of the Ten Minor Readings (القراءات العشر الصغرى), continuing the contiguous sequence of pages 1–145. **(1) Data.** 155 new variant records (783 total across 145 active pages) and 527 new `QiraatRuling` records (3,455 total) across the same 18 established أصول categories. Every single locus is resolved and anchored by `scripts/qiraat/tokens.py` to genuine Mushaf-1441 page-word tokens, partitions the 20 Riwayat without gap or overlap, and introduces zero new `NEEDS_MANUAL_REVIEW` records. **(2) Token Anchor Normalizations & Alignments Resolved.** Handled multi-word and small-letter queries: page 126 ﴿هَلْ يَسْتَطِيعُ رَبُّكَ﴾ (5:112); page 129 ﴿ٱسْتُهْزِئَ﴾ (6:10); page 133 ﴿أَقُولُ لَكُمْ﴾ (6:50) `all_occurrences=True`; page 134 ﴿أَنَّهُۥ مَنْ﴾ and ﴿فَأَنَّهُۥ﴾ (6:54); page 138 ﴿دَرَجَـٰتٍۢ مَّن نَّشَآءُ﴾ (6:83) aligned to Hafs tanween baseline; page 139 ﴿تَجْعَلُونَهُۥ﴾ (6:91); page 140 ﴿وَجَعَلَ ٱلَّيْلَ﴾ baseline vs ﴿وَجَاعِلُ ٱلَّيْلِ﴾ and ﴿ثَمَرِهِۦٓ﴾ (6:96, 99); page 143 ﴿رِسَالَتَهُۥ﴾ (6:124); page 145 ﴿زَيَّنَ﴾ and ﴿قَتْلَ أَوْلَـٰدِهِمْ شُرَكَآؤُهُمْ﴾ (6:137) linked via shared locus group `L145-زين_قتل_اولادهم`. **(3) Repositories & Fixtures.** Generated 20 new variant fixture files (`packages/qiraat-core/fixtures/pages/page-{126..145}.json`) and 20 new ruling fixture files (`packages/qiraat-core/fixtures/rulings/page-{126..145}.json`). Connected loaders in `packages/qiraat-core/repository.ts` under both `PAGE_VARIANT_LOADERS` and `PAGE_RULING_LOADERS`. **(4) Verification.** `node scripts/validate-qiraat-data.mjs` clean across all 145 pages (783 variants, 3,455 rulings, 18 usul categories, 4 flagged — unchanged); 30/30 Qiraat engine tests passed (`npm run test:qiraat`); `npx tsc --noEmit --incremental false -p .` clean (0 errors); all 7 Mushaf-1441 validators passed (`npm run mushaf:validate`); `next build` clean (31/31 routes). Files: `scripts/qiraat/data_variants.py`, `scripts/qiraat/data_rulings.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/pages/page-{126..145}.json` (20 files), `packages/qiraat-core/fixtures/rulings/page-{126..145}.json` (20 files). **DB migration:** none — fixture-only.
- **Feature — Qiraat pages 102–125 imported (Juz' 6 & early Juz' 7, النساء ١٤٨ – المائدة ١٠٨): +108 variants (628 total across 125 pages) and +569 أصول rulings (2,928 total), with zero anchor errors, 100% contiguous coverage of Juz' 1–6 (pages 1–125), and fixed page 117 fixture glyph:** the sixth major batch of the Ten Minor Readings (القراءات العشر الصغرى), continuing the contiguous sequence of pages 1–125. **(1) Data.** 108 new variant loci → **108 new `QiraatVariant` records** (628 total across 125 active pages) and **569 new `QiraatRuling` records** (2,928 total) across the same 18 established أصول categories. Every single locus is resolved and anchored by `scripts/qiraat/tokens.py` to genuine Mushaf-1441 page-word tokens, partitions the 20 Riwayat without gap or overlap, and introduces zero new `NEEDS_MANUAL_REVIEW` records. **(2) Fixture Correction & Token Anchors Resolved.** Fixed split glyph defect in `packages/quran-data/mushaf1441/fixtures/page-words/page-117.json` (token 5:2 `دَآئِرَ ةٌۭ ۚ` corrected to `دَآئِرَةٌۭ ۚ`, verified via `npm run mushaf:validate`); resolved multi-word anchor queries (page 115 5:45 `وَٱلْعَيْنَ بِٱلْعَيْنِ وَٱلْأَنفَ بِٱلْأَنفِ...`, page 117 5:53 `وَيَقُولُ ٱلَّذِينَ ءَامَنُوٓا۟`, page 118 5:62,63 `وَأَكْلِهِمُ ٱلسُّحْتَ`, page 125 5:107 `عَلَيْهِمُ ٱلْأَوْلَيَـٰنِ`); rasm normalizations (page 119 5:67 `رِسَالَتَهُۥ`, page 119 5:69 `وَٱلصَّـٰبِـُٔونَ`, page 124 5:97 `قِيَـٰمًا`, page 124 5:101 `يُنَزَّلُ`). **(3) Repositories & Fixtures.** Generated 24 new variant fixture files (`packages/qiraat-core/fixtures/pages/page-{102..125}.json`) and 24 new ruling fixture files (`packages/qiraat-core/fixtures/rulings/page-{102..125}.json`). Connected loaders in `packages/qiraat-core/repository.ts` under both `PAGE_VARIANT_LOADERS` and `PAGE_RULING_LOADERS`. **(4) Verification.** `node scripts/validate-qiraat-data.mjs` clean across all 125 pages (628 variants, 2,928 rulings, 18 usul categories, 4 flagged — unchanged); 30/30 Qiraat engine tests passed (`npm run test:qiraat`); `npx tsc --noEmit --incremental false -p .` clean (0 errors); all 7 Mushaf-1441 validators passed (`npm run mushaf:validate`). Files: `scripts/qiraat/data_variants.py`, `scripts/qiraat/data_rulings.py`, `packages/qiraat-core/repository.ts`, `packages/quran-data/mushaf1441/fixtures/page-words/page-117.json`, `packages/qiraat-core/fixtures/pages/page-{102..125}.json` (24 files), `packages/qiraat-core/fixtures/rulings/page-{102..125}.json` (24 files). **DB migration:** none — fixture-only.
- **Performance — Instant Mushaf and Qiraat page flips through direct fixture loading and mounted-slot prefetching:** `Mushaf1441Viewer.tsx` now imports immutable Qiraat page chunks directly through `FixtureQiraatRepository`, removing the route-handler request and second JSON serialization from page turns. Prefetch follows the actual mounted page slots, including both leaves of adjacent spreads, and publishes each completed window with one state update so chunk arrivals do not repeatedly rerender every Mushaf slot. Local production-browser measurements committed colored flips in 5.1–15.5 ms across cold, phone, tablet, and desktop trials with zero `/api/mushaf-1441/qiraat` requests. Added Playwright regressions for the no-API critical path and complete next-spread coloring. Files: `packages/qiraat-core/repository.ts`, `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, `tests/test_mushaf_page_performance.py`. No DB change.
- **Feature — Qiraat pages 62–81 imported (Juz' 4, آل عمران ٩٢ – النساء ٢٣): +107 variants (520 total across 101 pages) and +405 أصول rulings (2,359 total), with zero anchor errors and 100% contiguous coverage of Juz' 1–5 (pages 1–101):** the fifth major batch of the Ten Minor Readings (القراءات العشر الصغرى), completing the full contiguous sequence of pages 1–101. **(1) Data.** 84 new variant loci → **107 new `QiraatVariant` records** (520 total across 101 active pages) and **405 new `QiraatRuling` records** (2,359 total) across the same 18 established أصول categories. Every single locus is resolved and anchored by `scripts/qiraat/tokens.py` to genuine Mushaf-1441 page-word tokens, partitions the 20 Riwayat without gap or overlap, and introduces zero new `NEEDS_MANUAL_REVIEW` records. **(2) Token Anchor Normalization & Domain Slips Resolved.** Page 77 ﴿تَسَآءَلُونَ﴾ (4:1) aligned to Shatibiyyah (*وَكُوفِيُّهُمْ تَسَاءَلُونَ مُخَفَّفًا*), keeping Hafs baseline as تخفيف and Harami+Shami+Basri+Madani as تشديد ﴿تَسَّآءَلُونَ﴾; page 72 ﴿أَلَّا خَوْفٌ عَلَيْهِمْ﴾ (3:170) aligned to `'الا خوف عليهم'`; page 78 صلة هاء الكناية resolved to 4:8 ﴿مِنْهُ﴾ and 4:11 ﴿وَلِأَبَوَيْهِ﴾; page 79 صلة هاء الكناية resolved to 4:13 and 4:14 ﴿يُدْخِلْهُ﴾; page 80 صلة هاء الكناية resolved to 4:19 ﴿فِيهِ خَيْرًا كَثِيرًا﴾; page 81 distinguished between Abu Ja'far ikhfa (4:21 ﴿مِّيثَـٰقًا غَلِيظًۭا﴾) and Khalaf tark al-ghunna (4:20 ﴿بُهْتَـٰنًۭا وَإِثْمًۭا﴾, 4:21 ﴿بَعْضٍ وَأَخَذْنَ﴾, 4:22 ﴿فَـٰحِشَةًۭ وَمَقْتًۭا﴾). **(3) Repositories & Fixtures.** Generated 20 new variant fixture files (`packages/qiraat-core/fixtures/pages/page-{062..081}.json`) and 20 new ruling fixture files (`packages/qiraat-core/fixtures/rulings/page-{062..081}.json`). Connected loaders in `packages/qiraat-core/repository.ts` under both `PAGE_VARIANT_LOADERS` and `PAGE_RULING_LOADERS`. **(4) Verification.** `npm run qiraat:validate` clean across all 101 pages (520 variants, 2,359 rulings, 18 usul categories, 4 flagged — unchanged); 30/30 Qiraat engine tests passed (`npm run test:qiraat`); `npx tsc --noEmit --incremental false -p .` clean (0 errors); all 7 Mushaf-1441 validators passed (`npm run mushaf:validate`). Files: `scripts/qiraat/data_variants.py`, `scripts/qiraat/data_rulings.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/pages/page-{062..081}.json` (20 files), `packages/qiraat-core/fixtures/rulings/page-{062..081}.json` (20 files). **DB migration:** none — fixture-only.
- **Feature — Qiraat pages 42–61 imported (Juz' 3, البقرة ٢٥٣ – آل عمران ٩١): +121 variants (413 total across 81 pages) and +552 أصول rulings (1,954 total), with zero anchor errors and full test suite verification:** the fourth major batch of the Ten Minor Readings (القراءات العشر الصغرى), completing the contiguous sequence of pages 1–61 (Juz' 1, 2, and 3). **(1) Data.** 52 new variant loci → **121 new `QiraatVariant` records** (413 total across 81 active pages) and **552 new `QiraatRuling` records** (1,954 total) across the same 18 established أصول categories. Every single locus is resolved and anchored by `scripts/qiraat/tokens.py` to genuine Mushaf-1441 page-word tokens, partitions the 20 Riwayat without gap or overlap, and introduces zero new `NEEDS_MANUAL_REVIEW` records. **(2) Token Anchor Normalization & Source Slips Resolved.** In accordance with §12.4b: small high letters and small waw/yaa expansions (page 42 ﴿تَأْخُذُهُۥ﴾ `2:255` `'تاخذهو'`, ﴿بِإِذْنِهِۦ﴾ `2:255` `'باذنهي'`, ﴿شَىْءٍۢ﴾ `'شيء'`; page 43 ﴿أَنَا۠ أُحْىِۦ﴾ `2:258` `'انا احيي'`; page 53 ﴿نَفْسَهُۥ ۗ وَإِلَى﴾ `3:28` `'نفسهۥ والى'`; page 60 ﴿لَفَرِيقًا يَلْوُۥنَ﴾ `3:78` `'لفريقا يلوون'`; pages 52, 60 ﴿ٱلنَّبِيِّـۧنَ﴾ `3:21, 3:80, 3:81` `'النبيين'`); rasm alignment for Hafs baseline (page 43 ﴿نُنشِزُهَا﴾ `2:259` `'ننشزها'`, page 45 ﴿وَيَأْمُرُكُم﴾ `2:268` `'وَيَأْمُرُكُم'`, page 46 ﴿وَيُكَفِّرُ﴾ `2:271` `'ويكفر'`, page 56 ﴿أَنِّىٓ أَخْلُقُ﴾ `3:49` `'انى اخلق'`, page 60 ﴿عَلَىٰ ذَٰلِكُمْ إِصْرِى﴾ `3:81` `'اصري'`); multi-way splits accurately modeled (page 48 ﴿فَتُذَكِّرَ﴾ `2:282` 3-way partition: 12-6-2 = 20, page 57 ﴿فَيُوَفِّيهِمْ﴾ `3:57` 4-way split: 1-17-1-1 = 20, page 56 ﴿صِرَٰطٌ﴾ `3:51` split 17-2-1 = 20). **(3) Repositories & Fixtures.** Generated 20 new variant fixture files (`packages/qiraat-core/fixtures/pages/page-{042..061}.json`) and 20 new ruling fixture files (`packages/qiraat-core/fixtures/rulings/page-{042..061}.json`). Connected loaders in `packages/qiraat-core/repository.ts` under both `PAGE_VARIANT_LOADERS` and `PAGE_RULING_LOADERS`. **(4) Verification.** `npm run qiraat:validate` clean across all 81 pages (413 variants, 1,954 rulings, 18 usul categories, 4 flagged — unchanged from pages 1–20); 30/30 Qiraat engine tests passed (`npm run test:qiraat`); `npx tsc --noEmit -p .` clean (0 errors); all 7 Mushaf-1441 validators passed (`npm run mushaf:validate`); `next build` clean (31/31 routes). Files: `scripts/qiraat/data_variants.py`, `scripts/qiraat/data_rulings.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/pages/page-{042..061}.json` (20 files), `packages/qiraat-core/fixtures/rulings/page-{042..061}.json` (20 files). **DB migration:** none — fixture-only.
- **Feature — Qiraat pages 82–101 imported (Juz' 5, النساء ٢٤–١٤٧): +83 variants (292 total across 61 pages) and +509 أصول rulings (1,402 total), with zero anchor errors and full test suite verification:** the third major batch of the Ten Minor Readings (القراءات العشر الصغرى), following the repeatable import process in `PROJECT_MASTER.md` §12. **(1) Data.** 36 new variant loci → **83 new `QiraatVariant` records** (292 total across 61 active pages) and **509 new `QiraatRuling` records** (1,402 total) across the same 18 established أصول categories. Every single locus is resolved and anchored by `scripts/qiraat/tokens.py` to genuine Mushaf-1441 page-word tokens, partitions the 20 Riwayat without gap or overlap, and introduces zero new `NEEDS_MANUAL_REVIEW` records. **(2) Token Anchor Normalization & Source Slips Resolved.** In accordance with §12.4b, dagger alefs, small high letters (`ۥ` / `ۧ` / `ۦ`), and rasm normalizations were properly aligned: page 85 ﴿سُكَـٰرَىٰ﴾ (`'سكارى'`), page 86 ﴿هَـٰٓؤُلَآءِ أَهْدَىٰ﴾ (`'هاولاء اهدا'`), page 87 ﴿وَءَاتَيْنَـٰهُم﴾ (`'وءاتيناهم'`), page 89 ﴿ٱلنَّبِيِّـۧنَ﴾ (`'النبيين'`), page 93 ﴿فَتَبَيَّنُوا۟﴾ (corrected source typo `'فتتبينوا'`), page 94 ﴿بَيْتِهِۦ﴾ (`'بيتهي'`) and ﴿ٱلْمَلَـٰٓئِكَةُ ظَالِمِىٓ﴾ (`'الملايكه ظالمي'`), page 95 ﴿قِيَـٰمًۭا وَقُعُودًۭا وَعَلَىٰ﴾ (`'قياما وقعودا وعلا'`), page 96 ﴿بُهْتَـٰنًۭا وَإِثْمًۭا﴾ (corrected source slip quoting 4:112 as `بَرِيئًا وَإِثْمًا`), page 97 ﴿نُوَلِّهِۦ﴾ / ﴿وَنُصْلِهِۦ﴾ (`'نولهي'` / `'ونصلهي'`), page 98 ﴿يَتَـٰمَى﴾ (`'يتامى'`), and page 100 ﴿تَلْوُۥٓا۟﴾ (`'تلووا'`), ﴿نَزَّلَ عَلَىٰ رَسُولِهِۦ وَٱلْكِتَـٰبِ ٱلَّذِىٓ أَنزَلَ﴾ (`'نزل علا رسولهي والكتاب الذي انزل'`), ﴿ٱلْـَٔاخِرِ﴾ (`'الاخر'`), and ﴿حَدِيثٍ غَيْرِهِۦٓ﴾ (`'حديث غيرهي'`). **(3) Repositories & Fixtures.** Generated 20 new variant fixture files (`packages/qiraat-core/fixtures/pages/page-{082..101}.json`) and 20 new ruling fixture files (`packages/qiraat-core/fixtures/rulings/page-{082..101}.json`). Connected loaders in `packages/qiraat-core/repository.ts` under both `PAGE_VARIANT_LOADERS` and `PAGE_RULING_LOADERS`. **(4) Verification.** `npm run qiraat:validate` clean across all 61 pages (292 variants, 1,402 rulings, 18 usul categories, 4 flagged — unchanged from pages 1–20); 30/30 Qiraat engine tests passed (`npm run test:qiraat`); `npx tsc --noEmit -p .` clean (0 errors); all 7 Mushaf-1441 validators passed (`npm run mushaf:validate`). Files: `scripts/qiraat/data_variants.py`, `scripts/qiraat/data_rulings.py`, `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/pages/page-{082..101}.json` (20 files), `packages/qiraat-core/fixtures/rulings/page-{082..101}.json` (20 files). **DB migration:** none — fixture-only.

## 2026-09-17
- **Feature/Architecture — Smart Hifz & Mutashabihat Quiz Training System:** comprehensive redesign and implementation of the Quran memorization and Mutashabihat testing engine at `/test`. **(1) 5 Quiz Modes.** اختبار سريع (10-20 randomized questions), اختبار مخصص (customizable surah range, question count, difficulty, and question type filters), اختبار المتشابهات (specialized testing on Mutashabihat groups with authentic Quranic distractors), نقاط ضعفي (adaptive review targeting past mistakes and high error-rate ayahs), and اختبار اليوم (deterministic daily challenge consistent per calendar date). **(2) 13 Question Types & 5 Difficulty Levels.** Covers complete ayah, missing word, next word, ayah ending, ayah beginning, next ayah, previous ayah, transition between ayahs, identify surah, identify ayah number, identify location, mutashabihat, and subtle differences between mutashabihat across 5 calibrated difficulty levels (سهل, متوسط, صعب, متشابهات, إتقان). **(3) Authentic Quran-based Distractor Engine.** 100% Multiple Choice Questions (4 choices per question). All distractors are extracted strictly from authentic Quranic text and context (never synthetic or AI-generated). **(4) Anti-Cheat Presentation Layer.** Session payloads are projected through `publicSession` on the server before client dispatch, stripping correct choice IDs, distractor sources, and metadata until the question is answered. **(5) Performance Analytics & Weak-point Review.** Per-ayah, per-question-type, and per-mutashabihat error tracking stored in `user_ayah_performance`, `user_question_type_performance`, and `user_mutashabihat_performance`. **(6) Legacy Quiz Preserved.** Legacy quiz preserved at `/test/legacy` ensuring 100% backward compatibility. **Verification:** `npm run test:quiz` (77/77 tests passed), `npm run test:quiz:integration` passed, Playwright E2E (`tests/quiz/e2e/hifz.spec.ts`, 6/6 tests passed on Desktop, Tablet, and iPhone 13), `tsc --noEmit` clean, `next build` clean (31/31 routes). Files: `src/lib/quiz/*`, `src/components/quiz/*`, `src/app/api/quiz/*`, `src/app/(app)/test/*`, `tests/quiz/*`, `supabase/migrations/20260918000000_hifz_quiz.sql`. **DB migration:** applied `20260918000000_hifz_quiz.sql` with prior `pg_dump -Fc` backup.
- **Feature/Design — Mushaf-1441 Theme System (Dark Mode, White Page Mode, and Classic Sepia): settings modal in burger menu, theme-calibrated typography, Mutshabehat tints, Qiraat markers, and reverse flap curl:** implemented three distinct, scholarly calibrated themes for the Mushaf-1441 viewer (`'sepia' | 'dark' | 'white'`) persisted per device in `localStorage` (`mushaf1441:theme:v1`). **(1) Three Calibrated Themes.** `sepia`: classic warm heritage paper (`#fcf8ed`) with warm charcoal typography (`#1b1b1b`), golden accents (`#80662c`), and parchment spine shading; `dark`: ultra-refined night reading mode (`#18191d` background, `#22242a` page surface) with crisp high-contrast text (`#f1f5f9`), warm gold accents (`#c8a86b`), dark-adapted surah banners and basmala ornaments; `white`: pure crisp white paper (`#ffffff`) with deep carbon black ink (`#111827`) and rich bronze accents (`#92400e`). **(2) Theme-Adapted Highlighters, Annotations & Rulings.** Mutshabehat tint palettes calibrated per theme (`MUTSHABEHAT_TINTS_SEPIA`, `MUTSHABEHAT_TINTS_DARK` with higher luminance, controlled saturation, and radiant text contrast, and `MUTSHABEHAT_TINTS_WHITE` with ink-like vibrancy). Qiraat difference markers and Usul family ruling markers dynamically adapted for dark mode via `adaptColorForDark()`, preserving identity color distinction while ensuring strict contrast compliance. Personal annotations and highlight bands adapted for dark mode via `adaptAnnotationForDark()`. **(3) Settings Trigger & Modal.** Added gear icon button in the burger drawer header alongside a dedicated "مظهر المصحف والإعدادات" card in the drawer body. Built an accessible modal dialog (`role="dialog"`, `aria-modal="true"`, `aria-labelledby="mushaf-settings-title"`) featuring miniature preview swatch cards for each theme with live Arabic calligraphy ﴿بِسْمِ ٱللَّهِ﴾, gold accent lines, radio selection indicators, and instant live theme switching. **(4) Page Curl Reverse Flap.** `PageCurlOverlay` accepts `paperColor` so the single-page reverse flap matches the current theme paper color during curl transitions. **(5) Invariants Preserved.** The ~1 ms page-turn invariant is maintained by passing `theme` through `MushafPageSlot` props and updating the memo comparator. Strict compliance with all 7 Mushaf validators: zero `vw` units and zero `min-h-10` classes. **Verification:** `tsc --noEmit` clean; 30/30 Qiraat engine tests passed; `qiraat:validate` clean; all 7 Mushaf validators passed; `next build` clean (29/29 routes compiled). Files: `src/app/mushaf-1441/_components/mushafTheme.ts` (new), `src/app/mushaf-1441/_components/PageCurlOverlay.tsx`, `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`. No DB change.
- **Change/Fix — The burger menu loses التنقل و«معلومات الصفحة», every layer gets the side rail that only القراءات had, the surah list becomes an anchored dropdown, the page number gets its own frame, and the Qiraat peek stops closing itself:** five UI corrections in one pass, all in the reader. **(1) Two burger sections deleted.** التنقل (the surah/ayah/page sliders with «انتقل للآية») and معلومات الصفحة duplicated what the top bar and the bottom page slider already do, and the dead code went with them — `renderNavControls`, `jumpToSelectedAyah`, `submitAyahJump`, and the `selectedSurah`/`selectedSurahAyahCount`/`selectedAyahNumber` state, rather than leaving orphaned functions behind. **Worth knowing: the jump-to-a-specific-ayah box went with that panel**, because that panel was its only UI; surah navigation survives in the header picker and page navigation in the arrows and the bottom slider, and the ayah jump can go back somewhere else if it is wanted. **(2) One side rail, whichever layer is on.** `showReaderSidebar = isSpread && readerLayer !== 'none'` — at the same breakpoint the two-page spread uses, المتشابهات and الملاحظات now get the permanent 330px rail that مقارنة القراءات already had, titled by the layer (القراءات العشر / المتشابهات / الملاحظات والتمييز). With a rail on screen a linked ayah fills it instead of opening a modal over the page (`openMutshabehatPopup` returns early into `mutshabehatPanelAyahKey`), and selecting a word no longer opens the mobile notes sheet on a desktop. The popup's group list was extracted into a shared `renderMutshabehatGroups(ayahKey)` so the rail and the phone modal cannot drift apart. Phones and portrait tablets are untouched and keep the drawer. **(3) The surah list is a dropdown, not a modal.** `renderSurahPicker()` renders `absolute right-0 top-full` inside the surah button's own `relative` wrapper, capped at 17rem (19rem from `sm:`) and `min(60vh,420px)` tall, with its own click-away catcher — measured at 304×420 opening 4px under the button with its right edge on the button's right edge, instead of a sheet across the screen. **(4) The page number gets a frame of its own, outside the truncating block.** It used to sit inside the block that ellipsises a long surah name, so on a two-surah page the number was the thing that got clipped; it is now a fixed gold-bordered «صفحة / N» cell that cannot be squeezed — measured at [1246,1293] against the surah name's [1301,1380], no overlap, nothing clipped. **(5) The Qiraat peek no longer sits on the word you are reading — the real bug.** It was `absolute left-1/2 top-3`, floating over the lines: pressing a word underneath it hit the card, and the card's own click handler closed the peek before it could be read. It now takes a `placement`: with a rail it renders **in the rail** (`data-qiraat-peek="sidebar"`), and without one it is pinned to the bottom edge (`fixed inset-x-2 bottom-2`) clear of the text. Nothing about the closing behaviour needed to change — the card was never the problem, its position was. **Verification:** Playwright/Chromium at 1440×950, 1024×768 (iPad landscape), 900×800 (mouse, below the spread breakpoint) and iPhone 13 — **34/34 checks, zero real page errors** (the 66 console lines are all `ERR_TUNNEL_CONNECTION_FAILED` from this sandbox's unreachable dummy Supabase and the blocked font CDN): both deleted sections absent and «معلومات ومراجع» still there, the rail present and correctly titled for all three layers on both large viewports and absent for the plain mushaf and on the phone, the picker's measured geometry above, the page-number frame's measured geometry above, the peek rendering inside the rail with **no overlay over the page** and never intersecting the word it describes, pressing a second marked word leaving the rail populated (the old bug), the overlay peek bottom-pinned at 900×800 with the hovered word at y=333–353 and the card at y=539–792, the phone's tap still opening the full Qiraat bottom card, the phone picker fitting a 390px viewport, and no horizontal overflow. Plus `tsc --noEmit` clean, 30/30 engine tests, `qiraat:validate` clean, `next build` clean, and all 7 Mushaf validators — `validate-mushaf1441-objective.mjs` gains a reader-sidebar block and a peek-placement block (so a future change cannot float the peek back over the lines), and both it and `validate-mushaf1441-navigation.mjs` now assert the surface that exists (`renderSurahPicker`/`selectSurahFromPicker`/`goToAyah`) instead of the deleted slider panel. One measurement rule was worth keeping: `validate-mushaf1441-phase6.mjs` forbids the substring `vw` anywhere in the viewer to protect mushaf typography, so the picker's width is a rem cap, not `calc(100vw - …)`. Files: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, `scripts/validate-mushaf1441-objective.mjs`, `scripts/validate-mushaf1441-navigation.mjs`. No DB change.
- **Feature — New «معلومات ومراجع» section in the burger menu: the القرّاء العشرة ورموز الشاطبية والدرة reference, processed into typed data whose own counts are checked against the app's authority model:** the supplied reference was a markdown document; dropping it in as prose would have made it unreadable on a phone and unverifiable. It is now `packages/qiraat-core/symbols.ts` — **typed data keyed to the app's existing Q-IDs**, not name strings — plus a dedicated reading sheet. **(1) Data.** All 30 authority symbols (10 إمام + 20 راوٍ across both متون), the 8 رموز كلمية (صَحْبَة، صِحَاب، عَمَّ، سَمَا، حَقّ، نَفَر، حَرَمِيّ، حِصْن), the 6 رموز حرفية («ثخذ ظغش»), the decomposition relations, the documented composition examples, the Durrah letter remap, the geographic/group names, and the usage rules. Every symbol carries its `matn`, because **the same letter is a different person in each**: أ is نافع in the Shatibiyyah and أبو جعفر in the Durrah, and ض is the narrator خلف عن حمزة (`Q06-R01`) there but إسحاق عن خلف العاشر (`Q10-R01`) here — the exact collision the project's Q-ID space exists to prevent, so `resolveAuthoritySymbol` refuses to resolve without a متن. **(2) The source's own counts are used as a checksum, not copied decoration.** Each group symbol keeps the عدد الروايات its reference prints, and **5 new tests assert it equals what the Q-IDs actually expand to** — so a mis-transcribed group fails the build instead of teaching the reader wrongly. All 14 group symbols agree (صحبة 5، صحاب 5، عمّ 4، سما 6، حقّ 4، نفر 6، حرميّ 4، حصن 8، ث 6، خ 12، ذ 8، ظ 8، غ 8، ش 4). The tests also pin the two traps the source itself warns about: صحبة carries شعبة and never حفص while صحاب is the reverse, neither may quietly acquire خلف العاشر (a Durrah reader), خ never contains a نافع riwayah, and the tables cover every reader and narrator the app knows, exactly once. 30/30 tests pass (was 25). **(3) UI.** New `QiraatReferenceSheet.tsx`, opened from a «معلومات ومراجع» card at the foot of the burger menu and rendered in the same sheet shape as مفتاح القراءات (bottom sheet on a phone, right rail on desktop). Eight accordion sections with only the first open, so the reader unfolds what they want instead of scrolling past six tables. Every group symbol renders its members as the **same identity-coloured pills the rest of the Qiraat layer uses** (ورش in his blue, شعبة in his orange…), so a symbol in the reference and a word on the page speak one visual language; each pill reads «الراوي عن الإمام» so a riwayah is never confused with its reader. The cautions the source attaches to specific rows are kept attached rather than pooled at the end — the صحبة/صحاب distinction sits under the word symbols, «الأصل هنا للمقارنة» under the Durrah table, and the «الباقون ليست مجموعة ثابتة» caveat under the group names — and the six source links are listed under the usage rules. **Verification:** Playwright at 1440×950 and iPhone 13, **13/13 checks with zero page errors** — the menu card present, the sheet opening while the drawer closes, all 30 authority chips listed (10 إمام + 20 راوٍ) on both viewports, the 8 word symbols each showing their riwayah count, صَحْبَة rendering its 5 pills, the Durrah row naming إسحاق where the Shatibiyyah names خلف عن حمزة, and no horizontal overflow on the phone. One assertion in the first run was wrong rather than the code (a `~` sibling selector that could not match a nested title) and was corrected before the run counted. Plus `tsc` clean, `next build` clean, all 7 Mushaf validators, `qiraat:validate` clean. Files: `packages/qiraat-core/symbols.ts` (new), `packages/qiraat-core/engine.test.mjs`, `src/app/mushaf-1441/_components/qiraat/QiraatReferenceSheet.tsx` (new), `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`. No DB change.
- **Feature — Qiraat page 21 imported, closing the gap the previous batch left: pages 1–41 are now contiguous:** page 21 (البقرة ١٣٥–١٤١، آخر صفحات الجزء الأول) was missing from the source that supplied 22–41, and was recorded as a real gap; it is now in. **7 new `QiraatVariant` records** (209 total) and **19 new `QiraatRuling` records** (893 total). The four loci: ﴿إِبْرَٰهِـۧمَ﴾ at all three positions (2:135، 2:136، 2:140) with ابن عامر reading إِبْرَٰهَامَ and ابن ذكوان ذو وجهين — note this is a *different* attribution from page 20's same word, where the source gives the وجه to ابن ذكوان alone, and each page is stored as its own checked record exactly as the approved decision requires; ﴿ٱلنَّبِيُّونَ﴾ (نافع بالهمز); ﴿وَهُوَ﴾ at both positions (7 Riwayat read وَهْوَ); and ﴿أَمْ تَقُولُونَ﴾/﴿أَمْ يَقُولُونَ﴾ (10 Riwayat each way — the rare locus that splits the twenty exactly down the middle). The أصول table adds تغليظ اللام ×1، مد البدل ×6، الممال والمقلل ×5، المدغم الكبير ×4 (﴿وَنَحْنُ لَهُۥ﴾ at all three positions plus ﴿أَظْلَمُ مِمَّن﴾)، ترك الغنة ×2، and the page's ﴿ءَأَنتُمْ﴾ — whose **five مذاهب for the second hamzah are kept as five distinct actions rather than flattened to one**, with ورش and هشام each holding two of them and marked ذو وجهين accordingly. **Both generators accepted every anchor on the first run** — the first batch to need no anchor fixes at all, which is what the four shapes written up in `PROJECT_MASTER.md` §12.4b during the 22–41 batch were for. Every locus partitions the 20 Riwayat exactly once; no new `NEEDS_MANUAL_REVIEW` record and no new أصول category. **Verification:** `qiraat:validate` clean across all 41 pages (209 variants, 893 rulings, 4 flagged — unchanged); 25/25 engine tests; `tsc` clean; `next build` clean; all 7 Mushaf validators; Playwright at 1440×950 walking 20 → 21 → 22 with **zero page errors**, and the API returning page 21's records with every occurrence count matching the source table (إبراهيم ×3, ونحن لهو ×3, وهو ×2, نصارى ×2, أوتي ×2). Worth knowing when reading those numbers: at that viewport the reader draws a **two-page spread**, so a URL for page 22 renders the 21+22 spread — page 22's أصول count rose from 34 to 57 purely because page 21 now contributes its own 23, not because anything changed on page 22. Files: `scripts/qiraat/{data_variants,data_rulings}.py`, `packages/qiraat-core/fixtures/pages/page-021.json` (new), `packages/qiraat-core/fixtures/rulings/page-021.json` (new), `packages/qiraat-core/repository.ts`, `PROJECT_MASTER.md`, `HANDOFF.md`. **DB migration:** none — fixture-only.
- **Feature — Qiraat pages 22–41 imported (Juz' 2, البقرة ١٤٢–٢٥٢): +102 variants and +471 أصول rulings, and the two generators/linters that were pinned to 20 pages now follow the data:** the second batch, run through `PROJECT_MASTER.md` §12 exactly as written. **(1) Data.** 40 new loci → **102 new `QiraatVariant` records** (202 total) and **471 new `QiraatRuling` records** (874 total) across the same 18 أصول categories — no new category was needed, which is itself a check that the page-1–20 taxonomy holds. Every locus is anchored by `scripts/qiraat/tokens.py` to a real Mushaf-1441 token and every `baseText` is copied verbatim from the page-word fixtures; **every one of the 40 new variant loci partitions the 20 Riwayat exactly once**, no gaps and no overlaps, and the batch adds **zero** new `NEEDS_MANUAL_REVIEW` records. **(2) The generators caught 23 bad anchors on the first run — all of them source spellings, none a partition error.** Four recurring shapes, now written up in new `PROJECT_MASTER.md` §12.4b with the one-liner that dumps a page to resolve them: a small high ۥ/ۦ expands to و/ي in `norm()` (`قوله`→`قولهو`, `بيده`→`بيدهي`); the source quotes the *variant* where the anchor needs the *rasm* (`حتى يقتلوكم`→`حتى يقاتلوكم`); a final long aa written `ىٰ` (`واليتمى`→`واليتاما`); and a neighbouring word that belongs to the token (`شيء`→`بشيء`, `العذاب بالمغفرة`→`والعذاب بالمغفرة`). **(3) Three source slips corrected against the mushaf**, each because the word the source printed does not occur on that page at all: page 34 ﴿كَثِيرٌ وَمَنَٰفِعُ﴾ → ﴿كَبِيرٌ﴾ (كثير is the حمزة/الكسائي *reading*, recorded separately as a variant on the same page — a ترك الغنة anchor must sit on the rasm); page 38 ﴿خَيْرٌ﴾ → ﴿خَبِيرٌ﴾ (2:234); page 41 ﴿إِنِّىٓ إِلَّا﴾ → ﴿مِنِّىٓ إِلَّا﴾ (2:249). **Eight `؟`-marked or impossible entries were omitted rather than guessed** (page 27 ﴿ٱلْمَشْرِقِ﴾؟ and ﴿ٱلنَّأْىِ﴾ — which is in الإسراء, not on page 27; page 29 ﴿يُبَيِّنُ ٱللَّهُ﴾؟; page 33 ﴿زُيِّنَ﴾؟ ×2; page 36 ﴿أَنفُسِهِمَا﴾؟; page 37 ﴿يَعْمَلْ ذَٰلِكَ﴾؟), and **page 21 (2:135–141) is a real gap** — the supplied source jumps from 20 to 22. All of it is recorded in §12.5. **(4) Two modelling decisions.** Page 25 ﴿يَأْمُرُكُم﴾: الدوري عن أبي عمرو has the إسكان *and* the اختلاس, and two أوجه overlapping on one Riwayah cannot partition the 20 — so the إسكان وجه carries السوسي and الدوري, `alternate_of` marks الدوري ذو وجهين, and the اختلاس lives in the note. Page 39 ﴿وَيَبْصُۜطُ﴾: the baseline text must be the mushaf's own rasm (صاد with the small seen) with the description naming السين, because writing `وَيَبْسُطُ` fails the baseline invariant. **(5) Two tools were silently scoped to 20 pages and are now data-driven** — this batch would otherwise have shipped unvalidated: `scripts/qiraat/build_rulings.py` looped `range(1, 21)` and would have emitted nothing for the new pages, and `scripts/validate-qiraat-data.mjs` looped `page <= 20`, so it would have reported "all checks passed" while checking none of them. The builder now iterates `sorted(RULINGS)`, and the linter derives its page list from the repository's own loader table and fails outright if that table is empty. **(6) Chore:** `scripts/qiraat/__pycache__/*.pyc` was committed by accident in `7b54c40`; untracked and added to `.gitignore` so the remaining 563 pages don't each churn four binaries. **Verification:** `npm run qiraat:validate` clean across all 40 pages (202 variants, 874 rulings, 4 flagged — unchanged from before the batch); 23/23 engine tests; `tsc --noEmit` clean; `next build` clean; all 7 Mushaf validators; and Playwright/Chromium at 1440×950 on pages 22, 25, 28, 31, 35, 39 and 41 — 23–72 أصول-coloured words per page in 8–11 distinct family colours, 3–22 variant-marked words, the ذو وجهين underline present, the sidebar rendering, and **zero page errors**. Clicking ﴿وَٱلنَّهَارِ﴾ (2:164) explains it correctly as «إمالة → الإمام أبو عمرو، الراوي الدوري عن الكسائي» and «تقليل → الراوي ورش». Nothing in this batch claims `VERIFIED`; like pages 1–20 it ships at `REVIEWED`, visible only through the include-reviewed path, pending the paper original. Files: `scripts/qiraat/{data_variants,data_rulings,build_rulings}.py`, `scripts/validate-qiraat-data.mjs`, `packages/qiraat-core/fixtures/pages/page-0{22..41}.json` (new), `packages/qiraat-core/fixtures/rulings/page-0{22..41}.json` (new), `packages/qiraat-core/repository.ts`, `PROJECT_MASTER.md`, `.gitignore`. **DB migration:** none — fixture-only, same as every Qiraat batch so far.
- **Docs — Handoff report rewritten for the current state, and the reader's three-layer rule recorded as permanent architecture:** `HANDOFF.md` was still describing 2026-09-15 and knew nothing about the Qiraat work or the layer model. Rewritten for 2026-09-17: what the two Qiraat sessions actually shipped (commit table `f3d46d0` → `8a0fe08`), and an open-items list that now separates the live thread (the user's visual review of pages 1–20, the 584 pages still to import, the 3 `NEEDS_MANUAL_REVIEW` loci, the 3 generator-corrected `source_swap` loci that still need the paper original, the ~15 `؟` markers, the unapplied V2 migration) from the decisions worth revisiting (a new device defaults to the متشابهات layer — my call, not the user's, and the one to raise if a reader lives in their notes; the iOS haptic is unconfirmed on real hardware) and the older carried-over issues. §3 gains two verification lessons that cost real time this week: a feature that depends on the user's own data is not verified by watching `aria-pressed` flip — stub the API with Playwright's `context.route()` so there is something real to show or hide and count what is actually painted (that is what caught the memo-identity bug a state-only check had missed) — and, from a cloud sandbox, the agent proxy blocks `*.vercel.app`, so a 0-byte `curl` to production looks exactly like "not deployed yet" and is not; confirm deploy state through the Vercel API and say plainly that the live page was never loaded. §5b now leads with the memo-identity trap stated as a rule (**gate at the prop, not only at the derived map**) together with the two occasions it bit, and names the state that must never enter a slot prop (`hoveredAyahKey`, `hoveredQiraatWord`, `qiraatSelection`) or the ~1 ms page-turn invariant is lost. New §7 lists every `localStorage` key the reader owns, including the two superseded ones kept only for migration. New `PROJECT_MASTER.md` §13 records the layer model itself as permanent architecture — the four layers and what a press means in each, why it is one enum and not three booleans (three booleans can represent "two layers on at once", a state the reader is not allowed to be in, and each such state would then need defending separately at every render site), the two press-routing entry points that new behaviour must be added to rather than beside, the memoization trap, and the haptics contract with its honest iOS caveat. Files: `HANDOFF.md`, `PROJECT_MASTER.md`. No code or DB change.
- **Feature/Change — One reader layer at a time (الملاحظات / المتشابهات / القراءات are now mutually exclusive), a press answers only the active layer, and long-press gets an iOS-style haptic:** the mushaf paints three colour systems onto the same letters, and until now two of them could be on together and a single press could open any of the three panels. **(1) Mutual exclusion, enforced structurally.** The three independent booleans (`annotationsVisible`, `mutshabehatHighlightEnabled`, `qiraatMode`) are replaced by one `ReaderLayer` enum (`'none' | 'annotations' | 'mutshabehat' | 'qiraat'`) persisted in a single key (`mushaf1441:reader-layer:v1`). Three booleans can represent states the reader is not allowed to be in — two layers painting at once — and every one of those states would have to be defended against separately at each render site; one enum cannot. `annotationsVisible`, `mutshabehatHighlightEnabled` and `qiraatMode` are now *derived* from it, so the whole render path is unchanged and no code path can paint two systems. Pressing an inactive toggle switches to it and turns the other two off; pressing the active one turns it off (plain mushaf). Which Qiraat mode (مقارنة القراءات / القراءة برواية) is kept separately as `qiraatSubMode`, so leaving the layer and returning restores the mode the reader was in — and the burger panel's mode select and the "ق" button now go through the same `applyQiraatMode`, so they cannot disagree about whether Qiraat is on. Existing devices are migrated once from the two superseded keys: an explicit "annotations on, متشابهات off" keeps the annotations layer, anything else lands on متشابهات (this app's namesake layer, and what the mushaf showed before either switch existed). **(2) A press is answered by the active layer and nothing else.** In Qiraat mode a word gives its قراءات and never a متشابهات card or the notes sheet — even on ﴿إِبْرَٰهِـۧمَ﴾ 2:124, which carries a Qiraat variant *and* a متشابهات link *and* a highlight at once; in متشابهات mode a linked ayah opens its card and a word in no group does nothing at all, instead of quietly opening notes behind the reader's back. Both the tap path (`renderQcfWord`'s `onClick`) and the deliberate-press path are routed by layer, the latter through the single `openContextMenu` funnel that long-press and right-click already shared, so the note/highlight/bookmark/favourite menu now belongs to the annotations layer alone. The ayah-end medallion follows the same rule rather than staying a back door into the notes sheet. The detail sheet's three-way tab bar is gone: which panel it shows is derived from the layer, so it can no longer show متشابهات links over a page painted with Qiraat. `readerLayer` rides in the page slot's `selection` prop — the slot compares props by identity, and without it a slot could keep a closure from the previous layer and answer a press the way that layer used to (the same class of bug as yesterday's annotations toggle). **(3) Long-press feels like holding an iOS icon.** One crisp tick at the moment the press is recognised, and a CSS `active:` state for as long as the finger is down. New `haptics.ts` tries `navigator.vibrate(15)` (Android/Chromium; longer than ~20 ms stops reading as a tap and starts reading as a buzz) and, only where that API is absent, the iOS 17.4+ `<input type="checkbox" switch>` label trick — **mobile Safari exposes no Vibration API at all**, so that is the only haptic an iOS web page can ask for; it is a platform quirk, not a standard, and if a future WebKit stops playing it the tick goes quiet and nothing else breaks. The tick fires only when the press actually produced something: a long press that opens nothing buzzing anyway would be a lie about what just happened. **Verification** (Playwright/Chromium, annotations + متشابهات APIs stubbed so there is real conflicting data on one ayah): **31/31 checks, zero page errors** at 1440×950, 1024×768 (iPad landscape) and iPhone 13 — exclusivity and `aria-pressed` for all six transitions, the painted-word counts for each layer (each layer's own marks present, the other two at exactly 0), the plain-mushaf state, persistence across reload, the Qiraat-over-متشابهات conflict word, long-press opening the Qiraat sheet/sidebar and not the annotation menu on both phone and iPad, the متشابهات card still opening for a linked ayah, the annotation menu still opening in its own layer, and the haptic firing exactly once per productive press and not at all for an unproductive one. Two assertions in the first run were wrong rather than the code (a stale locator that made one check vacuous, and a mouse `click()` where a `tap()` was needed) and were corrected before the run counted. Plus `tsc --noEmit` clean, `next build` clean, 23/23 engine tests, `qiraat:validate` clean, and all 7 Mushaf validators — `validate-mushaf1441-objective.mjs` gains an exclusivity block that fails if the enum or the derived flags are replaced by independent on/off state again. Files: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, `src/app/mushaf-1441/_components/haptics.ts` (new), `scripts/validate-mushaf1441-objective.mjs`. No DB change.
- **Feature/Fix — Notes, highlight colours, bookmarks and favourites get their own on/off switch and clear themselves when the Qiraat layer comes on; fixed the toggle that silently did nothing:** the mushaf can now paint three different colour systems on the same letters (personal annotations, متشابهات links, Qiraat/أصول), so each gets one switch and the Qiraat layer takes precedence by default. **(1)** New "ن" button in the top bar, beside "م", toggling the whole personal-annotation layer — highlight background/text colour, the bookmark/favourite ring on words, and the tint on the ayah-end medallion — persisted per device (`mushaf1441:annotations-visible:v1`). Gated once at `slotAnnotationsByWordId`/`slotAnnotationsByAyahKey` and at the ayah marker rather than at each of the ~6 render sites, so no surface can be forgotten. The annotations themselves are never touched: the context menu, the hover card and the notes editor still see everything, so hiding the layer loses nothing. **(2)** Entering مقارنة القراءات / القراءة برواية now switches that layer off by itself, so Qiraat colours are never read against a highlight the user put there for an unrelated reason. It fires only on the normal→Qiraat transition, so a user who deliberately turns annotations back on *while in* Qiraat mode keeps them (verified). **(3) Real bug found and fixed while verifying:** the first cut of the toggle appeared to do nothing in normal mode. `MushafPageSlot` compares its props by identity, and the slot was being handed the raw `annotations` array — flipping the switch changed the derived maps but not that prop, so the memoized slot never re-rendered and the layer only vanished when some *other* prop happened to move (which is why entering Qiraat mode "worked" and the plain toggle did not). The slot is now handed the gated value. **(4)** The متشابهات toggle shipped in the previous entry — whose visual effect I could not verify then, for lack of backend data — is now verified properly by stubbing the API: 35 tinted words ON → 0 OFF → 35 back ON. Its wiring was already correct (it was passed the gated map, not a raw array), so only the annotation layer had the identity bug. Verification, all via a stubbed annotations/mutshabehat API so there is real data to hide: highlights 16→0→16, bookmark/favourite rings 12→0→12, auto-off on entering Qiraat mode, manual re-enable inside Qiraat mode sticks, and zero page errors; plus 23/23 tests, `tsc` clean, `next build` clean, 7 Mushaf validators, `qiraat:validate` clean. Files: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`. No DB change.
- **Feature — Permanent Qiraat sidebar (desktop / iPad landscape), a متشابهات highlight toggle, and per-word أصول explanation; import process documented for pages 21–604:** four follow-ups to the pages 1–20 import. **(1) Permanent sidebar.** In مقارنة القراءات / القراءة برواية the Qiraat panel is no longer buried in the burger drawer on large screens: at the same breakpoint the two-page spread uses (`(min-width: 1024px) and (orientation: landscape)`) it becomes a 330px sidebar beside the page, carrying the mode/riwayah/filter controls, the أصول family legend and the page-level rules. Phones and portrait tablets are untouched and keep the drawer (verified at 390×844: no sidebar, أصول colouring still renders on 38 words). The page re-fits itself into the narrower `<main>` through the existing container queries — no layout constants changed. **(2) متشابهات toggle.** A new "م" button in the top bar switches the mutshabehat highlight on and off independently of the Qiraat layer, so the page can show one colour system at a time instead of both competing for the same words; persisted per device (`mushaf1441:mutshabehat-highlight:v1`, every access try/caught). Implemented as a single swap of the highlight map that feeds every word (`slotHighlightByAyahKey`), so nothing else in the render path had to learn about it. **(3) Clicking an أصول-coloured word explains it in the sidebar.** The selection shows the word, then — for each ruling on it — the category badge in its family colour, the ruling **grouped by action**, and under each action the reader/narrator pills in their own identity colours. Grouping by action is the point: ﴿تَرْضَىٰ﴾ renders as «إمالة → الإمام حمزة، الإمام الكسائي، الإمام خلف العاشر» and «تقليل → الراوي ورش», which a flat attribution list would have destroyed. A «ذو وجهين (بخلف عنه)» note appears wherever some Riwayah has a second valid وجه, and any rasm-changing وجه at the same token is shown above the rulings. Pills name the level the source actually used — «الإمام» for a reader, «الراوي» for a narrator — never flattened to one. The selection state is deliberately kept out of every `MushafPageSlot` prop, so selecting a word never re-renders a page slot and the ~1 ms page-turn invariant still holds (same rule as `hoveredAyahKey`). **(4) Process documented.** New `PROJECT_MASTER.md` §12 records the repeatable import recipe for the remaining 584 pages: where each file lives, the six-step loop per batch, the five non-negotiable rules (never hand-type `baseText`; never write a bare «خلف» or «الدوري»; a rule is written once but never auto-applied; never infer performance-only from normalised text equality; nothing reaches VERIFIED without the paper original), the six generator invariants **with the specific defect each one caught**, and the carried-over source defects. Verification: 23/23 tests, `tsc` clean, `next build` clean, all 7 Mushaf validators pass, `npm run qiraat:validate` clean, Playwright at 1440×950, 1024×768 and 390×844 with **zero page errors**. **Not verified:** the متشابهات highlight visually disappearing — this sandbox has no reachable Supabase, so it renders zero mutshabehat highlights in either state; the control itself (aria-pressed, persistence) is verified and the gating is a one-line map swap, but the visual effect needs a signed-in account with real groups. Files: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, `PROJECT_MASTER.md`. No DB change.
- **Feature — Qiraat pages 1–20 fully imported (100 variants + 403 أصول rulings), أصول now colour the word itself, and a per-locus review mode:** implements the V2 plan agreed earlier today. **(1) Data.** The whole 20-page extraction is now in the app, not just the الأوجه tables: 100 `QiraatVariant` records across 72 loci, and **403 new `QiraatRuling` records** covering all 18 أصول categories the source prints (صلة هاء الكناية، ترقيق الراءات، تغليظ اللامات، مد البدل، مد اللين، الممال والمقلل، المدغم الصغير/الكبير، تغيير الهمز، الهمزتان من كلمة/كلمتين، ترك الغنة، الإخفاء، وقف حمزة، الوقف على مرسوم الخط، ياءات الإضافة/الزوائد، عد الآي). **Every single locus is anchored to a real Mushaf-1441 token** by `scripts/qiraat/tokens.py`, which matches against the actual page-word fixtures in three tiers (exact → alef-maqsura fold → rasm skeleton); `baseText` is copied verbatim from the fixture and never hand-typed, so combining-mark ordering can't drift. Per the approved decision a rule is written once but **never auto-applied**: each of the (e.g.) 215 Quran-wide عليهم positions is its own checked record, because the rule genuinely does not hold everywhere. **(2) أصول colouring.** A ruling never changes the rasm, so it colours the word: one fixed colour per usul family — إمالة+تقليل magenta, both إدغام kinds green, ترقيق+تغليظ amber, تغيير الهمز+الهمزتان red, مد البدل+مد اللين blue, ترك الغنة+الإخفاء cyan, السكت violet, وقف حمزة+الوقف على الرسم orange, صلة الهاء teal, ياءات gold. A word carrying more than one family gets a small second dot; a word where some Riwayah has «بخلف عنه» is underlined dotted as **ذو وجهين** (the engine no longer silently picks one). New burger-menu panel lists the families present on the page with live counts, each togglable. **(3) Review mode.** "مراجعة المواضع المستوردة" rings every imported locus on the page — amber unchecked, green confirmed, red wrong — with a scrollable per-locus list (✓/✕), a "بقي N من M" counter, and a JSON export so the verdicts can be fed back into the dataset. Verdicts persist per device in `localStorage` (`mushaf1441:qiraat-review:v1`), every access wrapped in try/catch. **(4) Real defects found and fixed while building.** The generator's invariants caught three loci where the supplied extraction's أوجه rows are **swapped** — the group containing عاصم did not match what the Hafs mushaf actually prints (2:9 يخدعون، 2:81 خطيئته، 2:111 أمانيهم, the last also mis-transcribed as ﴿أَمَانِيُّكُمْ﴾ for ﴿أَمَانِيُّهُمْ﴾); all three are corrected to the reading the mushaf and the Shatibiyyah شاهد support, and flagged `source_swap`. A **genuine bug in my own generator** was also caught by the tests: performance-only status was being inferred from text equality *after* normalisation, which strips harakat — so عَلَيْهِمْ vs عَلَيْهُمْ compared equal and the real, visible difference was being erased from 12+ records. It now keys off the source's own wording (إشمام/اختلاس/سكت), leaving exactly 12 genuine performance-only records and 88 textual ones. Three loci stay `NEEDS_MANUAL_REVIEW` with the defect documented (2:83 تعبدون — حمزة والكسائي in both أوجه and يعقوب unaccounted for; 2:93 قلوبهم العجل — خلف in two of three; 2:105 ينزل — the source omits أبو جعفر entirely). Nothing in this import claims VERIFIED. **(5) Verification.** New `scripts/validate-qiraat-data.mjs` (no dev deps, wired into `npm run test:qiraat` and `npm run qiraat:validate`) re-checks every anchor against the real page-word fixtures, that `baseText` is verbatim, that no reading is claimed twice at the same token, that `hasAlternate` agrees with its readings, and that each usul family has exactly one colour dataset-wide. 23/23 engine tests pass (7 rewritten to assert by position instead of brittle generated ids — one of them had baked in the exact combining-mark-ordering assumption this project warns about); `tsc --noEmit` clean; `next build` clean; all 7 Mushaf-1441 validators still pass; Playwright/Chromium verified on pages 1, 2, 8, 15 and 19 (page 19: 58 usul-coloured words in 9 distinct family colours, 15 variant-marked words, 5 marked ذو وجهين) with **zero page errors**, plus the review panel reporting "بقي 18 من 18 موضعًا". Files: `scripts/qiraat/{tokens,authorities,data_variants,data_rulings,build_variants,build_rulings}.py` (new), `scripts/validate-qiraat-data.mjs` (new), `packages/qiraat-core/fixtures/pages/page-0{01..20}.json` (regenerated), `packages/qiraat-core/fixtures/rulings/page-0{01..20}.json` (new), `packages/qiraat-core/{types,repository}.ts`, `packages/qiraat-core/engine.test.mjs`, `src/app/api/mushaf-1441/qiraat/route.ts`, `src/app/mushaf-1441/_components/qiraat/qiraatWordMarker.ts`, `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, `package.json`. **DB migration:** none — fixture-only, exactly as `docs/qiraat/10-v2-architecture-plan.md` §Phase A describes; `20260917120000_qiraat_v2_schema.sql` stays written-and-validated but unapplied.
- **Architecture/DB — Qiraat V2 schema: the permanent data architecture for all 604 pages, reviewed against a competing proposal and executed against a real PostgreSQL 16:** a full redesign of the Qiraat data model, driven by the complete 20-page source extraction (القراءات + جدول الصفحة + الشواهد + الملاحظات) which is far richer than anything the current model can hold. Reviewed an externally-supplied 10-table proposal and **adopted** four of its ideas as genuine improvements on what is shipped — splitting the *locus* from the *variant* (today `QiraatVariant` conflates them, so two أوجه of one word can silently disagree about their span), one self-referencing authority tree instead of separate reader/narrator tables (the source constantly mixes levels in one row: page 13 reads «أبو عمرو، ابن عامر، **حفص**، حمزة» — three readers and one narrator), `source_page_number` kept separate from `mushaf_page_number` (measured +5 across the fixtures, but that is an observation about 20 pages, not a guarantee about 604), and الشواهد/notes/QA-flags as first-class tables (the current implementation discards الشواهد entirely). **Rejected**, with evidence: (1) *rules without a token anchor* — the fatal defect; ~15 of the 17 أصول categories are word- or span-anchored (ترقيق الراءات ﴿وَبِٱلْآخِرَةِ﴾، تغليظ اللامات ﴿ٱلصَّلَوٰةَ﴾، الممال والمقلل ﴿هُدًى﴾، وقف حمزة ﴿مُسْتَهْزِءُونَ﴾), so unanchored none of them could ever be drawn on the word the user is reading; (2) the `nafi`/`hamza_khalaf` ID scheme, which would discard the shipped `Q01`/`Q01-R01` space baked into 79 fixture records, the TS types, the `--q01-*` CSS tokens and the tests — and is *weaker* on the very hazard it was meant to solve (verified in the test DB: `Q06-R01` خلف عن حمزة expands to itself alone while `Q10` خلف العاشر expands to its two narrators, so the two cannot be confused structurally); (3) runtime inheritance plus an unexpanded `all_except`, since «الباقون» is only computable when the full partition of a locus is known — now resolved once at write time by `qiraat_rebuild_entry_readings()` with a trigger keeping the verbatim and derived tables from drifting; (4) flat per-occurrence الشواهد (the Shatibiyyah line for عليهم appears verbatim **8 times in 20 pages**; across 604 it would be stored thousands of times). **Added, missing from both designs:** a global أصول layer (`scope='global'` — المد المنفصل/المتصل، ميم الجمع، نقل ورش، سكت حمزة are Quran-wide conventions stated once on page 2 that govern how any page renders, and were being buried as a page note), multi-ayah loci (الإدغام الكبير ﴿ٱلرَّحِيمِ ۝ مَٰلِكِ﴾ spans 1:3→1:4 — unrepresentable in the current TS engine), a separate `qiraat_count_schools` taxonomy for عد الآي (المكي/الكوفي/المدنيان/البصري/الشامي are ayah-numbering schools with no relation to the ten readers; modelling them as authorities would corrupt every "which riwayah reads this" query), and `qiraat_extraction_raw` holding the payload verbatim pre-normalisation so every later correction stays auditable. Per the user's explicit decision, **a rule is written once but never auto-applied**: each of the ~215 Quran-wide occurrences of عليهم must be checked against its own source page and stored as its own entry, because the rule genuinely does not hold everywhere; the `rule_id` link exists only so `qiraat_qa_rule_divergence` can raise a question, never make a correction. Variants and rulings share one `qiraat_entries` parent (they differ only in whether the rasm changes), so "everything marked on this page for riwayah X" is one join and there is exactly one place where "who reads this" lives. **The migration was executed, not merely written** — applied and re-applied (idempotent) against PostgreSQL 16.13, with functional tests proving: «الباقون» at 1:4 resolves 12 + 8 = 20 with the 8 being exactly عاصم/الكسائي/يعقوب/خلف العاشر; 2:67 هزوا resolves 1 (حفص alone) + 15 + 4 = 20 across mixed reader/narrator levels; ﴿بِٱلْهُدَىٰ﴾ carries two different actions (إمالة for حمزة/الكسائي/خلف العاشر, تقليل for ورش) with ورش's بخلف عنه second وجه stored as a non-default alternate; a 1:3→1:4 locus round-trips; `qiraat_export_page()` emits usable fixture JSON with deduplicated الشواهد; and RLS shows the anon role **0** entries until one is PUBLISHED, then exactly that one. The QA views independently caught **two real contradictions in the supplied extraction** — page 12 ﴿لا تعبدون﴾ lists حمزة and الكسائي under *both* أوجه (leaving يعقوب unaccounted for), and page 14 ﴿قلوبهم العجل﴾ lists خلف under two of three — plus page 8's corrupted ﴿وَعَٰدْنَا﴾ header and the ~15 `؟` markers on pages 9/11/16/19; all import as `NEEDS_MANUAL_REVIEW` with open error flags, never as trusted data. Files: `supabase/migrations/20260917120000_qiraat_v2_schema.sql` (new, 1037 lines), `docs/qiraat/10-v2-architecture-plan.md` (new), `supabase/migrations/20260916120000_qiraat_ashr_schema.sql` (bannered SUPERSEDED — never applied, nothing depends on it). **DB migration: written and validated, NOT applied** — needs a `pg_dump` backup and an explicit go-ahead per CLAUDE.md. No application code changed; the 79 shipped fixture records still render exactly as before.
- **Feature — Qiraat Ashr `RULES_TABLE_REGION` imported: page-level rules (عدّ الآي، الإدغام الكبير، أوجه الوصل بين السورتين، المد قبل الإدغام) for Mushaf page 1, new `QiraatRule` domain type:** follow-up to the pages 1–20 batch below, which had scoped `RULES_TABLE_REGION`/`FAWAID_REGION` out. New `QiraatRule` type (`packages/qiraat-core/types.ts`) models a page-level Qiraat convention NOT anchored to one Quran token — deliberately kept separate from `QiraatVariant` rather than force-fit into it. 8 new rules for page 1 (the only page in the batch whose `RULES_TABLE_REGION` isn't itself `needs_high_resolution_transcription`): 2 عدّ الآي rules (بسملة الفاتحة، أنعمت عليهم) attributed to **ayah-counting schools** (المكي/الكوفي/المدنيان/البصري/الشامي) — an entirely different, unrelated taxonomy from the ten-reader/twenty-narrator `ReadingId` model, so `readingIds` is optional and left unset for these, with a new free-text `attributionLabel` field carrying the school names instead (never guessed/force-fit); 1 الإدغام الكبير rule (السوسي عن أبي عمرو); 4 الأوجه بين السورتين rules (بسملة/وصل/سكت أو وصل/سكت أو وصل أو بسملة) that DO resolve onto real `ReadingId`s; 1 المد قبل الإدغام الكبير rule with 3 unattributed enumerated options (القصر/التوسط/الإشباع) and no reader tie at all. One resolution required care: the "الوصل" rule's attribution `["حمزة", "خلف"]` has a bare "خلف" ambiguous between narrator "خلف عن حمزة" (Q06-R01) and reader "خلف العاشر" (Q10) — resolved as Q10 after confirming the four أوجه بين السورتين rules only partition the full 20-reading set exactly once (no gap/overlap) under that reading; the generator script asserts this at build time and a new test re-checks it. Wired through `FixtureQiraatRepository.getRulesForPage()` (new `PAGE_RULE_LOADERS`, same one-line-per-page pattern as variants), returned by `GET /api/mushaf-1441/qiraat` as a new `rules` array alongside `variants`, and rendered in the reader's burger-menu "القراءات" panel under a new "قواعد ذات صلة بهذه الصفحة" section (comparison/riwayah mode only, reusing the existing colored reader-pill component for attribution). 1 new regression test (attribution/taxonomy checks, the 20-reading partition assertion) — 23/23 passing; `tsc`/`next build`/all Mushaf-1441 validators clean; Playwright-verified locally on page 1 (all 8 rules render with correct categories, text/reading/options and attribution) with no new console errors. Files: `packages/qiraat-core/types.ts` (`QiraatRule`, `QiraatSource.ruleId`), `packages/qiraat-core/repository.ts`, `packages/qiraat-core/fixtures/rules/page-001.json` (new), `scripts/build-qiraat-rules-page-001.py` (new), `src/app/api/mushaf-1441/qiraat/route.ts`, `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx` (`qiraatRulesByPage` state, `renderQiraatRules`), `packages/qiraat-core/engine.test.mjs`, `docs/qiraat/pages-011-020-dedup-and-import.md` (updated). **DB migration:** none — fixture-only, same as everything else in this batch.
- **Feature — Qiraat Ashr data for Mushaf pages 11–20 (Al-Baqarah 74–132), REVIEWED tier; pages 1–10 confirmed already covered (no duplicates added):** the task supplied a `qiraat-extraction-v2.0` payload for pages 1–20 with an explicit `import_policy` (`allowed_status: [reviewed]`, `blocked_status: [needs_manual_review, needs_high_resolution_transcription]`). Cross-checked all 24 pages 1–10 loci against the existing fixtures from the 2026-09-16 session — every one already present with matching attribution, nothing re-added. Added 39 new `QiraatVariant` records across 31 loci for pages 11–20 (فهي/تعملون سكون الهاء 2:74; أماني/خطيئته 2:78/81; تظاهرون/عليهم/أسارى/تفادوهم/وهو/تعملون/القدس 2:85/87; ينزّل/قيل إشمام/وهو 2:90/91; يعملون/جبريل×2/ميكال 2:96–98; ولكن+الشياطين/ينزّل 2:102/105; ننسخ/ننسها/أمانيّهم/وهو×2/خوف+عليهم 2:106–112; وقالوا/فيكون/تسأل 2:116–119; واتخذوا/فأمتعه 2:125/126; ووصى 2:132), all held at `REVIEWED` — unlike the pages 1–10 batch, this batch's `needs_manual_review` loci (P012-L003 تعبدون, P012-L004 حسنا, P014-L004 يأمركم, P019-L001/P020-L001 إبراهيم-إبراهام, P020-L002 وأرنا) are dropped **entirely** per the new explicit policy, not kept as debug-only placeholders. `RULES_TABLE_REGION`/`FAWAID_REGION` (waqf/عدّ الآي rules, poetic شواهد) are out of scope — a structurally different domain than a per-word `QiraatVariant`, and independently still `needs_high_resolution_transcription`. As with pages 1–10, every `hafsText` is derived from the real Mushaf-1441 word fixtures, never hand-typed; a new `splice()` helper in the generator substitutes only the differing root into a real prefix-bearing compound token (2:97/98 جبريل within `لِّجِبْرِيلَ`/`وَجِبْرِيلَ`, 2:98 ميكال within `وَمِيكَىٰلَ`) without ever retyping the real prefix. 2 new regression tests (batch-wide REVIEWED-only enforcement, the جبريل/ميكال splice invariant) — 24/24 passing; `tsc`/`next build`/all Mushaf-1441 validators clean; Playwright-verified locally (pages 11, 15, 20 in مقارنة القراءات mode — فهي, تعملون, يعملون, جبريل ×2, ميكال, ووصى all render the correct colored marker) with zero Qiraat-related console errors. Files: `packages/qiraat-core/fixtures/pages/page-{011..020}.json` (new), `packages/qiraat-core/repository.ts` (pages 11–20 wired into `PAGE_VARIANT_LOADERS`), `packages/qiraat-core/engine.test.mjs`, `scripts/build-qiraat-pages-011-020.py` (new), `data/qiraat/raw/pages-001-020-input.json` (new, the task's raw payload), `data/qiraat/raw/pages-011-020.json` (new, generated), `docs/qiraat/pages-011-020-dedup-and-import.md` (new). **DB migration:** none — still the unapplied `supabase/migrations/20260916120000_qiraat_ashr_schema.sql`; this batch is fixture-only, same as pages 1–10.

## 2026-09-16
- **Change — Qiraat "include reviewed" (developer mode) now defaults ON:** every Qiraat Ashr record imported so far (pages 1–10) sits at `REVIEWED`/`NEEDS_MANUAL_REVIEW` tier — none has reached `VERIFIED`/`PUBLISHED` — so the debug "عرض بيانات قيد المراجعة (غير معتمدة بعد) — لأغراض التطوير" checkbox previously had to be ticked by hand every session before any marker would show in مقارنة القراءات/القراءة برواية mode. `qiraatIncludeReviewed`'s `useState` default flipped `false`→`true`; confirmed this field is not part of the persisted `QiraatPrefs` shape (`localStorage` key `mushaf1441:qiraat-prefs:v1`) so the restore effect never fights it, and the checkbox itself is untouched (still there, still togglable, just starts checked). Playwright-verified on page 1 in مقارنة القراءات mode: مَلِك/الصراط/عليهم markers render on first load without touching the checkbox. Files: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`. No data change.
- **Fix — Qiraat hover popup: dropped the baseline reading, click-to-dismiss:** two corrections to the previous redesign. (1) Removed the "baseline reading" وجه card added in the prior pass (the readers who simply agree with Hafs, e.g. عاصم/الكسائي/يعقوب/خلف العاشر for 1:4 مالك) — per explicit direction the hover peek must only ever mention who **differs** from Hafs and how, never Hafs's own reading or whoever matches it; that full picture (including "الباقون") stays exclusive to the full detail panel opened by a second tap. (2) The peek previously had no way to dismiss itself except hovering/tapping a different marked word — clicking anywhere, including directly on the popup, now closes it: the marked word's own click handler `stopPropagation()`s (so opening/switching a peek is never immediately undone by the same click), a new `document` `click` listener (mirroring the existing context-menu/mutshabehat-popup Escape-key pattern) closes the peek on any other click, and the popup card itself also closes on click directly. Verified locally (Playwright): the 1:4 مالك/ملك peek now shows exactly one وجه card (مَلِك، 6 readers) with no baseline group; clicking on the card and clicking far outside both close it. Files: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`. No data change.
- **Feature — Qiraat Ashr hover popup redesigned to match a reference layout (large per-وجه cards, and the baseline reading shown as its own group):** three follow-up refinements. (1) Each reading ("وجه") is now its own bordered card: a badge naming the exact ruling (`تشكيل`/`تقليل`/`إمالة`/`إدغام`/`تحقيق أو إبدال الهمزة`/`مد`/…, taken from the variant's `performanceNote` when present, else its `differenceType` category — the generic "(اختلاف أداء)" placeholder is gone), the word itself large and clear (`text-3xl`, Amiri Quran font, was `text-sm`), a "القرّاء والرواة:" label, and the colored reader/narrator pills below it; a "الوجه N" counter badge appears whenever there is more than one group. (2) **The baseline (Hafs-matching) reading is now shown as its own وجه too** — previously the popup only showed the *documented* variant(s), which for a word like 1:4 مالك/ملك meant the four readers who read like Hafs (عاصم، الكسائي، يعقوب، خلف العاشر) were invisible; the baseline group's readers are computed as the 20-reading set minus everyone already covered by a shown variant (never guessed, never a new data field) and rendered first. (3) Reader pills now read "الإمام {short name}" (e.g. "الإمام عاصم", not "عاصم الكوفي") matching the reference design — new `nameArShort` field on `QiraatReader` (`packages/qiraat-core/types.ts`, `readers.ts`), via a fixed lookup table, never string-split from `nameAr` (Q10 stays "خلف العاشر" in full — critical, since truncating to "خلف" would collide with the unrelated narrator Q06-R01); narrator-only pills read "الراوي {name}" using the existing (already-short, or intentionally full for the two disambiguated "الدوري" narrators) `nameAr` unchanged. `DIFFERENCE_TYPE_LABELS_AR.HARAKAH` relabeled 'حركة'→'تشكيل' to match the requested terminology (only other caller, the full detail panel's "نوع الاختلاف" line, updated automatically). 20/20 tests still passing, `tsc`/`next build` clean, all Mushaf-1441 validators still passing; Playwright-verified against 1:4 مالك/ملك — two boxed وجه cards (القراءة الأصلية: عاصم/الكسائي/يعقوب/خلف العاشر; حرف: نافع/ابن كثير/أبو عمرو/ابن عامر/حمزة/أبو جعفر) with large legible text and pill colors matching `readers.ts` exactly. Files: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, `packages/qiraat-core/{types.ts,readers.ts}`. No data change.
- **Feature — Qiraat Ashr: colored reader/narrator pills in the hover popup:** the hover peek's attribution line was a plain grey comma-separated narrator-name string; it's now one colored pill per reader (their own identity color, a small dot + tinted background/border) — one pill for a whole reader when both of their narrators apply to that variant (Part 10 Case B), or one pill per individual narrator (narrator color) when only one of the two applies (Case A) — matching the palette the user confirmed already matches `packages/qiraat-core/{readers,narrators}.ts` exactly, and the pill-badge visual style from a reference screenshot. New `readerPillsForReadingIds()`/`renderReaderPill()` in `Mushaf1441Viewer.tsx`; the full detail panel (opened by a second tap/click) is unchanged — this was scoped to the hover popup only. Verified locally (Playwright) against the 1:4 مالك/ملك variant: 6 correctly colored reader pills (نافع بلو، ابن كثير أخضر، أبو عمرو سماوي، ابن عامر بنفسجي، حمزة أحمر، أبو جعفر ذهبي) matching each reader's exact hex from `readers.ts`. No data change.
- **Feature/Fix — Qiraat Ashr: mobile tap-to-peek, a distinct performance-ruling color, and only differing readers on the peek:** four follow-up fixes to yesterday's hover popup. (1) The peek popup only ever rendered on desktop (`lg:block`) and, worse, mobile taps silently did nothing at all — a first tap's synthetic `mouseenter`/`mouseleave` (fired by Chromium's touch emulation and some real devices) cleared the peek state the instant after the tap's own click handler set it, because the click handler was reading `hoveredQiraatWord` from a stale closure (the word buttons live inside a memoized `MushafPageSlot` that intentionally does not re-render on hover-state-only changes) and so never recognized "this word is already peeked." Fixed by (a) moving hover tracking from `onMouseEnter`/`onMouseLeave` to `onPointerEnter`/`onPointerLeave` gated to `pointerType === 'mouse'`, so touch taps no longer fire the hover logic at all, and (b) mirroring the peeked word's id into a ref (`hoveredQiraatWordIdRef`) that the click handler reads instead of the (possibly stale) state value. The popup itself is no longer desktop-only. Net effect: on mobile, a first tap on a marked word now shows the same lite peek as desktop hover; a second tap on the same word opens the full detail panel — exactly mirroring desktop's hover-then-click. (2) A phonetic/performance-only ruling (إمالة، تقليل، إدغام، سكت، إشمام، اختلاس...) now gets one fixed, distinct color (`#4F46E5`) applied to the word's own text (not just the underline) instead of the usual reader/narrator identity color — recognizable as "this word has a recitation ruling" at a glance across the page; an ordinary textual/spelling variant is unaffected and keeps the existing identity-color scheme. New `WordMarker.isPerformanceOnly` (set when every matched variant at that token has `variantText === hafsText`) drives both the underline and the word's `color` style. (3) The comparison/detail-panel peek already only ever listed the variant's own `readingIds` (the readers who documented-ly differ from Hafs) — confirmed this was already correct and added a regression test rather than a behavior change. (4) Removed the "قيد المراجعة (غير معتمدة بعد)" preview badge from both the hover popup and the full detail panel (every record in the pages-1-10 batch is currently at that tier, so it was showing on nearly every marked word) — the more serious "تحتاج مراجعة يدوية" (NEEDS_MANUAL_REVIEW) badge is unaffected. New regression test (`packages/qiraat-core/engine.test.mjs`: performance-only marker color) — 20/20 passing; `tsc`/`next build` clean; Playwright-verified on an iPhone 13 viewport (first tap peeks, second tap opens full detail, computed `color: rgb(79, 70, 229)` on a performance-only word vs. default black on textual ones, zero Qiraat-related console errors) and on desktop. Files: `src/app/mushaf-1441/_components/qiraat/qiraatWordMarker.ts` (`PERFORMANCE_MARKER_COLOR`, `isPerformanceOnlyVariant`), `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx` (pointer-event rewrite, `hoveredQiraatWordIdRef`, word text color, badge removal), `packages/qiraat-core/engine.test.mjs`.
- **Feature — Qiraat Ashr: desktop hover preview + a dedicated toggle icon:** hovering a Qiraat-marked word (comparison/riwayah mode) now shows a lightweight floating popup — same visual slot as the existing annotation hover card — with the Hafs→variant diff (or the performance note for a phonetic-only difference), verification-status badge, and a truncated attribution list; clicking the word still opens the full detail panel with every variant, source and the complete attribution. A new "ق" icon now sits beside the burger-menu button in the reader's top bar, toggling مقارنة القراءات on/off in one tap (`aria-pressed`, persisted via the existing Qiraat-prefs `localStorage` key) — previously the feature was reachable only from inside the burger menu, with no visible affordance that it existed. Files: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx` (`hoveredQiraatWord` state, `renderQiraatHoverCard`, the toggle button). No new data, no DB change; verified locally (Playwright) that the popup appears on hover and disappears on mouse-leave, the icon's `aria-pressed`/style track `qiraatMode` correctly, and the existing per-page-slot memoization invariant is untouched (the new state is not part of any `MushafPageSlot` prop, exactly like the pre-existing `hoveredAyahKey`).
- **Feature — Qiraat Ashr data for Mushaf pages 1–10 (Al-Fatihah + Al-Baqarah 1–69), REVIEWED tier:** extends the page-1 Qiraat Ashr prototype (below) with 40 `QiraatVariant` records across all 10 pages / 28 conceptual loci, covering every locus the task specified: single-word variants, phonetic/performance-only differences (`variantText === hafsText` + a `performanceNote`, e.g. إشمام, تقليل, سكت — never faked as a spelling change), and multi-target loci (one conceptual source location spanning more than one word or occurrence — 2:37 آدم+كلمات, 2:2/2:5 هدى, 1:7/2:6/2:61 عليهم, 2:11/2:13/2:59 قيل, 2:54 بارئكم×2, 2:67 هزوا's 3-way split including a computed "الباقون" set-difference). No PDF (`مصحف القراءات العشر-1.pdf`) was available in this environment to do the task's required page-image check, so per the user's explicit choice every record is held at `REVIEWED` (new `VerificationStatus` value `NEEDS_MANUAL_REVIEW` for the 5 items the task itself flagged as uncertain — 2:20 شاء الله, 2:54 بارئكم, 2:61 عليهم + النبيين, 2:67 يأمركم — none promoted, none guessed, 6 of the 40 records carry empty `readingIds` because no attribution was confident enough to assert) — **nothing in this batch is shown to an ordinary user yet** (`PUBLIC_VERIFICATION_STATUSES` still gates on `VERIFIED`/`PUBLISHED` only), reachable only through the existing debug "include reviewed" toggle for review against the actual PDF. The existing architecture was reused rather than duplicated: the task's proposed `qiraat_persons`/`qiraat_targets` tables are just the prototype's reader/narrator model plus a new `locusId`/`locusType` field grouping independently-resolving single-token records — no new join tables, no second Quran dataset. Found and fixed two real bugs while building this: (1) `comparisonMarkerForWord()` threw (`computeAttribution requires at least one reading id`) when a `NEEDS_MANUAL_REVIEW` placeholder with empty `readingIds` was visible in debug mode — exactly the case a reviewer needs to open; now returns a neutral `unresolved` marker instead of guessing a color, and the detail panel no longer shows a misleading "N رواية بلا تغيير" line for these records. (2) the dataset generator originally hand-typed `hafsText`; diffing against the real Mushaf-1441 word fixtures caught combining-mark-ordering drift and one incomplete multi-token span (2:20's `شاء الله`) — the generator (`scripts/build-qiraat-pages-001-010.py`, idempotent) now derives `hafsText`/performance-only `variantText` directly from the real, unmodified word fixtures, with an NFC-normalized sanity check against the hand-typed literal so a wrong token reference still fails loudly. Reconciliation invariants required by the task all hold: 0 base-Quran-fixture changes, 0 duplicate loci, 0 unresolved word IDs (checked programmatically, not by inspection). 3 new regression tests (multi-word locus grouping, performance-only variant shape, the empty-`readingIds` crash guard) plus 2 existing page-1 tests updated for the corrected مالك/ملك attribution (6 readers/12 narrators, not the earlier incorrect 8/16) — 19/19 passing; `tsc`/`next build`/all existing Mushaf-1441 validators clean; Playwright-verified locally (pages 1, 4, 6, 10 in comparison mode plus the page-4 detail panel for the شاء الله placeholder) with zero Qiraat-related console errors. Per the task's explicit stop condition, page 11 onward was never touched (confirmed both by `git status` and by the live API returning `[]`). Files: `packages/qiraat-core/types.ts` (`NEEDS_MANUAL_REVIEW`, `LocusType`, `locusId`/`locusType`/`performanceNote` fields), `packages/qiraat-core/repository.ts` (pages 2–10 wired into `PAGE_VARIANT_LOADERS`), `packages/qiraat-core/fixtures/pages/page-{001..010}.json` (page-001 corrected, 002–010 new), `packages/qiraat-core/engine.test.mjs`, `src/app/mushaf-1441/_components/qiraat/qiraatWordMarker.ts` (crash fix), `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx` (detail-panel copy fix), `scripts/build-qiraat-pages-001-010.py` (new), `data/qiraat/raw/pages-001-010.json` (new), `docs/qiraat/pages-001-010-*.md` (new: existing-architecture, word-mapping, reconciliation, final-report, visual-verification). **DB migration:** none — still the unapplied `supabase/migrations/20260916120000_qiraat_ashr_schema.sql` from the prototype; this batch is fixture-only, same as page 1.
- **Fix — Every page took ~7 s to load whenever the self-hosted backend was unreachable (site "not working" with no login page to fall back to):** the app already degrades gracefully when Supabase can't be reached (empty lists, 401/503 handling — see prior changelog entries), but only *after* the failing call actually settled. Root cause: `@supabase/postgrest-js` retries every `GET`/`HEAD` query up to 3 times with exponential backoff (1 s, 2 s, 4 s = 7 s total) on any fetch rejection that isn't an `AbortError`, and the `(app)` layout alone makes one such query on every navigation (plus more per page) — against a genuinely down backend (Mac Mini offline, Tailscale Funnel down) this turned an instant connection failure into a multi-second stall per query, on every single page. `resilientFetch` (`src/lib/resilient-fetch.ts`, the shared fetch used by every Supabase server client — `proxy.ts`, `supabase-server.ts`) now (1) sets explicit connect/headers/body timeouts on its `undici` `Agent` (previously unbounded — 10 s/300 s/300 s defaults — for the separate case of a backend that accepts a connection but never responds) and (2) normalizes every fetch rejection to an `AbortError`, which postgrest-js treats as final and never retries — this module already owns the one failure mode that's actually transient here (DNS, via system lookup → DNS-over-HTTPS → last-known-good), so a second generic retry layer on top just adds latency during a real outage without adding resilience. Verified locally against a fully unreachable dummy backend: every one of `/`, `/stats`, `/network`, `/test`, `/tools`, `/settings`, `/automated`, `/surahs`, `/groups/new`, `/mushaf-1441` went from ~7 s to well under 100 ms, with identical (already-correct) empty-state content and no new console/page errors (Playwright-verified). File: `src/lib/resilient-fetch.ts`. No DB change.
- **Feature — Qiraat Ashr (ten canonical Quran readings) prototype on Mushaf page 1:** a full,
  tested architecture (not just scaffolding) for the ten readers / twenty narrators / twenty
  Riwayat, integrated into the Mushaf 1441 reader as an additional layer alongside the existing
  Hafs baseline and Mutashabihat highlighting. Three modes reachable from the reader's burger menu
  under a new "القراءات" panel: **المصحف** (normal, unchanged Hafs, the default — Qiraat code paths
  don't even run), **مقارنة القراءات** (Hafs text stays displayed; a color-coded underline marks
  words with documented variants — solid narrator/reader color for one narrator or a whole reader,
  a segmented gradient for several readers sharing one variant; filterable by الكل/قارئ/رواية; a
  Study Mode toggle thickens the marker and adds a light background tint), and **القراءة برواية**
  (renders the entire page according to any of the 20 selected Riwayat, switchable instantly with
  no navigation, with an optional "إظهار الاختلاف عن حفص" marker). Tapping a marked word opens the
  existing (previously-empty-placeholder) "قراءات" detail-panel tab with the full attribution
  (readers grouped with their narrator pairs), difference type, verification status, and source
  citation. New framework-agnostic domain package `packages/qiraat-core/` (types, canonical
  reader/narrator/reading IDs and colors, a deterministic token-anchored rendering engine
  supporting KEEP/REPLACE/INSERT/DELETE/MERGE/SPLIT/DIACRITIC_CHANGE/ORTHOGRAPHIC_CHANGE, an
  EXTRACTED→MAPPED→REVIEWED→VERIFIED→PUBLISHED verification lifecycle that keeps unverified data
  out of the default view, and a fixture-backed `QiraatRepository` — superseding its earlier empty
  scaffold). New route `GET /api/mushaf-1441/qiraat?page=N` (no auth needed — shared reference
  data, same cache strategy as `page-words`). Seeded with one real, classically-cited variant
  (1:4 مالك/ملك, VERIFIED, 8 readers/16 narrators) and two REVIEWED-tier ones (1:6–1:7 الصراط
  سين/صاد, Hamzah) plus clearly-labeled SYNTHETIC fixtures exercising every remaining operation and
  attribution case (never rendered in the app). New CSS tokens `--q01-*`…`--q10-*` in
  `globals.css`. 16 new unit tests (`packages/qiraat-core/engine.test.mjs`, all passing), the whole
  Mushaf-1441 fixture set untouched and its 7 existing validators still passing, `tsc --noEmit` and
  `next build` clean, and manual Playwright/Chromium verification of every mode, the multi-reader
  marker, Riwayah switching, the difference-from-Hafs toggle, and Study Mode (screenshots and full
  results in `docs/qiraat/08-test-results.md`). Files: `packages/qiraat-core/*` (rewritten from the
  earlier stub), `src/app/api/mushaf-1441/qiraat/route.ts` (new),
  `src/app/mushaf-1441/_components/qiraat/*` (new: `QiraatToolbar`, `QiraatLegend`,
  `qiraatWordMarker.ts`), `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`,
  `src/app/globals.css`, `scripts/validate-mushaf1441-phase5.mjs` (rewritten for the real
  integration). **DB migration written, NOT applied** (no backend connectivity from this session —
  see `docs/qiraat/03-database-schema.md`): `supabase/migrations/20260916120000_qiraat_ashr_schema.sql`.
  Full design docs: `docs/qiraat/00`–`09`. **Not done**: full-Quran data (only page 1 has real
  content — see `docs/qiraat/09-full-rollout-plan.md` for what that needs; this was intentional
  per the task's own "prototype first" sequencing, not an oversight).

## 2026-09-15
- **Performance — Mushaf page turns render in ~1 ms (finishes the Codex "2 ms rendering" task):** the reader now keeps the current page/spread plus one neighbour on each side mounted as memoized `MushafPageSlot`s stacked in one grid cell; a turn only flips which slot is visible. Measured on a local production build (Chromium, prefetched turns): React render→commit **0.8–1.3 ms**, click→commit **1.8–2.4 ms**, **0 page slots re-rendered** (desktop spread and phone, with and without the curl); before: 9–31 ms per turn. New neighbours mount afterwards in a deferred pass (`useDeferredValue`, 2–3 ms each), and both pages of the next/previous spread are prefetched. Page slots read session-wide highlight/annotation maps so their props stay identical across turns; slot event handlers go through a live ref; re-fetched annotations that didn't change keep the same array. The page curl now animates the real on-screen page elements (clip-path on the outgoing page, reflection transform on the incoming page as the back of the sheet) instead of rendering three extra page copies, and no longer forces a layout at turn start. Codex's earlier commits (`272e4d3`, `2ef9d83`: readable fallback text before the QCF font, bundled metadata, request de-duplication, font preload) were reviewed and kept. Regression: `tests/test_mushaf_page_performance.py::test_prefetched_turn_reuses_the_mounted_neighbor_page`. Files: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, `src/app/mushaf-1441/_components/PageCurlOverlay.tsx`, `tests/test_mushaf_page_performance.py`, `scripts/validate-mushaf1441-objective.mjs` (context-menu token now goes through `liveRef`). No DB change.
- **Performance — Mushaf pages become readable immediately instead of waiting for the QCF font:** page text now paints with its Uthmani/Amiri fallback as soon as page data is available, then swaps to the exact page-specific QCF glyph font when it finishes loading. The initial font is preconnected/preloaded before hydration; page-word and metadata payloads are combined; concurrent page/prefetch requests share one in-flight promise; and the database-backed home route is no longer prefetched from the reader. Live baseline page turns were 794–1,087 ms because font loading gated text; local production-build app render/update is now 6.8–11.7 ms on an uncached turn and 3.7–5.9 ms on a prefetched turn across 1440×900, 1180×820 and 390×844 (below one 60 Hz frame; a literal 2 ms browser guarantee is not technically achievable). Files: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, `src/app/api/mushaf-1441/page-words/route.ts`, `tests/test_mushaf_page_performance.py`. Verification: `npx tsc --noEmit -p .` (exit 0); `npm run mushaf:validate` (all validators passed); `env -u __NEXT_PROCESSED_ENV npm run build` (exit 0, 28 static pages); `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q tests/test_mushaf_page_performance.py` against `next start -p 3222` (4 passed); Chromium checks at all three target viewports (readable at DOMContentLoaded, one neighbour page request, zero separate metadata/home-prefetch requests, zero console/page/HTTP errors, page curl completed). No DB change.
  - Production verification separates public network latency from rendering: a cold page-data request took 650.6 ms but rendered in 7.4 ms after `responseEnd`; warmed requests took 63–69 ms and rendered in 4.4–4.6 ms. The browser regression therefore allows up to 5 s for public data arrival while retaining a strict `<50 ms` post-response render assertion.
- **Feature — Groups list: collapsed and magazine card views (desktop / iPad landscape):** the home list has two new views, picked from the العرض select or the new قائمة / مطوية / مجلة switch shown at ≥1024px. **بطاقات مطوية** shows equal cards with only the colour, number, title, surahs, ayah count and actions; clicking a card expands it across the full row with its colour-coded ayat and open/edit buttons. **مجلة** is an editorial mosaic that fills the page width: a dense grid (3/4/5 columns by width) of unequal tiles — hero, tall, wide, regular, short — sized by the group's text length with a stable per-group hash for variety, ayat faded out at the tile edge. Both views drop the page's max width and show 24 groups per page; phones and portrait tablets keep the normal list. Files: `src/components/GroupCardGrid.tsx` (new), `src/components/SortBar.tsx`, `src/app/(app)/page.tsx`. No DB change.
- **Feature — Mushaf iBooks-style page curl (replaces the swing flip):** turning a page now peels it like iPad Books. The outgoing sheet folds along a moving line from its outer bottom corner; the flat part stays, the folded flap shows the back of the sheet (in a spread: the page that lands on the other side, correctly oriented; on a single page: paper with the print showing through mirrored), with a crease highlight, a drop shadow and a shadow cast on the page revealed underneath. Buttons, wheel, keys, taps and the page slider play it automatically (~0.7 s); dragging a page (touch or mouse) curls it under the finger — release past ~30% or with a flick to complete, otherwise it falls back to the page it started from. The overlay renders the outgoing sheets once and updates clip-path/transform/SVG gradients per frame; their highlights stay on while they curl. Skipped with reduced motion and on the saved-page resume. The old touch-swipe handlers are replaced by the pointer drag. Files: `src/app/mushaf-1441/_components/PageCurlOverlay.tsx` (new), `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`. No DB change.
- **Feature — Test mode: words test, whole-Quran source, right/wrong statistics:** `/test` now has a question source (مجموعاتي / القرآن كاملًا) and a new mode **الكلمات المتشابهة**: one word/part of the ayah is hidden and you pick it from the similar wording. For personal groups the hidden part is a coloured diff/addition/unique part and the choices are the matching parts of the other locations; for the whole Quran the choices are words that follow the same one or two preceding words elsewhere in the Quran (pause marks stripped so they don't hint). Whole-Quran location questions offer surahs where an ayah opens with the same words (or, with a surah filter, nearby ayah numbers); identical wording elsewhere counts as correct. Mixed now rotates location/words/flash. Every answer is saved (`POST /api/test/answers` → `test_answers`) and the statistics tab shows correct, wrong and accuracy overall and per test type/source (`get_test_answer_stats`). Missed whole-Quran questions link to their mushaf page. Files: `src/lib/test-questions.ts`, `src/lib/test-questions-quran.ts` (new), `src/components/TestRunner.tsx`, `src/components/TestAnswerStats.tsx` (new), `src/app/(app)/test/page.tsx`, `src/app/(app)/stats/page.tsx`, `src/app/api/test/answers/route.ts` (new), `src/app/globals.css` (`.part-blank`). **DB migration:** `supabase/migrations/20260915180000_test_answers.sql` (applied on self-host after a pg_dump backup `backups/pre-test-answers-20260915.dump`).
- **Change — Mushaf bottom slider moves by page, not surah:** the quick slider now spans pages 1–604 (page 1 on the right). While dragging, the bubble shows the page number and the surah on that page; releasing (or arrow keys) jumps straight to that page, with the flip animation. The label beside it reads `ص N · surah`. File: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`. No DB change.
- **Feature — Mushaf paper page-turn animation:** turning pages (arrows, wheel, swipe, taps, slider jumps) now plays a short 3D page flip. The incoming page swings flat from the spine (spread) or page edge (single page) — forward sweeps left→right, back right→left — with a passing shadow, while the page revealed underneath brightens out of the leaf's shadow. Uses the Web Animations API on `[data-mushaf-leaf]` elements (transform/filter only, no layout change), 520 ms, and is skipped when the OS asks for reduced motion. File: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`. No DB change.
- **Feature — Mushaf two-page spread on desktop and iPad landscape:** at `(min-width: 1024px) and (orientation: landscape)` the reader shows an open-book spread: the odd page on the right, its even partner on the left, with a soft spine shadow. Arrows, wheel, swipe and tapping a page (left page → forward, right page → back) turn a whole spread; `?page=` still records the page navigated to. The partner page's words, fonts, metadata, highlights and notes are loaded too, and the next/previous spread is prefetched. Phones and portrait tablets keep the single page. `renderLineWords` now renders any page (page, number, metadata, layout). File: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`; page-words validator token updated. No DB change.
- **Change — Mushaf ayah card opens collapsed:** tapping a highlighted ayah now shows only the group titles (colour swatch, title, number of locations, chevron). Tapping a title expands that group's ayat and open/edit actions (tap again to collapse); a group's details are fetched only when first expanded. File: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`. No DB change.
- **Fix — Mushaf highlight is one continuous band per ayah and appears instantly:** the space between words is now a flex filler (`renderWordGap`) that grows like `justify-between` and takes the highlight colour when both neighbouring words share it, so an ayah's colour is continuous instead of word-by-word. All of the user's ayah→group links are loaded on the server with the page (`src/lib/mushaf-mutshabehat.ts`, also used by `/api/mushaf-1441/mutshabehat`) and filtered per page locally, so highlights render in the first paint and on every page turn without a request (API fetch remains only as fallback). Files: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, `src/app/mushaf-1441/page.tsx`, `src/lib/mushaf-mutshabehat.ts`, `src/app/api/mushaf-1441/mutshabehat/route.ts`, validators read the new loader. No DB change.
- **Fix — Mushaf mutashabihat highlight was only a font colour:** ayat in your personal groups now get a highlighter-style background band, with a different soft colour per group (10-colour palette, picked deterministically from the group id so a group keeps its colour on every page). The ayah card shows the group's colour swatch and tints the current ayah with it. File: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`. No DB change.
- **Feature — Mushaf resumes the last page:** every page change is saved (`localStorage` key `mushaf1441:last-page:v1`); opening the reader without `?page=` (Mushaf tab, home → back, after opening a group) jumps to the saved page instead of page 1. An explicit `?page=N` link always wins. File: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`. No DB change.
- **Feature — Mushaf 1441: surah banner redesign, personal mutashabihat highlight + card, wheel paging:** the surah header is now one scalable SVG (gold band, lattice, pointed cartouche, surah-number and ayah-count medallions) with the name positioned by geometry, fixing the misaligned/clipped name. Ayat that belong to your personal groups are drawn in one consistent teal with a dotted underline (`PERSONAL_AYAH_HIGHLIGHT`); clicking/tapping one opens a card with every group containing it, all similar ayat (colour-coded parts, current ayah marked), "jump to ayah", open and edit links (Esc/backdrop closes). Mouse wheel/trackpad turns pages (down → next, one page per gesture, ignores momentum; disabled while menus or the card are open). `/api/mushaf-1441/mutshabehat` reads the user from the session cookie (no auth-server round-trip). Files: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, `src/app/api/mushaf-1441/mutshabehat/route.ts`, validators `objective` + `supabase-interactions` updated. No DB change.
- **Feature — Copied automated candidates are tagged and moved to the end:** personal groups now record `source_automated_id` (set by `/api/automated/copy`; earlier copies backfilled by exact title, lowest automated id when titles repeat). New view `automated_groups_with_copy` adds a per-user `copied` flag; `/automated` and the surah page's automated tab order by `copied, id`, so copied items sink to the end. Copied cards get a magenta "منسوخة" badge, soft border and tint (new tokens `--color-copied`, `--color-copied-bg`); the detail page shows the badge and a link to the personal copy instead of the copy button. **DB migration:** `supabase/migrations/20260915150000_automated_copy_tracking.sql`. Files: `src/components/AutomatedCard.tsx`, `src/app/(app)/automated/page.tsx`, `src/app/(app)/automated/[id]/page.tsx`, `src/app/(app)/surahs/[no]/page.tsx`, `src/app/api/automated/copy/route.ts`, `src/app/globals.css`.
- **Feature — Mushaf tab:** "المصحف" in the sidebar and mobile menu opens `/mushaf-1441`; the reader's top bar has a home button back to the app. Files: `src/components/Sidebar.tsx`, `src/components/MobileTopbar.tsx`, `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`. No DB change.
- **Fix — "Database not loaded" (Vercel DNS can't resolve the Funnel host, now also in `bom1`):** server-side Supabase clients (`src/lib/supabase-server.ts`, `src/proxy.ts`) use `resilientFetch` (`src/lib/resilient-fetch.ts`): undici Agent whose lookup tries system DNS, then DNS-over-HTTPS (Cloudflare → Google, TTL-cached), then the last known good IPs. TLS still validates the real hostname. `SUPABASE_FORCE_DOH=1` forces the DoH path for testing (verified locally: auto-login test + pages with data). New dependency `undici`. No DB change.
- **Feature — Mutashabihat test mode (`/test`, nav "اختبار"):** setup screen (mode, group scope all/favourite/completed/draft, surah, 10/20/50 questions). *اختيار الموضع*: the ayah is shown without colour hints and you pick its surah + ayah among the group's similar locations (identical wording counts for either). *بطاقات الاستذكار*: the location is shown, you recall, reveal and self-grade. *مختلط* alternates. After each answer every similar location is shown with colour-coded differences; the result screen lists missed groups with links. Files: `src/app/(app)/test/page.tsx`, `src/components/TestRunner.tsx`, `src/lib/test-questions.ts`, `Sidebar`, `MobileTopbar`. No DB migration.
- **Fix — Mushaf 1441 corrupted pages (data):** the importer placed every word on the page where its verse *starts* (`verses/by_page`), but verses spill onto the next page and QCF V2 glyphs only render with their own page's font. 25 pages had lines out of order and wrong glyphs (e.g. 80:41–42 drawn on page 585 line 1), and ~15 surahs lost their header/basmala slot. The importer now places words by their own `page_number`/`line_number` and moves misplaced ayah-end markers (84:21, page 589); all 604 fixtures were regenerated. New `scripts/validate-mushaf1441-page-order.mjs` (in `npm run mushaf:validate`) checks reading order, field/position agreement and header slots. Files: `scripts/import-mushaf1441-page-words.mjs`, `packages/quran-data/mushaf1441/fixtures/page-words/*`.
- **Fix — Mushaf 1441 rendering, layout and loading:** QCF text never renders before that page's own font is loaded (the old check `document.fonts.check()` returns true for unregistered fonts, and one failed neighbour font switched every page to fallback text); per-page font status with retry. Surah headers/basmalas whose slot is on the previous page are computed server-side (`pageDecorations.ts`, returned by `/api/mushaf-1441/page-words`). Lines are justified edge to edge (short surah endings centred); pages 1–2 use a centred block; surah banner no longer clips; page fills taller phone screens (up to +18%); font size is pure `cqw` so lines never overflow. Skeleton page while loading + route `loading.tsx`. `?page=N` deep links and URL sync. Files: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, `src/app/mushaf-1441/page.tsx`, `src/app/mushaf-1441/loading.tsx`, `src/app/api/mushaf-1441/page-words/route.ts`, `packages/quran-data/mushaf1441/{pageDecorations,types}.ts`; stale validator expectations updated.
- **Perf + Feature — Fast saves, fast filters, openable automated cards, Surah tab:**
  - *Save:* editor save was ~7 sequential backend round-trips (auth check, ownership check, update, delete verses, insert verses, insert parts) plus a 650 ms delay and a `router.refresh()` that re-rendered the editor. It's now **one** `save_group()` RPC and navigates immediately. Files: `src/app/api/groups/[id]/route.ts`, `src/components/EditForm.tsx`.
  - *Auth round-trips removed everywhere:* `proxy.ts`, layout, pages and API routes read the user from the session cookie (`src/lib/session-user.ts`) instead of calling `auth.getUser()` on the auth server per request. PostgREST still verifies the JWT and RLS still applies.
  - *Filtering:* surah filter is an inner join inside the main query (was a separate query first); text search is one `search_group_ids()` RPC (was 2–4 queries). Automated title filter gets a trigram index. File: `src/app/(app)/page.tsx`.
  - *Automated cards:* title and "عرض كل الآيات (N)" open the new `/automated/[id]` page with every ayah, surah links, notes and a copy-to-personal button. Files: `src/components/AutomatedCard.tsx`, `src/components/AutomatedCopyButton.tsx`, `src/components/AutomatedList.tsx`, `src/app/(app)/automated/[id]/page.tsx`.
  - *Surah tab (السور):* `/surahs` shows 114 surah cards with personal + automated counts; `/surahs/[no]` lists that surah's personal groups and automated candidates (tabs, pagination, prev/next surah). Nav added to `Sidebar` and `MobileTopbar`; `Pagination` takes a `basePath`.
  - **DB migration:** `supabase/migrations/20260915120000_perf_save_search_surahs.sql` (`save_group`, `search_group_ids`, `automated_surah_counts` view, `automated_groups_title_trgm_idx`). Must be applied **before** deploying this code.
- **Fix — Production showed no data (Vercel couldn't reach the Mac Mini):** Vercel functions in the default `iad1` region failed DNS for the Tailscale Funnel host (`getaddrinfo ENOTFOUND youssefs-mac-mini.tailcd68dd.ts.net`), so auto-login and all queries failed. Diagnostic previews on `diag/dns-region` showed `bom1` resolves it and reaches `/auth/v1/health` (200). Added `vercel.json` pinning functions to `bom1`, which is also closer to the Mac Mini. Files: `vercel.json`, `PROJECT_MASTER.md`. No DB change.
- **Docs — Agent handoff verified against live systems:** `PROJECT_MASTER.md` now has a "where to search" guide (§10), an agent access/tools section (§11), the current prod deployment, the `git-main` alias, the Vercel dashboard link, the temporary `diag/dns-region` branch, and the `page-ayat` API route. `CLAUDE.md`: removed the stale `~/dev` copy note and replaced the Management-API DDL advice with the self-hosted `docker compose exec psql` path. No code or DB change.
- **DB — Re-applied missing schema on the self-hosted stack:** `supabase-stats-aggregates.sql`, `supabase-parts-fts.sql`, `supabase-indexes.sql`, `supabase-migration-tag-uniqueness.sql` (never re-run after the cloud→Mac Mini migration). `/stats` now uses the `get_dashboard_stats()` RPC, and search uses FTS plus rasm-skeleton trigram columns instead of the ILIKE fallback. Verified through the Funnel URL as the app user (RPC counts, `surah_counts` view, FTS). Backup: `mutshabehat-selfhost/backups/pre-schema-apply-20260915b.dump`.
- **Chore — Sync GitHub with production source:** committed the previously uncommitted Mushaf 1441 module (`src/app/mushaf-1441`, `src/app/api/mushaf-1441`, `packages/`), mushaf validation/import scripts, PWA icons/manifest/SW, loading skeletons and export route, which were already live on Vercel via CLI deploys. Merged `origin/feature/mushaf-1441-module` (auto-login first-request fix + proxy test). Added `PROJECT_MASTER.md` agent handoff doc and corrected stale location/backend notes in `CLAUDE.md`. No DB migration.

## 2026-06-28
- **Feature — Mushaf 1441 reader refinements (round 4):** surah banner spans full page width; double-tap/double-click a word copies the whole ayah to clipboard (+ toast); disabled native text selection & iOS callout on the page so long-press opens the menu without highlighting text; emoji replaced with inline SVG icons in context menu + hover card; bottom surah slider less sensitive (bigger thumb, navigates on release only) with a zoomed surah-name bubble above the thumb while dragging. File: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`. No DB migration.
- **Feature — Mushaf 1441 reader refinements (round 3):** full-width surah banner (name fits width); removed technical word-mapping fields from ayah details (friendly surah+ayah header instead); desktop hover preview card (highlight colour, notes, bookmark/favourite, mutshabehat); bookmark+favourite now in any word's right-click menu (act on the ayah), all menu items iconed; mobile swipe reversed to Arabic RTL (right→next, left→previous); bottom surah slider for quick jump; mobile ayah details open by long-press only (tap no longer selects) and the menu opens without selecting; top arrows follow RTL (previous → right, next ← left). File: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`. No DB migration.
- **Feature — Mushaf 1441 reader refinements (round 2):** tap left half → next page / right half → previous (RTL); selected ayah/word uses a subtle paper-darker tint (`#ece2c8`) instead of yellow/black; 14 highlight colours (was 4); instant flipping via adjacent-page prefetch (words+metadata+font); full-bleed page on mobile; surah banner restyled with a Madina-style Islamic ornamental frame before the basmala; mutshabehat mapping activated (default flag on) — connected ayat marked + tappable connections panel. Files: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, `packages/mutshabehat-core/mushafLinkAdapter.ts`. No DB migration.
- **Feature — Mushaf 1441 reader UX overhaul:** `/mushaf-1441` is now the mushaf page itself — surah-name banner + basmala before ayah 1; juz/حزب/ربع + left/right page indicator moved into the page margins (separate header strip removed); fit-to-screen page via container-query sizing with evenly distributed 15 lines; swipe to turn pages; long-press a word/ayah for the highlight/notes menu; surah/ayah/page sliders + sign-in inside a burger menu; notes editor as an on-demand sheet. Files: `src/app/mushaf-1441/_components/Mushaf1441Viewer.tsx`, validators updated. No DB migration.
- **Fix — Login honors `?next=`:** `/login?next=/mushaf-1441` returns the user to the mushaf after sign-in (password/magic/Google). File: `src/app/(auth)/login/page.tsx`. No DB migration.
- **DB — Mushaf 1441 annotations table created (production):** applied the `mushaf_annotations` migration to the production Supabase project `tthlhkdmwusxerfiimgc`. Purely additive, isolated table (notes/highlights/bookmarks/favorites) with per-user RLS (`auth.uid() = user_id` on select/insert/update/delete), CHECK constraints (target shape, `line_number` 1–15, `page_number` 1–604, `ayah_key` format) and 3 supporting indexes. Existing tables untouched. Unblocks the Mushaf 1441 annotations feature (was 501 "table not installed"). Migration: `supabase/migrations/20260628000000_mushaf_annotations_preview.sql`.
- **Fix — Mushaf 1441 API type-safety bug:** `POST /api/mushaf-1441/annotations` failed `tsc` (validated `target` possibly `undefined` after the error guard). Tightened the guard so `target` is provably defined before the insert. File: `src/app/api/mushaf-1441/annotations/route.ts`. No DB migration needed.
- **Fix — Mushaf 1441 logged-out UX (401 vs 503):** annotations and Mutshabehat preview routes returned `503 "authentication unavailable"` for not-signed-in visitors; they now detect a missing session (`AuthSessionMissingError`) and return `401`, so the viewer shows the friendly "sign in to sync" message instead of a red error. Genuine misconfig still returns 503. Files: `src/app/api/mushaf-1441/annotations/route.ts`, `src/app/api/mushaf-1441/mutshabehat/route.ts`. No DB migration needed.

## 2026-06-11
- **Feature — Surah names preview on mobile group cards:** added a list of unique Surah names on the front face (preview) of mobile group cards in `GroupRow.tsx` formatted as inline flex items separated by gray dots and spaces (`السور: البقرة · آل عمران`). This uses a flexbox layout (`flex flex-wrap items-center`) to force block-level box formatting on children, which bypasses a well-known Safari rendering bug where inline text elements inside a parent container with 3D transforms (`preserve-3d`, `rotateY`, `backface-visibility`) flatten and overlap horizontally. Mobile tap highlights and text selection are disabled using `select-none` and `tap-highlight-color`. Files: `src/components/GroupRow.tsx`, `src/app/globals.css`. No DB migration needed.
- **Feature — Mobile header links to home page:** wrapped the mobile branding header (`متشابهات القرآن` and its subtitle) in a Link component in `MobileTopbar.tsx` so that tapping it returns the user to the home screen, matching the desktop brand behavior. Files: `src/components/MobileTopbar.tsx`. No DB migration needed.
- **Fix — Darker page background for contrast:** adjusted CSS variables (`--color-paper`, `--color-surface-2`, `--color-border`, `--color-border-soft`) in `globals.css` to make the page background slightly darker, significantly improving visual contrast and card boundary separation while maintaining the warm scholarly theme. Files: `src/app/globals.css`. No DB migration needed.
- **Fix — Mobile group cards now resize to fit active face:** modified CSS to make mobile card size only fit the active face (relative positioning when active, absolute when inactive). This keeps front face height compact, preventing unnecessary blank vertical space, and auto-extends the card to fit all ayahs when flipped. Files: `src/app/globals.css`. No DB migration needed.

## 2026-06-08 (2)
- **Fix — Swipe setting now changes the main groups screen:** when the swipe navigation setting is enabled, the mobile main screen no longer renders all flat/titles-only groups stacked vertically. It shows a single current group card with right/left swipe paging and previous/next buttons; desktop and setting-off behavior remain the normal vertical list. Files: `src/components/MainGroupSwipePager.tsx`, `src/app/(app)/page.tsx`, `src/components/SwipeNavigationSetting.tsx`. No DB migration needed.
- **Feature — Settings toggle for swipe-priority group navigation:** added a `التنقل` setting that lets the user prioritize right/left swipes for previous/next group navigation on the group detail page instead of having the gesture treated as vertical scrolling. The preference is stored per device in `localStorage` and takes effect without a reload. Files: `src/components/SwipeNavigationSetting.tsx`, `src/components/SwipeNavWrapper.tsx`, `src/app/(app)/settings/page.tsx`. No DB migration needed.

## 2026-06-08
- **Fix — Mobile group cards now flip instead of immediately navigating:** group rows and titles-only rows split desktop click-to-open from mobile tap-to-flip behavior. On mobile, tapping a card rotates it in place with stable 3D faces; the back side exposes the ayahs/options plus explicit `فتح المجموعة` and `تعديل` links, so the card tap is no longer stolen by row navigation. Files: `src/components/GroupRow.tsx`, `src/components/GroupRowTitlesOnly.tsx`, `src/app/globals.css`. No DB migration needed.

## 2026-06-07
- **Feature — SQL database backup/restore (Settings):** new `?format=sql` export emits a Supabase-compatible `.sql` restore file for all user data (tags, groups, verses, parts, group↔tag links). Idempotent upserts (`on conflict (id) do update`; links `do nothing`) in a `begin/commit` transaction, FK-ordered. Preserves original UUIDs + timestamps; embeds `user_id` literally → restore into the same project/auth user only. Files: `src/app/api/groups/export/route.ts`, `src/components/DatabaseExport.tsx`. No DB migration needed.
- **Feature — Mobile swipe navigation on group detail page:** swiping left navigates to التالية (next group) and swiping right navigates to السابقة (previous group), matching RTL reading direction. Real-time follow-finger translateX feedback with spring-back on aborted swipe; edge hint arrows (‹ ›) appear as opacity ramp during swipe. Axis detection (horiz vs. vertical) prevents interfering with normal page scroll; non-passive `touchmove` listener calls `preventDefault` only when horizontal. New file: `src/components/SwipeNavWrapper.tsx`. Updated: `src/app/(app)/groups/[id]/page.tsx` (wraps `GroupDetail` in `SwipeNavWrapper`). No DB migration needed.

## 2026-06-03 (2)
- **DB — Full indexing pass (`supabase-indexes.sql`):** Audit found 9 missing indexes. Added: `surah_counts` security-invoker view (SurahFilter was querying a non-existent relation), `groups_title_trgm_idx` + `parts_text_trgm_idx` (GIN trgm for ILIKE search fallbacks), `groups_user_title_idx(user_id, title)` (title sort), `verses_group_sort_idx(group_id, sort_order)` + `parts_verse_sort_idx(verse_id, sort_order)` (pre-sorted delivery on every group page), `group_tags_tag_id_idx(tag_id)` (reverse tag join in stats RPC), partial indexes `groups_user_favorite_idx` + `groups_user_completed_idx`, `groups_user_status_idx(user_id, status)`. **⚠️ Run `supabase-indexes.sql` in Supabase SQL Editor.**

## 2026-06-03
- **Perf — Group navigation made fast:** Three compounding performance bugs fixed:
  (1) `prefetch={false}` on all nav Links disabled the Next.js router cache — removed from `GroupMobileBottomBar` and `GroupBrowseNav`. (2) No `loading.tsx` meant blank screen on click — added skeletons for both `/groups/[id]/` and `/groups/[id]/edit/`. (3) Nested PostgREST select ran 3-4 sequential Supabase round-trips — rewritten as 2 parallel rounds: (group+verses+auth) then (parts+nextNeighbour+prevNeighbour). (4) Added `PrefetchNeighbours` client component that calls `router.prefetch()` for both neighbour routes on mount, so by the time the user taps, the RSC payload for the destination is already cached → instant navigation. Files: `src/components/PrefetchNeighbours.tsx` (new), `src/app/(app)/groups/[id]/page.tsx`, `src/app/(app)/groups/[id]/edit/page.tsx`, `src/app/(app)/groups/[id]/loading.tsx` (new), `src/app/(app)/groups/[id]/edit/loading.tsx` (new), `src/components/GroupMobileBottomBar.tsx`, `src/components/GroupBrowseNav.tsx`.
- **UX — Mobile bottom bar redesigned for large tap targets:** replaced the cramped icon-only buttons with a native-app-style tab bar: 5 full-width equal cells (رجوع · السابقة · التالية · حذف · تعديل) on the view page, 4 cells (رجوع · سابقة · حفظ · تالية) on the edit page. Each cell is `min-h-[62px]` with a 24px icon + 11px label, `active:` background feedback, and iOS safe-area inset. Confirm-delete replaces the bar inline rather than popping a modal. `src/components/GroupMobileBottomBar.tsx` (new), `src/components/EditForm.tsx`.
- **Feature — Mobile bottom action bar (view + edit pages):** on mobile the sticky top toolbars (back, prev/next, edit/save) are hidden and replaced with a `fixed bottom-0` bar that stays within thumb reach. View page bottom bar: back | prev/next/delete | edit. Edit page bottom bar: back | prev-save / save / next-save. Desktop layout is unchanged. Files: `src/app/(app)/groups/[id]/page.tsx`, `src/components/EditForm.tsx`.
- **Feature — Main-screen search includes parts text:** the `?q=` listing filter now searches both group titles AND the ayah parts text (previously title-only). Uses FTS on `search_vec` for both, with ILIKE fallback. Both searches run in parallel; results are unioned — so a group appears if the query matches its title OR any of its part text. No DB migration needed (uses existing `search_vec` and `text` columns). File: `src/app/(app)/page.tsx`.
- **Feature — Browsing-mode group navigation + delete:** the group **view** page toolbar now has
  previous/next buttons (navigate to the adjacent group's view page, no save) and a delete button
  with inline confirm (deletes, then lands on a neighbour or `/`). New client component
  `src/components/GroupBrowseNav.tsx`; `src/app/(app)/groups/[id]/page.tsx` now fetches `created_at`
  + computes prev/next neighbours. Deployed.
- **Feature — Edit-mode group navigation (review sweep):** in the group editor, the toolbar now has
  Save-&-Previous / Save-&-Next buttons (jump straight to the adjacent group's editor, no back-and-
  forth), and the main save **auto-advances to the next group** when the group is marked *completed*
  or *published* (with an inline hint + a `حفظ والتالية` button label). Neighbours computed in
  `created_at DESC` order. Files: `src/app/(app)/groups/[id]/edit/page.tsx` (adds `prevGroupId`),
  `src/components/EditForm.tsx`. Deployed.
- **Feature — Search highlighting:** matched word(s) are now highlighted (`<mark>`) inside each ayah
  in the Quran search results, with the match position mapped from normalized text back onto the
  original Uthmani characters (tashkeel/dagger-alef aware). Exact + approximate (skeleton) matches,
  and multiple occurrences, all highlight. Added `normalizeArabicWithMap()` + `matchRanges()` in
  `src/lib/arabic.ts`; `highlight()` render in `src/components/QuranSearch.tsx`. Deployed.
- **Feature — Search: Uthmani rasm tolerance (Tier 1 + Tier 2):** mushaf spelling now matches what
  users type across all search surfaces (Quran ayahs, group titles, part text).
  - *Tier 1* — dagger alef `ٰ` (U+0670) is restored to `ا` in `normalizeArabic` instead of being
    stripped as tashkeel, so `عَٰلَمِينَ`→`عالمين`, `ٱلصِّرَٰطَ`→`الصراط` match. (`src/lib/arabic.ts`)
  - *Tier 2* — `rasmSkeleton()` drops the long `ا` so plene/defective variants collapse
    (`السماوات` ↔ `السموات` → `لسموت`, `عاكفين` ↔ `عكفين`). Used as an `approximate`-flagged fallback
    tier in `src/lib/quran.ts` (`searchAyahs`) and `src/app/api/search/route.ts` (groups + parts).
    Approximate hits get a `≈ تقريبي` badge in `QuranSearch.tsx`.
  - **DB migration APPLIED** (`supabase-parts-fts.sql`): `normalize_arabic()` updated (dagger→ا),
    `parts.search_vec` rebuilt, `rasm_skeleton` generated columns + `pg_trgm` GIN indexes on
    `parts` and `groups`, and `groups.search_vec` switched to use `normalize_arabic` (was plain
    `to_tsvector(title)`). Deployed.
- **Cleanup — removed dead `/api/stats` route** (`src/app/api/stats/route.ts`): old 12-query
  dashboard endpoint, superseded by the `get_dashboard_stats()` RPC; nothing referenced it. Deployed.

# Performance program — STATUS (last updated 2026-06-03)

## ✅ DONE & deployed to production
- **Phase 1 — bundles:** `NetworkGraph.tsx` uses narrow d3 submodule imports (d3-selection/zoom/
  force/drag/interpolate/transition) instead of `import * as d3` → /network chunk **222 KB → 68 KB**.
  `package.json` swapped `d3`→submodules. Cairo fonts trimmed 5→3 weights (dropped unused 600/800) in
  `src/app/layout.tsx`.
- **Phase 2 — data layer:**
  - `src/lib/quran.ts` `searchAyahs()` uses a pre-normalized in-memory index (no re-normalizing 6,236
    ayahs per request).
  - `src/app/api/quran/route.ts` adds immutable CDN `Cache-Control` headers.
  - `src/lib/arabic.ts` `normalizeArabic` fixed to fold alef-wasla `ٱ` (search for "الرجفة" now works).
  - `src/app/(app)/stats/page.tsx` calls `get_dashboard_stats()` RPC with a fallback to the old
    12-query path.
  - **DB migration APPLIED** (`supabase-stats-aggregates.sql`): RPC + 4 indexes (`verses_surah_idx`,
    `verses_group_surah_idx`, `groups_user_created_idx`, `groups_user_updated_idx`). Verified live.
- **Phase 3 — dedup:** `src/lib/surah-names.ts` (`useSurahNames` hook, singleton in-flight promise +
  session cache). `SurahFilter`, `QuranSearch`, `AutomatedList` use it → 3 identical fetches → 1.
- **Phase 4 — offline SW:** `public/sw.js` (cache `mutshabehat-v3`) stale-while-revalidates
  `/api/quran`; cache-first for `/_next/static`; user/auth data always hits network.
- **Phase 5 — search FTS + rasm tolerance:** parts/groups search uses `tsvector` FTS on
  `search_vec` (GIN-indexed) with ILIKE fallback; Uthmani dagger-alef + rasm-skeleton matching;
  result highlighting. See the `## 2026-06-03` Changelog entries for detail. Migration
  `supabase-parts-fts.sql` applied. **Closes the former "Parts FTS" + "dead `/api/stats`" items.**

## 🔜 REMAINING (start here next session)
1. **iOS PWA icons** (blocks "installable iPhone app"): manifest only points to one SVG. Need PNG
   `apple-touch-icon` sizes (180×180, etc.) + iOS splash screens, then wire into
   `src/app/layout.tsx` `<head>` and `public/manifest.json`. **Needs a source logo/image from the user.**
2. **Native-app feature roadmap** (longer-term):
   - Spaced-repetition review of groups (data model already has favorite/completed/status).
   - Per-ayah audio recitation playback.
   - Push / daily-review reminders (Web Push, or native via Capacitor).
   - Capacitor wrapper for App Store distribution.
   - Native feel: haptics, swipe gestures, bottom-tab nav (MobileTopbar exists), Face ID lock.

## ✅ CLOSED 2026-06-03
- ~~`/api/stats` dead route~~ — removed (see Changelog).
- ~~Parts text search uses ILIKE, no tsvector~~ — upgraded to FTS + rasm tolerance (see Changelog).

## ⚠️ Action item for the user
Revoke the Supabase personal access token shared on 2026-06-03
(https://supabase.com/dashboard/account/tokens).
